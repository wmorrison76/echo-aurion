apiVersion: v1
kind: VaultMount
metadata:
  name: echo-core
spec:
  mount: kv/echo
  description: Echo AI³ application secret store
  options:
    version: 2
  allowed_roles:
    - builder
    - orchestrator
  ttl: 1h
  max_ttl: 24h
