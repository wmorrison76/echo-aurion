import express, { Request, Response } from"express";
import { body, query, validationResult } from"express-validator";
import POSService from"../services/pos.service";
import { authenticate } from"../middleware/auth"; const router = express.Router(); // Configure POS for a venue
router.post("/config", authenticate, [ body("venue_id").isUUID(), body("pos_type").isIn(["square","toast","margin_edge","other"]), body("api_key").notEmpty(), body("api_secret").notEmpty(), body("webhook_url").isURL(), body("webhook_secret").notEmpty(), body("sync_frequency_minutes").optional().isInt({ min: 1, max: 60 }), ], async (req: Request, res: Response) => { const errors = validationResult(req); if (!errors.isEmpty()) { return res.status(400).json({ errors: errors.array() }); } try { const result = await POSService.savePOSConfig( req.user.org_id, req.body.venue_id, req.body ); res.json({ success: true, message:"POS configuration saved", config: result.rows[0], }); } catch (error) { console.error("Error saving POS config:", error); res.status(500).json({ error:"Failed to save POS configuration", message: error instanceof Error ? error.message :"Unknown error", }); } }
); // Get POS configuration for a venue
router.get("/config/:venue_id", authenticate, async (req: Request, res: Response) => { try { const config = await POSService.getPOSConfig(req.params.venue_id); if (!config) { return res.status(404).json({ error:"POS configuration not found" }); } res.json({ config }); } catch (error) { console.error("Error fetching POS config:", error); res.status(500).json({ error:"Failed to fetch POS configuration", message: error instanceof Error ? error.message :"Unknown error", }); }
}); // Record a sale from POS
router.post("/record-sale", authenticate, [ body("venue_id").isUUID(), body("pos_id").notEmpty(), body("item_name").notEmpty(), body("qty_sold").isFloat({ min: 0.001 }), body("unit_price").isFloat({ min: 0 }), body("total_amount").isFloat({ min: 0 }), body("transaction_date").isISO8601(), ], async (req: Request, res: Response) => { const errors = validationResult(req); if (!errors.isEmpty()) { return res.status(400).json({ errors: errors.array() }); } try { const transaction = await POSService.recordSale({ ...req.body, transaction_date: new Date(req.body.transaction_date), }); res.json({ success: true, message:"Sale recorded successfully", transaction, }); } catch (error) { console.error("Error recording sale:", error); res.status(500).json({ error:"Failed to record sale", message: error instanceof Error ? error.message :"Unknown error", }); } }
); // Handle POS webhook (Square, Toast, MarginEdge)
router.post("/webhook/:venue_id", async (req: Request, res: Response) => { try { const { venue_id } = req.params; const webhookType = req.headers["x-webhook-type"] ||"unknown"; await POSService.logWebhook(venue_id, String(webhookType), req.body,"received"); // Parse transaction data based on POS type const transactions = parsePOSWebhook(req.body, String(webhookType)); // Record each transaction for (const transaction of transactions) { await POSService.recordSale({ ...transaction, venue_id, }); } await POSService.updateSyncStatus(venue_id,"success", 0); res.json({ success: true, message: `Processed ${transactions.length} transactions`, count: transactions.length, }); } catch (error) { console.error("Webhook processing error:", error); res.status(500).json({ error:"Webhook processing failed", message: error instanceof Error ? error.message :"Unknown error", }); }
}); // Get sales data for reporting
router.get("/sales-data/:venue_id", authenticate, [ query("start_date").isISO8601(), query("end_date").isISO8601(), query("group_by").optional().isIn(["day","hour"]), ], async (req: Request, res: Response) => { const errors = validationResult(req); if (!errors.isEmpty()) { return res.status(400).json({ errors: errors.array() }); } try { const result = await POSService.getSalesData( req.params.venue_id, new Date(req.query.start_date as string), new Date(req.query.end_date as string), (req.query.group_by as"day" |"hour") ||"day" ); res.json({ data: result.rows, count: result.rows.length, }); } catch (error) { console.error("Error fetching sales data:", error); res.status(500).json({ error:"Failed to fetch sales data", message: error instanceof Error ? error.message :"Unknown error", }); } }
); // Get top-selling items
router.get("/top-items/:venue_id", authenticate, [ query("start_date").isISO8601(), query("end_date").isISO8601(), query("limit").optional().isInt({ min: 1, max: 50 }), ], async (req: Request, res: Response) => { try { const result = await POSService.getTopSellingItems( req.params.venue_id, new Date(req.query.start_date as string), new Date(req.query.end_date as string), parseInt(req.query.limit as string) || 10 ); res.json({ items: result.rows, count: result.rows.length, }); } catch (error) { console.error("Error fetching top items:", error); res.status(500).json({ error:"Failed to fetch top-selling items", message: error instanceof Error ? error.message :"Unknown error", }); } }
); // Create dynamic pricing rule
router.post("/pricing-rule", authenticate, [ body("item_id").isUUID(), body("base_price").isFloat({ min: 0 }), body("min_price").optional().isFloat({ min: 0 }), body("max_price").optional().isFloat({ min: 0 }), body("venue_id").optional().isUUID(), ], async (req: Request, res: Response) => { const errors = validationResult(req); if (!errors.isEmpty()) { return res.status(400).json({ errors: errors.array() }); } try { const result = await POSService.createDynamicPricingRule( req.user.org_id, req.body ); res.json({ success: true, message:"Pricing rule created", rule: result.rows[0], }); } catch (error) { console.error("Error creating pricing rule:", error); res.status(500).json({ error:"Failed to create pricing rule", message: error instanceof Error ? error.message :"Unknown error", }); } }
); // Calculate optimal price for an item
router.get("/optimal-price/:item_id", authenticate, [query("venue_id").isUUID()], async (req: Request, res: Response) => { try { const pricing = await POSService.calculateOptimalPrice( req.params.item_id, req.query.venue_id as string ); if (!pricing) { return res.status(404).json({ error:"No pricing rule found for this item", }); } res.json(pricing); } catch (error) { console.error("Error calculating optimal price:", error); res.status(500).json({ error:"Failed to calculate optimal price", message: error instanceof Error ? error.message :"Unknown error", }); } }
); // Get revenue report
router.get("/revenue-report/:venue_id", authenticate, [ query("start_date").isISO8601(), query("end_date").isISO8601(), ], async (req: Request, res: Response) => { try { const result = await POSService.getRevenueReport( req.params.venue_id, new Date(req.query.start_date as string), new Date(req.query.end_date as string) ); res.json({ report: result.rows, count: result.rows.length, }); } catch (error) { console.error("Error fetching revenue report:", error); res.status(500).json({ error:"Failed to fetch revenue report", message: error instanceof Error ? error.message :"Unknown error", }); } }
); // Helper function to parse webhook payloads from different POS systems
function parsePOSWebhook( payload: Record<string, any>, webhookType: string
): Array<{ pos_id: string; item_id?: string; item_name: string; sku?: string; qty_sold: number; unit_price: number; total_amount: number; cost_price?: number; transaction_date: Date; payment_method?: string; server_name?: string; table_number?: string;
}> { const transactions = []; switch (webhookType) { case"square": // Parse Square webhook format if (payload.order && payload.order.line_items) { payload.order.line_items.forEach((item: any) => { transactions.push({ pos_id: payload.order.id, item_name: item.name, qty_sold: item.quantity, unit_price: (item.base_price_money?.amount || 0) / 100, total_amount: (item.gross_sales_money?.amount || 0) / 100, transaction_date: new Date(payload.order.created_at), table_number: payload.order.reference_id, }); }); } break; case"toast": // Parse Toast webhook format if (payload.lineItems) { payload.lineItems.forEach((item: any) => { transactions.push({ pos_id: payload.checkId, item_name: item.name, qty_sold: item.quantity, unit_price: item.unitPriceBeforeModifiers, total_amount: item.total, transaction_date: new Date(payload.closedTime), table_number: payload.tableNumber, }); }); } break; case"margin_edge": // Parse MarginEdge webhook format if (payload.items) { payload.items.forEach((item: any) => { transactions.push({ pos_id: payload.transactionId, item_name: item.itemName, sku: item.sku, qty_sold: item.quantity, unit_price: item.price, total_amount: item.totalPrice, cost_price: item.costPrice, transaction_date: new Date(payload.timestamp), server_name: payload.serverName, }); }); } break; default: // Generic format transactions.push({ pos_id: payload.id ||"unknown", item_name: payload.itemName ||"Unknown Item", qty_sold: payload.quantity || 1, unit_price: payload.price || 0, total_amount: payload.total || 0, transaction_date: new Date(payload.timestamp || Date.now()), }); } return transactions;
} export default router;
