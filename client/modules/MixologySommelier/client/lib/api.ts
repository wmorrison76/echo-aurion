// API Configuration and Service Layer
// Support both REACT_APP_ and VITE_ prefixes for environment variables
const API_BASE = (import.meta.env.REACT_APP_API_URL ||
  import.meta.env.VITE_API_URL ||
  "http://localhost:8080") as string;
const PAIRING_API = (import.meta.env.REACT_APP_PAIRING_URL ||
  import.meta.env.VITE_PAIRING_URL ||
  "http://localhost:8090") as string;
const COSTING_API = (import.meta.env.REACT_APP_COSTING_URL ||
  import.meta.env.VITE_COSTING_URL ||
  "http://localhost:8100") as string;
const ANALYTICS_API = (import.meta.env.REACT_APP_ANALYTICS_URL ||
  import.meta.env.VITE_ANALYTICS_URL ||
  "http://localhost:8120") as string;
const IOT_API = (import.meta.env.REACT_APP_IOT_URL ||
  import.meta.env.VITE_IOT_URL ||
  "http://localhost:8140") as string;
const TRAINING_API = (import.meta.env.REACT_APP_TRAINING_URL ||
  import.meta.env.VITE_TRAINING_URL ||
  "http://localhost:8150") as string;
const ARCHIVE_API = (import.meta.env.REACT_APP_ARCHIVE_URL ||
  import.meta.env.VITE_ARCHIVE_URL ||
  "http://localhost:8160") as string;
const BUILDER_API_KEY = (import.meta.env.REACT_APP_BUILDER_API_KEY ||
  import.meta.env.VITE_BUILDER_API_KEY ||
  "476345005b164694b85d4afb69cf760e") as string;
export interface ApiError {
  message: string;
  status: number;
  details?: any;
}
class ApiService {
  private authToken: string | null = null;
  setAuthToken(token: string) {
    this.authToken = token;
    localStorage.setItem("authToken", token);
  }
  getAuthToken(): string | null {
    return this.authToken || localStorage.getItem("authToken");
  }
  private async fetch<T>(url: string, options: RequestInit = {}): Promise<T> {
    const headers: HeadersInit = {
      "Content-Type": "application/json",
      ...options.headers,
    };
    if (this.authToken) {
      headers["Authorization"] = `Bearer ${this.authToken}`;
    }
    const response = await fetch(url, { ...options, headers });
    if (!response.ok) {
      const error: ApiError = {
        message: `API Error: ${response.statusText}`,
        status: response.status,
      };
      try {
        error.details = await response.json();
      } catch {
        error.details = null;
      }
      throw error;
    }
    return response.json();
  } // Auth API async login(email: string, password: string) { const data = await this.fetch(`${API_BASE}/api/auth/login`, { method:"POST", body: JSON.stringify({ email, password }), }); if (data.token) { this.setAuthToken(data.token); } return data; } async logout() { this.authToken = null; localStorage.removeItem("authToken"); } // Wine API async getWines(filters?: { type?: string; search?: string; sortBy?: string }) { const params = new URLSearchParams(); if (filters?.type) params.append("type", filters.type); if (filters?.search) params.append("search", filters.search); if (filters?.sortBy) params.append("sortBy", filters.sortBy); const query = params.toString(); return this.fetch(`${API_BASE}/api/wines${query ?"?" + query :""}`); } async getWineById(id: string) { return this.fetch(`${API_BASE}/api/wines/${id}`); } async createWine(wine: any) { return this.fetch(`${API_BASE}/api/wines`, { method:"POST", body: JSON.stringify(wine), }); } async updateWine(id: string, wine: any) { return this.fetch(`${API_BASE}/api/wines/${id}`, { method:"PATCH", body: JSON.stringify(wine), }); } // Inventory API async getInventory(filters?: { location?: string; search?: string }) { const params = new URLSearchParams(); if (filters?.location) params.append("location", filters.location); if (filters?.search) params.append("search", filters.search); const query = params.toString(); return this.fetch(`${API_BASE}/api/inventory${query ?"?" + query :""}`); } async getInventoryItem(id: string) { return this.fetch(`${API_BASE}/api/inventory/${id}`); } async updateInventoryItem(id: string, item: any) { return this.fetch(`${API_BASE}/api/inventory/${id}`, { method:"PATCH", body: JSON.stringify(item), }); } async createInventoryItem(item: any) { return this.fetch(`${API_BASE}/api/inventory`, { method:"POST", body: JSON.stringify(item), }); } async deleteInventoryItem(id: string) { return this.fetch(`${API_BASE}/api/inventory/${id}`, { method:"DELETE", }); } // Pairing Engine API (Port 8090) async getPairingRecommendations(dish: string, preferences?: any) { return this.fetch(`${PAIRING_API}/pairings/compute`, { method:"POST", body: JSON.stringify({ dish, preferences }), }); } async getPairingRationale(wineId: string, dishId: string) { return this.fetch(`${PAIRING_API}/pairings/rationale`, { method:"POST", body: JSON.stringify({ wineId, dishId }), }); } // Sales Analytics API (Port 8120) async getAnalytics(period?: string) { const params = new URLSearchParams(); if (period) params.append("period", period); const query = params.toString(); return this.fetch(`${ANALYTICS_API}/analytics${query ?"?" + query :""}`); } async getTopPairings(limit: number = 10) { return this.fetch(`${ANALYTICS_API}/analytics/top-pairings?limit=${limit}`); } async getSalesMetrics(startDate?: string, endDate?: string) { const params = new URLSearchParams(); if (startDate) params.append("startDate", startDate); if (endDate) params.append("endDate", endDate); const query = params.toString(); return this.fetch(`${ANALYTICS_API}/analytics/sales${query ?"?" + query :""}`); } // Costing API (Port 8100) async getMonthEndReport(month: number, year: number) { return this.fetch(`${COSTING_API}/reports/month-end?month=${month}&year=${year}`); } async getCostAnalysis(method:"FIFO" |"LIFO" |"WAC" ="WAC") { return this.fetch(`${COSTING_API}/reports/cost-analysis?method=${method}`); } async getInventoryValue() { return this.fetch(`${COSTING_API}/reports/inventory-value`); } // IoT Monitoring API (Port 8140) async getTemperatureAlerts() { return this.fetch(`${IOT_API}/alerts`); } async getTemperatureHistory(location: string, hours: number = 24) { return this.fetch(`${IOT_API}/sensors/${location}/history?hours=${hours}`); } async getLocationStats(location: string) { return this.fetch(`${IOT_API}/sensors/${location}/stats`); } // Training API (Port 8150) async getTrainingFlashcards(topic?: string) { const params = new URLSearchParams(); if (topic) params.append("topic", topic); params.append("count","10"); const query = params.toString(); const response = await this.fetch<any>(`${TRAINING_API}/training/flashcards${query ?"?" + query :""}`); return response.data || response; } async getBlindTastingScenario() { const response = await this.fetch<any>(`${TRAINING_API}/training/exam/blind`); return response.data || response; } async submitBlindTastingAnswer(scenarioId: string, answer: string) { return this.fetch(`${TRAINING_API}/training/exam/submit`, { method:"POST", body: JSON.stringify({ scenarioId, answer }), }); } async getTrainingLeaderboard() { const response = await this.fetch<any>(`${TRAINING_API}/training/leaderboard`); return response.data || response; } // Wine Archive API (Port 8160) async getVintageHistory(wineName: string) { return this.fetch(`${ARCHIVE_API}/archive/vintage?wine=${encodeURIComponent(wineName)}`); } async getDecadeAnalysis(decade: number) { return this.fetch(`${ARCHIVE_API}/archive/decade/${decade}`); } async getProducerHistory(producer: string) { return this.fetch(`${ARCHIVE_API}/archive/producer?name=${encodeURIComponent(producer)}`); } async searchArchive(query: string) { return this.fetch(`${ARCHIVE_API}/archive/search?q=${encodeURIComponent(query)}`); } // Purchase Orders API async getPurchaseOrders(status?: string) { const params = new URLSearchParams(); if (status) params.append("status", status); const query = params.toString(); return this.fetch(`${API_BASE}/api/purchases${query ?"?" + query :""}`); } async createPurchaseOrder(po: any) { return this.fetch(`${API_BASE}/api/purchases`, { method:"POST", body: JSON.stringify(po), }); } async updatePurchaseOrder(id: string, po: any) { return this.fetch(`${API_BASE}/api/purchases/${id}`, { method:"PATCH", body: JSON.stringify(po), }); } async deletePurchaseOrder(id: string) { return this.fetch(`${API_BASE}/api/purchases/${id}`, { method:"DELETE", }); } // Builder.io Recipe API async getRecipes() { try { const response = await fetch( `https://cdn.builder.io/api/v3/content/echo-recipe?apiKey=${BUILDER_API_KEY}&limit=100`, ); if (!response.ok) throw new Error("Failed to fetch recipes"); const data = await response.json(); return data.results || []; } catch (error) { console.error("Error fetching recipes from Builder.io:", error); return []; } } async getRecipeById(id: string) { try { const response = await fetch( `https://cdn.builder.io/api/v3/content/echo-recipe/${id}?apiKey=${BUILDER_API_KEY}`, ); if (!response.ok) throw new Error("Failed to fetch recipe"); const data = await response.json(); return data.data || null; } catch (error) { console.error("Error fetching recipe from Builder.io:", error); return null; } } // Menu Items API async getMenuItems(venueId?: string) { const params = new URLSearchParams(); if (venueId) params.append("venueId", venueId); const query = params.toString(); return this.fetch(`${ANALYTICS_API}/analytics/menu-items${query ?"?" + query :""}`); } async syncMenuItems(menu: any[]) { return this.fetch(`${ANALYTICS_API}/analytics/menu/sync`, { method:"POST", body: JSON.stringify({ menu }), }); } async getMenuItemTrend(venueId: string, menuItemId: string, days?: number) { const params = new URLSearchParams(); if (days) params.append("days", days.toString()); const query = params.toString(); return this.fetch( `${ANALYTICS_API}/analytics/trend/${venueId}/${menuItemId}${query ?"?" + query :""}`, ); } // Onboarding API async getStateRegulations() { return this.fetch(`${API_BASE}/api/onboarding/state-regulations`); } async getStateRegulation(state: string) { return this.fetch(`${API_BASE}/api/onboarding/state-regulations/${state}`); } async validateOnboardingConfig(config: any) { return this.fetch(`${API_BASE}/api/onboarding/validate`, { method:"POST", body: JSON.stringify(config), }); } async initializeSystem(config: any) { return this.fetch(`${API_BASE}/api/onboarding/initialize`, { method:"POST", body: JSON.stringify(config), }); } async getOnboardingStatus(orgId: string) { return this.fetch(`${API_BASE}/api/onboarding/status/${orgId}`); } async seedStateRegulations() { return this.fetch(`${API_BASE}/api/onboarding/seed-regulations`, { method:"POST", }); } // Liquor Inventory API async getLiquorInventory(venueId?: string) { const params = new URLSearchParams(); if (venueId) params.append("venueId", venueId); const query = params.toString(); return this.fetch(`${API_BASE}/api/liquor-inventory${query ?"?" + query :""}`); } async getLiquorInventoryByVenue(venueId: string) { return this.fetch(`${API_BASE}/api/liquor-inventory/by-venue/${venueId}`); } async getInventoryStats(venueId: string) { return this.fetch(`${API_BASE}/api/liquor-inventory/stats/${venueId}`); } async getLowStockItems(venueId: string) { return this.fetch(`${API_BASE}/api/liquor-inventory/low-stock/${venueId}`); } async addLiquorItem(liquorData: any) { return this.fetch(`${API_BASE}/api/liquor-inventory`, { method:"POST", body: JSON.stringify(liquorData), }); } async addStockToVenue(liquorId: string, stockData: any) { return this.fetch(`${API_BASE}/api/liquor-inventory/${liquorId}/add-stock`, { method:"POST", body: JSON.stringify(stockData), }); } // Transfers API async createTransfer(transferData: any) { return this.fetch(`${API_BASE}/api/transfers`, { method:"POST", body: JSON.stringify(transferData), }); } async getTransfers(filters?: any) { const params = new URLSearchParams(); if (filters?.venueId) params.append("venueId", filters.venueId); if (filters?.status) params.append("status", filters.status); const query = params.toString(); return this.fetch(`${API_BASE}/api/transfers${query ?"?" + query :""}`); } async getTransferDetails(transferId: string) { return this.fetch(`${API_BASE}/api/transfers/${transferId}`); } async approveTransfer(transferId: string, approvedByUserId: string) { return this.fetch(`${API_BASE}/api/transfers/${transferId}/approve`, { method:"PATCH", body: JSON.stringify({ approvedByUserId }), }); } async receiveTransfer(transferId: string, receiptData: any) { return this.fetch(`${API_BASE}/api/transfers/${transferId}/receive`, { method:"PATCH", body: JSON.stringify(receiptData), }); } // Comped Drinks API async logCompedDrink(compData: any) { return this.fetch(`${API_BASE}/api/comped-drinks`, { method:"POST", body: JSON.stringify(compData), }); } async getCompedDrinks(filters?: any) { const params = new URLSearchParams(); if (filters?.venueId) params.append("venueId", filters.venueId); if (filters?.compReason) params.append("compReason", filters.compReason); const query = params.toString(); return this.fetch(`${API_BASE}/api/comped-drinks${query ?"?" + query :""}`); } async getCompedDrinksStats(venueId: string) { return this.fetch(`${API_BASE}/api/comped-drinks/stats/${venueId}`); } async getCompedDrinksRDAnalysis(venueId: string) { return this.fetch(`${API_BASE}/api/comped-drinks/rd-analysis/${venueId}`); } // Variance & Audit API async startInventoryAudit(auditData: any) { return this.fetch(`${API_BASE}/api/variance-audit/start-audit`, { method:"POST", body: JSON.stringify(auditData), }); } async addAuditCount(auditId: string, countData: any) { return this.fetch(`${API_BASE}/api/variance-audit/${auditId}/add-count`, { method:"POST", body: JSON.stringify(countData), }); } async completeAudit(auditId: string) { return this.fetch(`${API_BASE}/api/variance-audit/${auditId}/complete`, { method:"PATCH", }); } async getAudits(venueId: string) { return this.fetch(`${API_BASE}/api/variance-audit/${venueId}`); } async getFlaggedVariances(venueId: string) { return this.fetch(`${API_BASE}/api/variance-audit/flagged/${venueId}`); } async getVarianceReport(venueId: string) { return this.fetch(`${API_BASE}/api/variance-audit/report/${venueId}`); } // Mini Bar API async createMiniBarPOI(poiData: any) { return this.fetch(`${API_BASE}/api/mini-bar/pois`, { method:"POST", body: JSON.stringify(poiData), }); } async getMiniBarPOIs(venueId: string) { return this.fetch(`${API_BASE}/api/mini-bar/pois/${venueId}`); } async getMiniBarStock(poiId: string) { return this.fetch(`${API_BASE}/api/mini-bar/${poiId}`); } async addMiniBarStock(poiId: string, stockData: any) { return this.fetch(`${API_BASE}/api/mini-bar/${poiId}/stock`, { method:"POST", body: JSON.stringify(stockData), }); } async restockMiniBar(poiId: string, restockData: any) { return this.fetch(`${API_BASE}/api/mini-bar/${poiId}/restock`, { method:"POST", body: JSON.stringify(restockData), }); } async getMiniBarSummary(venueId: string) { return this.fetch(`${API_BASE}/api/mini-bar/venues/${venueId}/summary`); } // Banquet API async createBanquetEvent(eventData: any) { return this.fetch(`${API_BASE}/api/banquet/events`, { method:"POST", body: JSON.stringify(eventData), }); } async getBanquetEvents(venueId: string) { return this.fetch(`${API_BASE}/api/banquet/events/${venueId}`); } async getBanquetEventDetails(eventId: string) { return this.fetch(`${API_BASE}/api/banquet/events/${eventId}/details`); } async startBanquetEvent(eventId: string, startData: any) { return this.fetch(`${API_BASE}/api/banquet/events/${eventId}/start`, { method:"POST", body: JSON.stringify(startData), }); } async endBanquetEvent(eventId: string, endData: any) { return this.fetch(`${API_BASE}/api/banquet/events/${eventId}/end`, { method:"PATCH", body: JSON.stringify(endData), }); } async getBanquetVarianceReport(eventId: string) { return this.fetch(`${API_BASE}/api/banquet/${eventId}/report`); } async getBanquetHistory(venueId: string) { return this.fetch(`${API_BASE}/api/banquet/venues/${venueId}/history`); } // Advanced Analytics API analytics = { getForecasts: (venueId: string, days?: number) => { const params = new URLSearchParams(); if (days) params.append("days", days.toString()); const query = params.toString(); return this.fetch(`${API_BASE}/api/analytics/forecasts/${venueId}${query ?"?" + query :""}`); }, getAnomalies: (venueId: string) => { return this.fetch(`${API_BASE}/api/analytics/anomalies/${venueId}`); }, getInventoryOptimization: (venueId: string) => { return this.fetch(`${API_BASE}/api/analytics/inventory-optimization/${venueId}`); }, getRevenue: (venueId: string, days?: number) => { const params = new URLSearchParams(); if (days) params.append("days", days.toString()); const query = params.toString(); return this.fetch(`${API_BASE}/api/analytics/revenue/${venueId}${query ?"?" + query :""}`); }, getTopItems: (venueId: string, days?: number, limit?: number) => { const params = new URLSearchParams(); if (days) params.append("days", days.toString()); if (limit) params.append("limit", limit.toString()); const query = params.toString(); return this.fetch(`${API_BASE}/api/analytics/top-items/${venueId}${query ?"?" + query :""}`); }, initiateSync: (venueId: string, syncType: string) => { return this.fetch(`${API_BASE}/api/analytics/sync/${venueId}`, { method:"POST", body: JSON.stringify({ sync_type: syncType }), }); }, getSyncStatus: (operationId: string) => { return this.fetch(`${API_BASE}/api/analytics/sync-status/${operationId}`); }, getInventorySnapshot: (venueId: string) => { return this.fetch(`${API_BASE}/api/analytics/inventory-snapshot/${venueId}`); }, getActiveAlerts: (venueId: string) => { return this.fetch(`${API_BASE}/api/analytics/alerts/active/${venueId}`); }, getAlertHistory: (venueId: string, limit?: number, severity?: string) => { const params = new URLSearchParams(); if (limit) params.append("limit", limit.toString()); if (severity) params.append("severity", severity); const query = params.toString(); return this.fetch(`${API_BASE}/api/analytics/alerts/history/${venueId}${query ?"?" + query :""}`); }, acknowledgeAlert: (alertId: string) => { return this.fetch(`${API_BASE}/api/analytics/alerts/acknowledge/${alertId}`, { method:"POST", }); }, getWebSocketHealth: () => { return this.fetch(`${API_BASE}/api/analytics/websocket/health`); }, getEventHistory: (venueId: string, limit?: number) => { const params = new URLSearchParams(); if (limit) params.append("limit", limit.toString()); const query = params.toString(); return this.fetch(`${API_BASE}/api/analytics/events/history/${venueId}${query ?"?" + query :""}`); }, };
}
export const apiService = new ApiService();
export { ApiService };
export default apiService;
