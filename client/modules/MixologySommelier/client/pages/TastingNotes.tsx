import React, { useState, useEffect } from "react";
import { useTheme } from "../components/ThemeProvider";
import { wines, aromaOptions, type TastingNote } from "../lib/wines";
import { BookOpen, Plus, Trash2, Wine, Star } from "lucide-react";
export const TastingNotes: React.FC = () => {
  const { theme, isDark } = useTheme();
  const [notes, setNotes] = useState<TastingNote[]>([]);
  const [selectedWine, setSelectedWine] = useState<string>(wines[0].id);
  const [aromaNotes, setAromaNotes] = useState<string[]>([]);
  const [palateNotes, setPalateNotes] = useState<string[]>([]);
  const [finishNotes, setFinishNotes] = useState<string>("");
  const [personalRating, setPersonalRating] = useState<number>(5);
  useEffect(() => {
    const saved = localStorage.getItem("tastingNotes");
    if (saved) {
      try {
        setNotes(JSON.parse(saved));
      } catch {
        // Ignore parse errors } } }, []); useEffect(() => { localStorage.setItem("tastingNotes", JSON.stringify(notes)); }, [notes]); const addNote = () => { if (!selectedWine || aromaNotes.length === 0) { alert("Please select a wine and aroma notes"); return; } const newNote: TastingNote = { id: Date.now().toString(), wineId: selectedWine, aromaNotes, palateNotes, finishNotes, personalRating, createdAt: new Date(), }; setNotes([newNote, ...notes]); setAromaNotes([]); setPalateNotes([]); setFinishNotes(""); setPersonalRating(5); }; const deleteNote = (id: string) => { setNotes(notes.filter((n) => n.id !== id)); }; const getWineDetails = (wineId: string) => { return wines.find((w) => w.id === wineId); }; return ( <div style={{ backgroundColor: theme.colors.background, color: theme.colors.foreground, minHeight:"100vh", padding:"3rem 2rem", background: isDark ? `linear-gradient(135deg, ${theme.colors.background} 0%, rgba(${theme.colors.primary},0.03) 100%)` : theme.colors.background, }} > <div style={{ maxWidth:"1400px", margin:"0 auto" }}> {/* Header Section */} <div style={{ marginBottom:"3.5rem" }}> <div style={{ display:"flex", alignItems:"center", gap:"1rem", marginBottom:"0.75rem", }} > <div style={{ background: `linear-gradient(135deg, ${theme.colors.primary}, ${theme.colors.secondary})`, borderRadius:"12px", padding:"0.75rem", display:"flex", alignItems:"center", justifyContent:"center", }} > <BookOpen size={28} style={{ color: theme.colors.background }} /> </div> <h1 style={{ fontSize:"2.75rem", fontWeight:"700", marginBottom: 0, color: theme.colors.foreground, letterSpacing:"-0.5px", }} > Tasting Journal </h1> </div> <p style={{ color: theme.text.secondary, fontSize:"1.05rem", marginTop:"0.5rem", }} > Track and remember every wine you taste </p> </div> <div style={{ display:"grid", gridTemplateColumns:"1fr 1fr", gap:"2.5rem", marginBottom:"3rem", }} > {/* Add New Note Form */} <div style={{ backgroundColor: theme.colors.card, border: `1px solid ${theme.colors.border}`, borderRadius:"16px", padding:"2.5rem", boxShadow: isDark ? `0 4px 20px rgba(0,0,0,0.3), 0 0 40px rgba(${parseInt(theme.colors.primary.slice(1, 3), 16)}, ${parseInt(theme.colors.primary.slice(3, 5), 16)}, ${parseInt(theme.colors.primary.slice(5, 7), 16)}, 0.05)` :"0 2px 12px rgba(0,0,0,0.06)", backdropFilter: isDark ?"blur(10px)" :"none", transition:"all 0.3s ease", position:"relative", overflow:"hidden", }} > <h2 style={{ color: theme.colors.foreground, fontSize:"1.5rem", fontWeight:"700", marginBottom:"2rem", letterSpacing:"-0.3px", }} > Add a Tasting Note </h2> <div style={{ display:"flex", flexDirection:"column", gap:"2rem" }} > {/* Wine Selection */} <div> <label style={{ display:"block", color: theme.colors.foreground, fontWeight:"600", marginBottom:"0.75rem", fontSize:"0.95rem", }} > Select Wine </label> <select value={selectedWine} onChange={(e) => setSelectedWine(e.target.value)} style={{ width:"100%", padding:"0.875rem 1.125rem", backgroundColor: theme.colors.background, color: theme.colors.foreground, border: `1.5px solid ${theme.colors.border}`, borderRadius:"10px", outline:"none", fontSize:"0.95rem", transition:"all 0.2s", cursor:"pointer", fontWeight:"500", }} onFocus={(e) => { e.currentTarget.style.borderColor = theme.colors.primary; e.currentTarget.style.boxShadow = `0 0 0 3px rgba(${parseInt(theme.colors.primary.slice(1, 3), 16)}, ${parseInt(theme.colors.primary.slice(3, 5), 16)}, ${parseInt(theme.colors.primary.slice(5, 7), 16)}, 0.1)`; }} onBlur={(e) => { e.currentTarget.style.borderColor = theme.colors.border; e.currentTarget.style.boxShadow ="none"; }} > {wines.map((wine) => ( <option key={wine.id} value={wine.id}> {wine.name} ({wine.vintage}) </option> ))} </select> </div> {/* Aroma Notes */} <div> <label style={{ display:"block", color: theme.colors.foreground, fontWeight:"600", marginBottom:"1rem", fontSize:"0.95rem", }} > Aroma Notes </label> <div style={{ display:"grid", gridTemplateColumns:"repeat(2, 1fr)", gap:"0.625rem", }} > {aromaOptions.map((aroma) => ( <button key={aroma} onClick={() => { setAromaNotes((prev) => prev.includes(aroma) ? prev.filter((a) => a !== aroma) : [...prev, aroma], ); }} style={{ padding:"0.625rem 1rem", backgroundColor: aromaNotes.includes(aroma) ? theme.colors.primary : theme.colors.background, color: aromaNotes.includes(aroma) ? theme.colors.background : theme.colors.foreground, border: `1.5px solid ${aromaNotes.includes(aroma) ? theme.colors.primary : theme.colors.border}`, borderRadius:"10px", cursor:"pointer", fontSize:"0.85rem", fontWeight:"500", transition:"all 0.2s cubic-bezier(0.4, 0, 0.2, 1)", boxShadow: aromaNotes.includes(aroma) ? `0 0 12px rgba(${parseInt(theme.colors.primary.slice(1, 3), 16)}, ${parseInt(theme.colors.primary.slice(3, 5), 16)}, ${parseInt(theme.colors.primary.slice(5, 7), 16)}, 0.3)` :"none", }} onMouseEnter={(e) => { if (!aromaNotes.includes(aroma)) { e.currentTarget.style.backgroundColor = theme.colors.border; } }} onMouseLeave={(e) => { if (!aromaNotes.includes(aroma)) { e.currentTarget.style.backgroundColor = theme.colors.background; } }} > {aroma} </button> ))} </div> </div> {/* Palate Notes */} <div> <label style={{ display:"block", color: theme.colors.foreground, fontWeight:"600", marginBottom:"1rem", fontSize:"0.95rem", }} > Palate Notes </label> <div style={{ display:"grid", gridTemplateColumns:"repeat(2, 1fr)", gap:"0.625rem", }} > {["Dry","Sweet","Acidic","Tannic","Smooth","Crisp"].map( (note) => ( <button key={note} onClick={() => { setPalateNotes((prev) => prev.includes(note) ? prev.filter((n) => n !== note) : [...prev, note], ); }} style={{ padding:"0.625rem 1rem", backgroundColor: palateNotes.includes(note) ? theme.colors.primary : theme.colors.background, color: palateNotes.includes(note) ? theme.colors.background : theme.colors.foreground, border: `1.5px solid ${palateNotes.includes(note) ? theme.colors.primary : theme.colors.border}`, borderRadius:"10px", cursor:"pointer", fontSize:"0.85rem", fontWeight:"500", transition:"all 0.2s cubic-bezier(0.4, 0, 0.2, 1)", boxShadow: palateNotes.includes(note) ? `0 0 12px rgba(${parseInt(theme.colors.primary.slice(1, 3), 16)}, ${parseInt(theme.colors.primary.slice(3, 5), 16)}, ${parseInt(theme.colors.primary.slice(5, 7), 16)}, 0.3)` :"none", }} onMouseEnter={(e) => { if (!palateNotes.includes(note)) { e.currentTarget.style.backgroundColor = theme.colors.border; } }} onMouseLeave={(e) => { if (!palateNotes.includes(note)) { e.currentTarget.style.backgroundColor = theme.colors.background; } }} > {note} </button> ), )} </div> </div> {/* Finish Notes */} <div> <label style={{ display:"block", color: theme.colors.foreground, fontWeight:"600", marginBottom:"0.75rem", fontSize:"0.95rem", }} > Finish Notes </label> <textarea value={finishNotes} onChange={(e) => setFinishNotes(e.target.value)} placeholder="Describe the aftertaste..." style={{ width:"100%", padding:"0.875rem 1.125rem", backgroundColor: theme.colors.background, color: theme.colors.foreground, border: `1.5px solid ${theme.colors.border}`, borderRadius:"10px", fontFamily:"inherit", fontSize:"0.95rem", outline:"none", minHeight:"100px", resize:"vertical", transition:"all 0.2s", }} onFocus={(e) => { e.currentTarget.style.borderColor = theme.colors.primary; e.currentTarget.style.boxShadow = `0 0 0 3px rgba(${parseInt(theme.colors.primary.slice(1, 3), 16)}, ${parseInt(theme.colors.primary.slice(3, 5), 16)}, ${parseInt(theme.colors.primary.slice(5, 7), 16)}, 0.1)`; }} onBlur={(e) => { e.currentTarget.style.borderColor = theme.colors.border; e.currentTarget.style.boxShadow ="none"; }} /> </div> {/* Rating */} <div> <div style={{ display:"flex", justifyContent:"space-between", alignItems:"center", marginBottom:"1rem", }} > <label style={{ color: theme.colors.foreground, fontWeight:"600", fontSize:"0.95rem", }} > Your Rating </label> <div style={{ display:"flex", alignItems:"center", gap:"0.375rem", }} > {[...Array(Math.round(personalRating / 2))].map((_, i) => ( <Star key={i} size={18} style={{ fill: theme.colors.primary, color: theme.colors.primary, }} /> ))} <span style={{ marginLeft:"0.5rem", color: theme.colors.primary, fontWeight:"700", fontSize:"0.95rem", }} > {personalRating}/10 </span> </div> </div> <input type="range" min="0" max="10" value={personalRating} onChange={(e) => setPersonalRating(parseInt(e.target.value))} style={{ width:"100%", height:"6px", cursor:"pointer", accentColor: theme.colors.primary, borderRadius:"3px", }} /> </div> {/* Submit Button */} <button onClick={addNote} style={{ padding:"1rem", backgroundColor: theme.colors.primary, color: theme.colors.background, border:"none", borderRadius:"10px", fontWeight:"600", cursor:"pointer", display:"flex", alignItems:"center", justifyContent:"center", gap:"0.75rem", fontSize:"0.95rem", boxShadow: isDark ? `0 4px 16px rgba(${parseInt(theme.colors.primary.slice(1, 3), 16)}, ${parseInt(theme.colors.primary.slice(3, 5), 16)}, ${parseInt(theme.colors.primary.slice(5, 7), 16)}, 0.4)` :"0 2px 8px rgba(0, 122, 255, 0.2)", transition:"all 0.3s cubic-bezier(0.4, 0, 0.2, 1)", }} onMouseEnter={(e) => { (e.currentTarget as HTMLButtonElement).style.transform ="translateY(-2px)"; (e.currentTarget as HTMLButtonElement).style.boxShadow = isDark ? `0 6px 24px rgba(${parseInt(theme.colors.primary.slice(1, 3), 16)}, ${parseInt(theme.colors.primary.slice(3, 5), 16)}, ${parseInt(theme.colors.primary.slice(5, 7), 16)}, 0.5)` :"0 4px 16px rgba(0, 122, 255, 0.3)"; }} onMouseLeave={(e) => { (e.currentTarget as HTMLButtonElement).style.transform ="translateY(0)"; (e.currentTarget as HTMLButtonElement).style.boxShadow = isDark ? `0 4px 16px rgba(${parseInt(theme.colors.primary.slice(1, 3), 16)}, ${parseInt(theme.colors.primary.slice(3, 5), 16)}, ${parseInt(theme.colors.primary.slice(5, 7), 16)}, 0.4)` :"0 2px 8px rgba(0, 122, 255, 0.2)"; }} > <Plus size={20} /> Save Note </button> </div> </div> {/* Notes List */} <div style={{ backgroundColor: theme.colors.card, border: `1px solid ${theme.colors.border}`, borderRadius:"16px", padding:"2.5rem", boxShadow: isDark ? `0 4px 20px rgba(0,0,0,0.3), 0 0 40px rgba(${parseInt(theme.colors.primary.slice(1, 3), 16)}, ${parseInt(theme.colors.primary.slice(3, 5), 16)}, ${parseInt(theme.colors.primary.slice(5, 7), 16)}, 0.05)` :"0 2px 12px rgba(0,0,0,0.06)", backdropFilter: isDark ?"blur(10px)" :"none", transition:"all 0.3s ease", display:"flex", flexDirection:"column", }} > <div style={{ display:"flex", alignItems:"center", gap:"0.75rem", marginBottom:"2rem", }} > <div style={{ background: `linear-gradient(135deg, ${theme.colors.secondary}, ${theme.colors.primary})`, borderRadius:"10px", padding:"0.625rem", display:"flex", alignItems:"center", justifyContent:"center", }} > <Wine size={22} style={{ color: theme.colors.background }} /> </div> <h2 style={{ color: theme.colors.foreground, fontSize:"1.5rem", fontWeight:"700", marginBottom: 0, letterSpacing:"-0.3px", }} > Your Notes </h2> <div style={{ marginLeft:"auto", backgroundColor: theme.colors.primary, color: theme.colors.background, borderRadius:"8px", padding:"0.5rem 0.875rem", fontWeight:"600", fontSize:"0.95rem", minWidth:"2.5rem", textAlign:"center", }} > {notes.length} </div> </div> <div style={{ display:"flex", flexDirection:"column", gap:"1rem", maxHeight:"560px", overflowY:"auto", overflowX:"hidden", paddingRight:"0.5rem", }} > {notes.length === 0 ? ( <div style={{ textAlign:"center", padding:"2rem 1rem", color: theme.text.secondary, }} > <Wine size={40} style={{ opacity: 0.3, marginBottom:"1rem" }} /> <p style={{ fontSize:"1.05rem", fontWeight:"500" }}> No tasting notes yet </p> <p style={{ fontSize:"0.9rem", marginTop:"0.5rem" }}> Start tracking by adding your first note </p> </div> ) : ( notes.map((note, index) => { const wine = getWineDetails(note.wineId); return ( <div key={note.id} style={{ backgroundColor: theme.colors.background, border: `1px solid ${theme.colors.border}`, borderRadius:"12px", padding:"1.25rem", transition:"all 0.2s ease", cursor:"pointer", }} onMouseEnter={(e) => { e.currentTarget.style.borderColor = theme.colors.primary; e.currentTarget.style.backgroundColor = isDark ? `rgba(${parseInt(theme.colors.primary.slice(1, 3), 16)}, ${parseInt(theme.colors.primary.slice(3, 5), 16)}, ${parseInt(theme.colors.primary.slice(5, 7), 16)}, 0.05)` : theme.colors.card; }} onMouseLeave={(e) => { e.currentTarget.style.borderColor = theme.colors.border; e.currentTarget.style.backgroundColor = theme.colors.background; }} > <div style={{ display:"flex", justifyContent:"space-between", alignItems:"flex-start", marginBottom:"1rem", }} > <div style={{ flex: 1 }}> <h4 style={{ color: theme.colors.foreground, fontWeight:"700", margin:"0 0 0.375rem 0", fontSize:"0.95rem", }} > {wine?.name} </h4> <p style={{ color: theme.text.secondary, fontSize:"0.8rem", margin:"0.25rem 0 0 0", }} > {wine?.vintage} •{""} {note.createdAt ? new Date(note.createdAt).toLocaleDateString() :""} </p> </div> <button onClick={() => deleteNote(note.id)} style={{ background:"none", border:"none", color: theme.colors.accent, cursor:"pointer", padding:"0.5rem", display:"flex", alignItems:"center", justifyContent:"center", borderRadius:"6px", transition:"all 0.2s", }} onMouseEnter={(e) => { e.currentTarget.style.backgroundColor = `rgba(${parseInt(theme.colors.accent.slice(1, 3), 16)}, ${parseInt(theme.colors.accent.slice(3, 5), 16)}, ${parseInt(theme.colors.accent.slice(5, 7), 16)}, 0.1)`; }} onMouseLeave={(e) => { e.currentTarget.style.backgroundColor ="transparent"; }} > <Trash2 size={18} /> </button> </div> {/* Rating Stars */} <div style={{ marginBottom:"0.875rem", display:"flex", alignItems:"center", gap:"0.5rem", }} > <div style={{ display:"flex", gap:"0.25rem" }}> {[...Array(5)].map((_, i) => ( <Star key={i} size={16} style={{ fill: i < Math.round(note.personalRating / 2) ? theme.colors.primary : theme.colors.border, color: i < Math.round(note.personalRating / 2) ? theme.colors.primary : theme.colors.border, }} /> ))} </div> <span style={{ color: theme.colors.primary, fontWeight:"600", fontSize:"0.85rem", marginLeft:"0.375rem", }} > {note.personalRating}/10 </span> </div> {/* Aroma Notes */} {note.aromaNotes.length > 0 && ( <div style={{ marginBottom:"0.75rem" }}> <p style={{ color: theme.text.secondary, fontSize:"0.75rem", fontWeight:"600", margin:"0 0 0.375rem 0", textTransform:"uppercase", letterSpacing:"0.3px", }} > Aromas </p> <div style={{ display:"flex", flexWrap:"wrap", gap:"0.375rem", }} > {note.aromaNotes.map((aroma) => ( <span key={aroma} style={{ backgroundColor: `rgba(${parseInt(theme.colors.primary.slice(1, 3), 16)}, ${parseInt(theme.colors.primary.slice(3, 5), 16)}, ${parseInt(theme.colors.primary.slice(5, 7), 16)}, 0.15)`, color: theme.colors.primary, borderRadius:"6px", padding:"0.375rem 0.75rem", fontSize:"0.75rem", fontWeight:"600", }} > {aroma} </span> ))} </div> </div> )} {/* Palate Notes */} {note.palateNotes.length > 0 && ( <div style={{ marginBottom:"0.75rem" }}> <p style={{ color: theme.text.secondary, fontSize:"0.75rem", fontWeight:"600", margin:"0 0 0.375rem 0", textTransform:"uppercase", letterSpacing:"0.3px", }} > Palate </p> <div style={{ display:"flex", flexWrap:"wrap", gap:"0.375rem", }} > {note.palateNotes.map((palate) => ( <span key={palate} style={{ backgroundColor: `rgba(${parseInt(theme.colors.secondary.slice(1, 3), 16)}, ${parseInt(theme.colors.secondary.slice(3, 5), 16)}, ${parseInt(theme.colors.secondary.slice(5, 7), 16)}, 0.15)`, color: theme.colors.secondary, borderRadius:"6px", padding:"0.375rem 0.75rem", fontSize:"0.75rem", fontWeight:"600", }} > {palate} </span> ))} </div> </div> )} {/* Finish Notes */} {note.finishNotes && ( <div> <p style={{ color: theme.text.secondary, fontSize:"0.75rem", fontWeight:"600", margin:"0 0 0.375rem 0", textTransform:"uppercase", letterSpacing:"0.3px", }} > Finish Notes </p> <p style={{ color: theme.colors.foreground, fontSize:"0.85rem", margin: 0, lineHeight:"1.4", }} > {note.finishNotes} </p> </div> )} </div> ); }) )} </div> </div> </div> </div> </div> );
      }
    }
  });
};
export default TastingNotes;
