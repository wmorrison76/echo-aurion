/**
 * SessionToastBridge
 * Listens for auth session events and surfaces toast notifications.
 * Currently a no-op placeholder – add real session-event listeners when
 * the auth backend is wired up.
 */
export function SessionToastBridge() {
  return null;
}
