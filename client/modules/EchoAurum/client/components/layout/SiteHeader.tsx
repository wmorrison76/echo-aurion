import React, { useState } from "react";
import { Link, useLocation } from "react-router-dom";
import { Menu, Moon, Sun, X, Bell, HelpCircle } from "lucide-react";
import { useTheme } from "../theme/ThemeProvider";
import { Button } from "@/components/ui/button";
import { SessionMenu, useSession } from "../../modules/auth";
const LOGO_PLACEHOLDER =
  "data:image/svg+xml,%3Csvg xmlns='http://www.w3.org/2000/svg' viewBox='0 0 40 40'%3E%3Crect width='40' height='40' rx='8' fill='%23d4af37'/%3E%3Ctext x='20' y='26' text-anchor='middle' fill='%231a1a1a' font-size='18' font-weight='bold'%3EE%3C/text%3E%3C/svg%3E"; // Marketing nav - only for public/landing pages
const marketingNav = [
  { label: "Features", href: "/features" },
  { label: "Security", href: "/security" },
  { label: "Pricing", href: "/pricing" },
  { label: "Docs", href: "/docs" },
]; // App nav - only for authenticated users
const appNav = [
  { label: "Dashboard", href: "/dashboard" },
  { label: "GL Operations", href: "/gl" },
  { label: "AP & Invoices", href: "/ap" },
  { label: "Reports", href: "/reports" },
  { label: "Admin", href: "/admin" },
];
export function SiteHeader() {
  const [open, setOpen] = useState(false);
  const { theme, toggleTheme } = useTheme();
  const { session } = useSession();
  const location = useLocation(); // Determine if user is authenticated const isAuthenticated = session !== null && session !== undefined; // Determine which nav to show const isOnAppPages = location.pathname.startsWith("/dashboard") || location.pathname.startsWith("/gl") || location.pathname.startsWith("/ap") || location.pathname.startsWith("/reports") || location.pathname.startsWith("/admin") || location.pathname.startsWith("/console") || location.pathname.startsWith("/purchasing"); const shouldShowAppNav = isAuthenticated && isOnAppPages; const shouldShowMarketingNav = !shouldShowAppNav; return ( <header className="sticky top-0 z-40 border-b border-border/40 bg-surface/80 backdrop-blur-xl"> <div className="mx-auto flex h-16 max-w-7xl items-center justify-between px-6 sm:px-10"> {/* Logo */} <Link to="/" className="flex items-center gap-3 text-left flex-shrink-0" > <div className="relative flex h-10 w-10 items-center justify-center rounded-xl border border-border/40 bg-surface shadow-lg shadow-aurum-500/30"> <img src={LOGO_PLACEHOLDER} alt="" aria-hidden="true" className="h-full w-full rounded-xl object-cover" /> <span className="sr-only">EchoAurum logo</span> </div> <div> <p className="text-sm font-semibold tracking-tight text-foreground"> EchoAurum </p> <p className="text-xs font-medium uppercase tracking-[0.28em] text-muted-foreground"> Finance Core </p> </div> </Link> {/* Desktop Navigation */} <nav className="hidden items-center gap-1 lg:flex"> {shouldShowAppNav ? ( // App Navigation - Task Focused <> {appNav.map((item) => ( <Link key={item.href} to={item.href} className={`rounded-lg px-3 py-2 text-sm font-medium transition-colors ${ location.pathname.startsWith(item.href) ?"bg-aurum-500/20 text-aurum-300" :"text-muted-foreground hover:text-foreground hover:bg-surface-variant/40" }`} > {item.label} </Link> ))} </> ) : ( // Marketing Navigation - Public <> {marketingNav.map((item) => ( <Link key={item.href} to={item.href} className="rounded-lg px-3 py-2 text-sm font-medium text-muted-foreground transition-colors hover:text-foreground hover:bg-surface-variant/40" > {item.label} </Link> ))} {isAuthenticated && ( <Link to="/dashboard" className="rounded-lg px-3 py-2 text-sm font-medium text-muted-foreground transition-colors hover:text-foreground hover:bg-surface-variant/40" > Dashboard </Link> )} </> )} </nav> {/* Right Side Actions */} <div className="flex items-center gap-2"> {/* Help (if in app) */} {shouldShowAppNav && ( <Button variant="ghost" size="icon" className="text-muted-foreground hover:text-foreground" title="Help" > <HelpCircle className="h-5 w-5" /> </Button> )} {/* Notifications (if authenticated) */} {isAuthenticated && ( <Button variant="ghost" size="icon" className="relative text-muted-foreground hover:text-foreground" title="Notifications" > <Bell className="h-5 w-5" /> <span className="absolute top-1 right-1 h-2 w-2 rounded-full bg-amber-500" /> </Button> )} {/* Theme Toggle */} <Button variant="ghost" size="icon" className="text-muted-foreground transition-colors hover:text-foreground" onClick={toggleTheme} aria-label={`Switch to ${theme ==="dark" ?"light" :"dark"} mode`} > {theme ==="dark" ? ( <Sun className="h-5 w-5" /> ) : ( <Moon className="h-5 w-5" /> )} </Button> {/* Session Menu (if authenticated) */} {isAuthenticated && <SessionMenu />} {/* Sign In Menu (if not authenticated) */} {!isAuthenticated && <SessionMenu />} {/* Mobile Menu Toggle */} <Button variant="ghost" size="icon" className="lg:hidden text-muted-foreground hover:text-foreground" onClick={() => setOpen(!open)} > {open ? <X className="h-5 w-5" /> : <Menu className="h-5 w-5" />} </Button> </div> </div> {/* Mobile Navigation */} {open && ( <div className="border-t border-border/40 bg-surface/95 px-6 py-4 lg:hidden"> <nav className="space-y-2"> {shouldShowAppNav ? ( // App Navigation - Mobile <> {appNav.map((item) => ( <Link key={item.href} to={item.href} className="block rounded-lg px-4 py-2 text-sm font-medium text-muted-foreground hover:text-foreground hover:bg-surface-variant/40 transition-colors" onClick={() => setOpen(false)} > {item.label} </Link> ))} </> ) : ( // Marketing Navigation - Mobile <> {marketingNav.map((item) => ( <Link key={item.href} to={item.href} className="block rounded-lg px-4 py-2 text-sm font-medium text-muted-foreground hover:text-foreground hover:bg-surface-variant/40 transition-colors" onClick={() => setOpen(false)} > {item.label} </Link> ))} {isAuthenticated && ( <Link to="/dashboard" className="block rounded-lg px-4 py-2 text-sm font-medium text-muted-foreground hover:text-foreground hover:bg-surface-variant/40 transition-colors" onClick={() => setOpen(false)} > Dashboard </Link> )} </> )} </nav> </div> )} </header> );
}
