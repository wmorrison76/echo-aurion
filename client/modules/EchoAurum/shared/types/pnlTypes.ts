/** * Enterprise P&L Type System * World-class hospitality accounting with complete detail and transparency * Supports multi-venue resorts with granular departmental analysis */ // ============================================================================
// VENUE TYPES & CONFIGURATION
// ============================================================================ export type VenueType = |"hotel" |"casino" |"restaurant" |"bar" |"spa" |"retail" |"entertainment" |"parking" |"golf" |"events"; export interface VenueConfig { type: VenueType; name: string; code: string; glAccountPrefix: string; // e.g.,"4100" for hotel rooms costCenters: CostCenter[]; departments: Department[]; standardLineItems: PnLLineItem[];
} export interface CostCenter { id: string; code: string; name: string; venueId: string; manager: string; budget?: number;
} export interface Department { id: string; code: string; name: string; venueId: string; type:"revenue" |"expense" |"support"; parentDepartmentId?: string; costCenters: string[];
} // ============================================================================
// P&L LINE ITEMS & STRUCTURE
// ============================================================================ export type PnLLineType = |"revenue" |"cost-of-sales" |"gross-profit" |"operating-expense" |"operating-income" |"other-income" |"other-expense" |"interest" |"tax" |"net-income" |"ebitda" |"depreciation"; export type PnLDetailLevel ="summary" |"departmental" |"cost-center" |"gl-account" |"transaction"; export interface PnLLineItem { id: string; lineNumber: number; // For consistent ordering (100, 110, 120, etc.) code: string; // GL account code name: string; description: string; type: PnLLineType; glAccounts: string[]; // Array of GL account codes backing this line parentLineId?: string; // For grouping isCalculated: boolean; // True if formula-based (e.g., Gross Profit) formula?: string; //"Revenue - COGS" etc. isExpanded: boolean; // Default expansion state in UI precedence: number; // For display ordering metrics: PnLMetric[]; // Associated KPIs variance?: { budgetAmount: number; priorYearAmount: number; variance: number; variancePercent: number; };
} export interface PnLMetric { id: string; name: string; // e.g.,"RevPAR","Labor %" formula: string; // Calculation formula format:"currency" |"percentage" |"number" |"count"; benchmark?: number; // Industry standard actual: number; prior: number; budget: number; variance: number; trendDirection:"up" |"down" |"flat";
} // ============================================================================
// P&L DATA & PERIOD INFORMATION
// ============================================================================ export type PeriodType ="monthly" |"quarterly" |"annual" |"ytd" |"trailing-twelve"; export interface Period { type: PeriodType; startDate: string; // YYYY-MM-DD endDate: string; fiscalYear: number; fiscalPeriod: number; label: string; //"January 2024","Q1 2024", etc.
} export interface PropertyIdentifier { propertyId: string; propertyName: string; propertyCode: string; chain?: string; location: { address: string; city: string; state: string; country: string; }; venueComposition: VenueType[];
} // ============================================================================
// DETAILED P&L DATA
// ============================================================================ export interface DetailedPnL { id: string; propertyId: string; propertyName: string; period: Period; generatedAt: string; lastUpdated: string; // Summary metrics totalRevenue: number; totalCogs: number; grossProfit: number; grossMarginPercent: number; totalOperatingExpense: number; operatingIncome: number; operatingMarginPercent: number; netIncome: number; ebitda: number; // Detailed line items sections: PnLSection[]; // Multi-period data for analysis comparison?: { priorPeriod: DetailedPnL; priorYear: DetailedPnL; budget: DetailedPnL; ytd: DetailedPnL; }; // Drill-down capability canDrillDown: boolean; venueBreakdown?: VenuePnL[];
} export interface PnLSection { id: string; sectionNumber: number; title: string; //"Revenue","Cost of Sales", etc. type: PnLLineType; description?: string; isExpanded: boolean; subsections?: PnLSection[]; lineItems: PnLLineItem[]; subtotal: number; subtotalPercent: number; notes?: string;
} // ============================================================================
// MULTI-VENUE & DEPARTMENTAL BREAKDOWN
// ============================================================================ export interface VenuePnL { venueId: string; venueName: string; venueType: VenueType; venueCode: string; period: Period; // Key metrics revenue: number; cogs: number; grossProfit: number; grossMarginPercent: number; operatingExpense: number; operatingIncome: number; operatingMarginPercent: number; // Department breakdown departments: DepartmentPnL[]; // Detailed data detailedPnL: DetailedPnL;
} export interface DepartmentPnL { departmentId: string; departmentName: string; departmentCode: string; costCenters: CostCenterPnL[]; // Financial data revenue: number; directCost: number; grossProfit: number; laborCost: number; operatingExpense: number; operatingIncome: number; // Metrics marginPercent: number; laborPercent: number; costPercent: number; // Detail available hasDetail: boolean;
} export interface CostCenterPnL { costCenterId: string; costCenterName: string; costCenterCode: string; revenue: number; directCost: number; grossProfit: number; laborCost: number; operatingExpense: number; operatingIncome: number; marginPercent: number; laborPercent: number; manager: string; lastReviewed: string;
} // ============================================================================
// VARIANCE & ANALYSIS
// ============================================================================ export interface VarianceAnalysis { period: Period; actual: number; budget: number; priorYear: number; budgetVariance: number; budgetVariancePercent: number; priorYearVariance: number; priorYearVariancePercent: number; varianceCategory:"favorable" |"unfavorable" |"neutral"; trend:"improving" |"declining" |"stable"; explanations?: string[]; // AI-generated or manual action?: string; // Recommended action
} export interface ConsolidatedVariance { lineItemId: string; lineItemName: string; variances: VarianceAnalysis[]; summary: { totalBudgetVariance: number; totalPriorYearVariance: number; significantVariances: VarianceAnalysis[]; };
} // ============================================================================
// GL ACCOUNT BACKING & TRANSACTION DETAIL
// ============================================================================ export interface GLBackingDetail { glAccountCode: string; glAccountName: string; glAccountDescription: string; // Account class type:"asset" |"liability" |"equity" |"revenue" |"expense"; subclass: string; // Period totals openingBalance: number; debits: number; credits: number; closingBalance: number; // Transaction detail transactions: GLTransaction[]; // Detailed breakdown byDepartment: DepartmentGLDetail[]; byCostCenter: CostCenterGLDetail[];
} export interface GLTransaction { id: string; transactionDate: string; transactionType:"journal-entry" |"invoice" |"payment" |"accrual" |"reversal"; description: string; reference: string; // Invoice #, check #, etc. debitAmount: number; creditAmount: number; department?: string; costCenter?: string; postedBy: string; postedAt: string; relatedDocuments: { documentType: string; documentId: string; documentNumber: string; }[]; status:"posted" |"pending" |"reversed" |"voided"; approvalStatus?:"approved" |"pending-approval" |"rejected";
} export interface DepartmentGLDetail { departmentId: string; departmentName: string; glAccountCode: string; amount: number; percent: number;
} export interface CostCenterGLDetail { costCenterId: string; costCenterName: string; glAccountCode: string; amount: number; percent: number;
} // ============================================================================
// COMPARISON & MULTI-DIMENSIONAL ANALYSIS
// ============================================================================ export interface PnLComparison { baselineProperty: DetailedPnL; comparisonProperties: DetailedPnL[]; comparePeriods?: { current: DetailedPnL; prior: DetailedPnL; budget: DetailedPnL; }; // Analysis results rankings: PropertyRanking[]; trends: TrendAnalysis[]; outliers: OutlierAnalysis[];
} export interface PropertyRanking { propertyId: string; propertyName: string; metric: string; //"Revenue","Margin %", etc. value: number; rank: number; percentileToBest: number; // How % behind best performer benchmarkPosition:"above" |"at" |"below";
} export interface TrendAnalysis { propertyId: string; lineItemId: string; periods: { period: Period; amount: number; variance?: number; }[]; slope:"upward" |"downward" |"stable"; seasonality: boolean; forecast?: number;
} export interface OutlierAnalysis { propertyId: string; lineItemId: string; lineItemName: string; value: number; expectedRange: { min: number; max: number }; severity:"minor" |"significant" |"critical"; likelyCauses: string[];
} // ============================================================================
// EXPORT & REPORTING
// ============================================================================ export type ExportFormat ="pdf" |"excel" |"json" |"xbrl" |"csv"; export interface PnLExportOptions { format: ExportFormat; includeVariance: boolean; includeGLDetail: boolean; includeComparison: boolean; includeDepartmentBreakdown: boolean; includeMetrics: boolean; pageOrientation:"portrait" |"landscape"; companyHeader: boolean; signatureLine: boolean; notes?: string;
} export interface ExportResult { id: string; format: ExportFormat; url: string; filename: string; generatedAt: string; expiresAt: string; fileSize: number; status:"ready" |"generating" |"failed";
} // ============================================================================
// FILTER & VIEW OPTIONS
// ============================================================================ export interface PnLFilterOptions { properties?: string[]; // Multiple property IDs venues?: VenueType[]; // Filter by venue type departments?: string[]; costCenters?: string[]; glAccountsOnly?: string[]; // Specific GL accounts to show periods?: Period[]; minAmount?: number; // Hide items below threshold hideZeros?: boolean;
} export type PnLViewMode = |"full-page" // All data visible with scroll |"tabbed" // Revenue | COGS | Operating Exp | Summary tabs |"expandable" // Collapse/expand sections |"comparison" // Side-by-side periods |"analysis" // Variance and KPI focused |"custom"; // User-defined layout export interface PnLViewState { viewMode: PnLViewMode; filterOptions: PnLFilterOptions; expandedSections: string[]; selectedLineItems: string[]; comparisonPeriods: Period[]; comparisonProperties: string[]; sortBy:"lineNumber" |"amount" |"variance" |"name"; sortDirection:"asc" |"desc";
} // ============================================================================
// DRILL-DOWN NAVIGATION
// ============================================================================ export type DrillDownLevel = |"property" |"venue" |"department" |"cost-center" |"gl-account" |"transaction"; export interface DrillDownPath { propertyId: string; level: DrillDownLevel; venueId?: string; departmentId?: string; costCenterId?: string; glAccountCode?: string; lineItemId?: string;
} export interface DrillDownContext { currentPath: DrillDownPath; breadcrumb: { level: DrillDownLevel; name: string; id: string }[]; availableNext: DrillDownLevel[]; parentLevel?: { level: DrillDownLevel; name: string; summary: { revenue?: number; expense?: number; profit?: number; marginPercent?: number; }; };
} // ============================================================================
// SUMMARY & KEY METRICS
// ============================================================================ export interface PropertySummary { propertyId: string; propertyName: string; period: Period; // Key metrics occupancyPercent?: number; // For hotels adr?: number; // Average Daily Rate revpar?: number; // Revenue per Available Room revenue: number; grossProfit: number; grossMarginPercent: number; operatingIncome: number; operatingMarginPercent: number; ebitda: number; ebitdaMarginPercent: number; // Workforce fteCounts: { total: number; byDepartment: { department: string; count: number }[]; }; // Comparison priorYearGrowth: number; budgetVariancePercent: number; // Key alerts alerts: { severity:"warning" |"critical"; message: string; lineItem: string; }[];
} export interface KPIDashboard { propertyId: string; period: Period; // Financial KPIs financialKpis: { name: string; value: number; unit: string; target?: number; trend:"up" |"down" |"flat"; }[]; // Operational KPIs operationalKpis: { name: string; value: number; unit: string; benchmark?: number; }[]; // Department KPIs departmentKpis: { department: string; kpis: { name: string; value: number; variance: number; }[]; }[];
}
