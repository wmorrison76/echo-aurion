import React from "react";
// iter154 · stubbed: postprocessing deps removed (incompatible with three@0.184).
// Restore when a compatible postprocessing version becomes available.
export interface PostFXLaneProps {
  enabled?: boolean;
  bloomIntensity?: number;
}
export function PostFXLane(_props: PostFXLaneProps) {
  return null;
}
