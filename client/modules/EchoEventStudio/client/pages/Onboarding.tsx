import React, { useState } from "react";
import { useNavigate } from "react-router-dom";
import { Button } from "@/components/ui/button";
import { Input } from "@/components/ui/input";
import {
  Card,
  CardContent,
  CardDescription,
  CardHeader,
  CardTitle,
} from "@/components/ui/card";
import { Checkbox } from "@/components/ui/checkbox";
import {
  ArrowRight,
  Check,
  Zap,
  Users,
  BarChart3,
  Lightbulb,
} from "lucide-react";
import { useAuth } from "@/hooks/useAuth";
import { toast } from "@/hooks/use-toast";
type OnboardingStep =
  | "welcome"
  | "profile"
  | "preferences"
  | "template"
  | "complete";
export default function OnboardingPage() {
  const navigate = useNavigate();
  const { user, isAuthenticated } = useAuth();
  const [step, setStep] = useState<OnboardingStep>("welcome");
  const [formData, setFormData] = useState({
    fullName: user?.user_metadata?.name || "",
    organization: "",
    role: "event-planner",
    interests: {
      diningEvents: false,
      weddings: false,
      conferences: false,
      corporate: false,
    },
    getUpdates: true,
  }); // Redirect if not authenticated React.useEffect(() => { if (!isAuthenticated) { navigate("/auth"); } }, [isAuthenticated, navigate]); const handleNext = () => { const steps: OnboardingStep[] = ["welcome","profile","preferences","template","complete"]; const currentIndex = steps.indexOf(step); if (currentIndex < steps.length - 1) { setStep(steps[currentIndex + 1]); } }; const handleComplete = () => { // Save preferences and redirect toast({ title:"Welcome to EchoEventStudio!" }); navigate("/dashboard"); }; const handleSkip = () => { navigate("/dashboard"); }; return ( <div className="min-h-screen bg-gradient-to-br from-primary/5 via-background to-primary/10"> <div className="max-w-2xl mx-auto px-4 py-12"> {/* Progress Bar */} <div className="mb-8"> <div className="flex gap-2"> {["welcome","profile","preferences","template","complete"].map((s, i) => ( <div key={s} className={`h-1 flex-1 rounded-full transition-colors ${ ["welcome","profile","preferences","template","complete"].indexOf(s) <= ["welcome","profile","preferences","template","complete"].indexOf(step) ?"bg-primary" :"bg-muted" }`} /> ))} </div> </div> {/* Welcome Step */} {step ==="welcome" && ( <Card className="border-0 shadow-lg"> <CardHeader className="text-center"> <div className="mb-4 inline-block p-4 bg-primary/10 rounded-lg"> <Zap className="h-8 w-8 text-primary" /> </div> <CardTitle className="text-3xl">Welcome to EchoEventStudio</CardTitle> <CardDescription className="text-base mt-2"> The easiest way to design professional event layouts </CardDescription> </CardHeader> <CardContent className="space-y-6"> <div className="space-y-3"> <div className="flex gap-3"> <Check className="h-5 w-5 text-green-500 flex-shrink-0 mt-0.5" /> <div> <p className="font-medium">3D Layout Designer</p> <p className="text-sm text-muted-foreground"> Create stunning event layouts with our intuitive 3D editor </p> </div> </div> <div className="flex gap-3"> <Check className="h-5 w-5 text-green-500 flex-shrink-0 mt-0.5" /> <div> <p className="font-medium">Real-time Analytics</p> <p className="text-sm text-muted-foreground"> Get instant insights on capacity, flow, and compliance </p> </div> </div> <div className="flex gap-3"> <Check className="h-5 w-5 text-green-500 flex-shrink-0 mt-0.5" /> <div> <p className="font-medium">Professional Tools</p> <p className="text-sm text-muted-foreground"> Templates, presets, and AI-powered suggestions </p> </div> </div> </div> <div className="flex gap-3"> <Button onClick={handleSkip} variant="outline" className="flex-1"> Skip Tour </Button> <Button onClick={handleNext} className="flex-1 gap-2"> Let's Get Started <ArrowRight className="h-4 w-4" /> </Button> </div> </CardContent> </Card> )} {/* Profile Step */} {step ==="profile" && ( <Card className="border-0 shadow-lg"> <CardHeader> <CardTitle>Tell Us About You</CardTitle> <CardDescription> This helps us personalize your experience </CardDescription> </CardHeader> <CardContent className="space-y-4"> <div className="space-y-2"> <label htmlFor="fullName" className="text-sm font-medium"> Full Name </label> <Input id="fullName" placeholder="John Doe" value={formData.fullName} onChange={(e) => setFormData({ ...formData, fullName: e.target.value }) } /> </div> <div className="space-y-2"> <label htmlFor="organization" className="text-sm font-medium"> Organization / Company </label> <Input id="organization" placeholder="Your event company" value={formData.organization} onChange={(e) => setFormData({ ...formData, organization: e.target.value }) } /> </div> <div className="space-y-2"> <label htmlFor="role" className="text-sm font-medium"> Your Role </label> <select id="role" value={formData.role} onChange={(e) => setFormData({ ...formData, role: e.target.value }) } className="w-full px-3 py-2 border rounded-md bg-background" > <option value="event-planner">Event Planner</option> <option value="venue-manager">Venue Manager</option> <option value="designer">Designer</option> <option value="catering">Catering Professional</option> <option value="other">Other</option> </select> </div> <div className="flex gap-3 pt-4"> <Button onClick={() => setStep("welcome")} variant="outline" className="flex-1" > Back </Button> <Button onClick={handleNext} className="flex-1 gap-2"> Next <ArrowRight className="h-4 w-4" /> </Button> </div> </CardContent> </Card> )} {/* Preferences Step */} {step ==="preferences" && ( <Card className="border-0 shadow-lg"> <CardHeader> <CardTitle>What Are Your Interests?</CardTitle> <CardDescription> Help us suggest relevant templates and features </CardDescription> </CardHeader> <CardContent className="space-y-4"> <div className="space-y-2"> {[ { key:"diningEvents", label:"Dining Events" }, { key:"weddings", label:"Weddings" }, { key:"conferences", label:"Conferences" }, { key:"corporate", label:"Corporate Events" }, ].map(({ key, label }) => ( <div key={key} className="flex items-center gap-2"> <Checkbox id={key} checked={formData.interests[key as keyof typeof formData.interests]} onChange={(checked) => setFormData({ ...formData, interests: { ...formData.interests, [key]: checked, }, }) } /> <label htmlFor={key} className="text-sm cursor-pointer flex-1"> {label} </label> </div> ))} </div> <div className="flex items-center gap-2 pt-4 border-t"> <Checkbox id="getUpdates" checked={formData.getUpdates} onChange={(checked) => setFormData({ ...formData, getUpdates: checked }) } /> <label htmlFor="getUpdates" className="text-sm cursor-pointer flex-1"> Send me tips and product updates </label> </div> <div className="flex gap-3 pt-4"> <Button onClick={() => setStep("profile")} variant="outline" className="flex-1" > Back </Button> <Button onClick={handleNext} className="flex-1 gap-2"> Next <ArrowRight className="h-4 w-4" /> </Button> </div> </CardContent> </Card> )} {/* Template Step */} {step ==="template" && ( <Card className="border-0 shadow-lg"> <CardHeader> <CardTitle>Choose a Starting Template</CardTitle> <CardDescription> Start with a pre-built template or create from scratch </CardDescription> </CardHeader> <CardContent className="space-y-4"> <div className="grid grid-cols-1 md:grid-cols-2 gap-3"> {[ { name:"Blank Canvas", description:"Start from scratch" }, { name:"Dinner Gala", description:"Formal dining event" }, { name:"Wedding Reception", description:"Wedding celebration" }, { name:"Corporate Conference", description:"Conference setup" }, ].map((template) => ( <button key={template.name} className="text-left p-3 border rounded-lg hover:border-primary hover:bg-primary/5 transition-colors" > <p className="font-medium text-sm">{template.name}</p> <p className="text-xs text-muted-foreground"> {template.description} </p> </button> ))} </div> <div className="flex gap-3 pt-4"> <Button onClick={() => setStep("preferences")} variant="outline" className="flex-1" > Back </Button> <Button onClick={handleNext} className="flex-1 gap-2"> Next <ArrowRight className="h-4 w-4" /> </Button> </div> </CardContent> </Card> )} {/* Complete Step */} {step ==="complete" && ( <Card className="border-0 shadow-lg"> <CardHeader className="text-center"> <div className="mb-4 inline-block p-4 bg-green-100 rounded-lg"> <Check className="h-8 w-8 text-green-600" /> </div> <CardTitle className="text-3xl">You're All Set!</CardTitle> <CardDescription className="text-base mt-2"> Welcome to EchoEventStudio. You're ready to create amazing event layouts. </CardDescription> </CardHeader> <CardContent className="space-y-6"> <div className="space-y-2 p-4 bg-muted/50 rounded-lg"> <p className="text-sm font-medium">Next steps:</p> <ul className="text-sm text-muted-foreground space-y-1"> <li>✓ Create your first project</li> <li>✓ Design your event layout</li> <li>✓ Share with your team</li> <li>✓ Generate reports and exports</li> </ul> </div> <Button onClick={handleComplete} className="w-full" size="lg"> Go to Dashboard </Button> <p className="text-xs text-muted-foreground text-center"> Need help? <a href="#" className="underline">View tutorials</a> or{""} <a href="#" className="underline">contact support</a> </p> </CardContent> </Card> )} {/* Footer */} <div className="mt-8 text-center text-sm text-muted-foreground"> <p> Having trouble? <a href="#" className="underline">Get help</a> </p> </div> </div> </div> );
}
