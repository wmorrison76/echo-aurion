import React from 'react' export function DiagnosticsPanel({ status, error }: { status: string; error: string | null }) { return ( <div style={{ fontFamily: 'ui-monospace, monospace', fontSize: 12, padding: 8, background: '#0b0f14', color: '#cde4ff', borderRadius: 8, marginTop: 8 }}> <div>Planner → Studio status: <strong>{status}</strong></div> {error && <div style={{ color: '#ff648a' }}>Error: {error}</div>} <ul style={{ marginTop: 8, lineHeight: 1.4 }}> <li>Check JSON schema: room.w/d and items[] required</li> <li>If using GLB, verify modelUrl is correct and accessible by Vite dev server</li> <li>No model? Bridge falls back to parametric primitives</li> <li>Camera defaults to (4,4,6); adjust after import as needed</li> </ul> </div> )
}
