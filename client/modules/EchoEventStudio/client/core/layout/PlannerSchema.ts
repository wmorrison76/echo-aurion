export type AssetKind = 'roundTable' | 'rectTable' | 'chair' | 'bar' | 'buffet' | 'serpentine' | 'stage' | 'zone' export interface PlannerItem { id: string kind: AssetKind label?: string // world units in meters x: number y: number z?: number rotation?: number // radians around Y // size for parametric primitives (fallback if model missing) w?: number d?: number h?: number r?: number // radius for rounds // model path (optional GLB override) modelUrl?: string meta?: Record<string, any>
} export interface PlannerScene { version: string room: { name: string w: number // meters d: number // meters h?: number } items: PlannerItem[] // optional: camera bookmark camera?: { x: number; y: number; z: number; tx?: number; ty?: number; tz?: number } lighting?: { ambient?: number; point?: number }
} export function isPlannerScene(o: any): o is PlannerScene { return ( !!o && typeof o === 'object' && !!o.room && typeof o.room.w === 'number' && typeof o.room.d === 'number' && Array.isArray(o.items) )
}
