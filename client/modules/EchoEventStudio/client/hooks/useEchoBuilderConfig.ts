import { useEffect, useState } from"react"; export type ScenePreset = { session: string; covers?: number; theme?: string; flowPreference?: string; glCodes?: Array<{ code: string; label: string }>; laborRate?: number; fuelCostPerUnit?: number; requireLidar?: boolean; coverageTarget?: number; maxHoleRatio?: number;
}; export function useEchoBuilderConfig(session: string): ScenePreset { const [preset, setPreset] = useState<ScenePreset>({ session }); useEffect(() => { let alive = true; const fetchPreset = async () => { try { const response = await fetch( `/api/builder/preset?session=${encodeURIComponent(session)}` ); if (response.ok) { const data = await response.json(); if (alive && data) { setPreset(data); } } } catch (_err) { // Silently handle fetch errors } }; fetchPreset(); const interval = setInterval(fetchPreset, 5000); return () => { alive = false; clearInterval(interval); }; }, [session]); return preset;
}
