/**
 * Safety Controls module
 * Panel: SafetyControlsPanel — Safe Mode, kill switch; RBAC; trace-backed.
 */

import React from "react";
import SafetyControlsPanel from "./SafetyControlsPanel";

export default function SafetyControls() {
  return <SafetyControlsPanel />;
}
