import React from "react";
export const CapacityWarnings: React.FC = () => {
  // TODO: show computed warnings (over-capacity, tight turns, etc.) return ( <div className="mt-3 text-xs text-amber-600"> {/* dynamically render warnings from your event risk engine */} {/* Example:"Warning: Setup window between two events is only 45 minutes." */} </div> );
};
