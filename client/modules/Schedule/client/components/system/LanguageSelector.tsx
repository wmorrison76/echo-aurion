/** * Language Selector Component * Allows users to switch between languages * Flags: 🇺🇸 🇫🇷 🇮🇹 🇩🇪 🇧🇷 🇪🇸 */ import React from "react";
import { locales, getFlagForLocale, Locale } from"@/i18n/config"; interface LanguageSelectorProps { onChange?: (locale: Locale) => void; currentLang?: Locale;
} export const LanguageSelector: React.FC<LanguageSelectorProps> = ({ onChange, currentLang ="en",
}) => { const [lang, setLang] = React.useState<Locale>(currentLang); const handleChange = (newLang: Locale) => { setLang(newLang); onChange?.(newLang); // Store in localStorage for persistence localStorage.setItem("luccca_lang", newLang); }; const localeKeys = Object.keys(locales) as Locale[]; return ( <div className="flex items-center space-x-1 bg-slate-800 rounded-lg p-1"> {localeKeys.map((locale) => ( <button key={locale} className={`text-xl p-2 rounded transition-all ${ lang === locale ?"bg-cyan-600 scale-110 shadow-lg shadow-cyan-500/50" :"opacity-50 hover:opacity-75 cursor-pointer" }`} onClick={() => handleChange(locale)} title={locales[locale]} aria-label={`Switch to ${locales[locale]}`} > {getFlagForLocale(locale)} </button> ))} </div> );
}; export default LanguageSelector;
