import React, { useState, useMemo } from "react";
import { useMultiOutlet } from "@/context/MultiOutletContext";
import {
  Card,
  CardContent,
  CardDescription,
  CardHeader,
  CardTitle,
} from "@/components/ui/card";
import { Tabs, TabsContent, TabsList, TabsTrigger } from "@/components/ui/tabs";
import { Badge } from "@/components/ui/badge";
import { Button } from "@/components/ui/button";
import { OutletSelector, OutletBreadcrumb } from "@/components/OutletSelector";
import {
  Building2,
  TrendingUp,
  Users,
  AlertCircle,
  MapPin,
  DollarSign,
  Package,
} from "lucide-react";
import {
  LineChart,
  Line,
  BarChart,
  Bar,
  XAxis,
  YAxis,
  CartesianGrid,
  Tooltip,
  Legend,
  ResponsiveContainer,
  PieChart,
  Pie,
  Cell,
} from "recharts";
import { AppLayout } from "@/components/AppLayout";
const COLORS = ["#3b82f6", "#10b981", "#f59e0b", "#ef4444", "#8b5cf6"];
interface OutletMetrics {
  outletId: string;
  outletName: string;
  invoiceCount: number;
  totalSpend: number;
  avgInvoiceValue: number;
  vendorCount: number;
  status: string;
}
export function MultiOutletDashboard() {
  const {
    organization,
    outlets,
    currentOutlet,
    isMultiOutlet,
    getAccessibleOutlets,
  } = useMultiOutlet();
  const [viewMode, setViewMode] = useState<"organization" | "outlet">(
    "organization",
  ); // Mock metrics data - in a real app, this would come from an API const generateMockMetrics = (outlet: (typeof outlets)[0]): OutletMetrics => { const baseInvoices = Math.floor(Math.random() * 150) + 50; const baseSpend = Math.random() * 50000 + 10000; return { outletId: outlet.id, outletName: outlet.name, invoiceCount: baseInvoices, totalSpend: baseSpend, avgInvoiceValue: baseSpend / baseInvoices, vendorCount: Math.floor(Math.random() * 15) + 5, status: outlet.status, }; }; const outletMetrics = useMemo( () => getAccessibleOutlets().map(generateMockMetrics), [getAccessibleOutlets], ); const organizationMetrics = useMemo(() => { return { totalOutlets: outlets.length, totalInvoices: outletMetrics.reduce((sum, m) => sum + m.invoiceCount, 0), totalSpend: outletMetrics.reduce((sum, m) => sum + m.totalSpend, 0), totalVendors: new Set(outletMetrics.map((m) => m.vendorCount)).size, avgSpendPerOutlet: outletMetrics.reduce((sum, m) => sum + m.totalSpend, 0) / outlets.length || 0, }; }, [outlets.length, outletMetrics]); const chartData = useMemo( () => outletMetrics.map((m) => ({ outlet: m.outletName, spend: Math.round(m.totalSpend), invoices: m.invoiceCount, avgValue: Math.round(m.avgInvoiceValue), })), [outletMetrics], ); const trendData = useMemo( () => Array.from({ length: 12 }).map((_, i) => ({ month: ["Jan","Feb","Mar","Apr","May","Jun","Jul","Aug","Sep","Oct","Nov","Dec", ][i], spend: Math.floor(Math.random() * organizationMetrics.totalSpend * 0.3) + Math.floor(organizationMetrics.totalSpend * 0.2), })), [organizationMetrics.totalSpend], ); const currentOutletMetrics = useMemo( () => currentOutlet ? outletMetrics.find((m) => m.outletId === currentOutlet.id) : null, [currentOutlet, outletMetrics], ); if (!organization) { return ( <div className="flex items-center justify-center min-h-screen"> <Card> <CardContent className="pt-6"> <p className="text-muted-foreground"> Loading organization data... </p> </CardContent> </Card> </div> ); } return ( <AppLayout> <div className="space-y-6 pb-6"> {/* Header */} <div className="flex flex-col gap-4 sm:flex-row sm:items-center sm:justify-between"> <div> <h1 className="text-3xl font-bold tracking-tight"> {organization.name} </h1> <p className="text-muted-foreground mt-2"> {isMultiOutlet() ? `${outlets.length} outlets` :"Single outlet"} </p> </div> <div className="flex gap-2"> {isMultiOutlet() && <OutletSelector />} </div> </div> {/* Breadcrumb and toggle */} <div className="flex flex-col gap-4 sm:flex-row sm:items-center sm:justify-between"> {isMultiOutlet() && <OutletBreadcrumb />} {isMultiOutlet() && ( <Tabs defaultValue="organization" onValueChange={(value) => setViewMode(value as"organization" |"outlet") } > <TabsList> <TabsTrigger value="organization">Organization View</TabsTrigger> <TabsTrigger value="outlet">Outlet Details</TabsTrigger> </TabsList> </Tabs> )} </div> {/* Organization Overview */} {viewMode ==="organization" && ( <div className="space-y-6"> {/* KPI Cards */} <div className="grid gap-4 grid-cols-1 sm:grid-cols-2 lg:grid-cols-5"> <Card> <CardHeader className="flex flex-row items-center justify-between space-y-0 pb-2"> <CardTitle className="text-sm font-medium"> Total Outlets </CardTitle> <Building2 className="h-4 w-4 text-muted-foreground" /> </CardHeader> <CardContent> <div className="text-2xl font-bold"> {organizationMetrics.totalOutlets} </div> <p className="text-xs text-muted-foreground"> {outlets.filter((o) => o.status ==="active").length} active </p> </CardContent> </Card> <Card> <CardHeader className="flex flex-row items-center justify-between space-y-0 pb-2"> <CardTitle className="text-sm font-medium"> Total Invoices </CardTitle> <TrendingUp className="h-4 w-4 text-muted-foreground" /> </CardHeader> <CardContent> <div className="text-2xl font-bold"> {organizationMetrics.totalInvoices} </div> <p className="text-xs text-muted-foreground"> Across all outlets </p> </CardContent> </Card> <Card> <CardHeader className="flex flex-row items-center justify-between space-y-0 pb-2"> <CardTitle className="text-sm font-medium"> Total Spend </CardTitle> <DollarSign className="h-4 w-4 text-muted-foreground" /> </CardHeader> <CardContent> <div className="text-2xl font-bold"> ${(organizationMetrics.totalSpend / 1000).toFixed(1)}K </div> <p className="text-xs text-muted-foreground"> ${(organizationMetrics.avgSpendPerOutlet / 1000).toFixed(1)}K avg/outlet </p> </CardContent> </Card> <Card> <CardHeader className="flex flex-row items-center justify-between space-y-0 pb-2"> <CardTitle className="text-sm font-medium"> Unique Vendors </CardTitle> <Users className="h-4 w-4 text-muted-foreground" /> </CardHeader> <CardContent> <div className="text-2xl font-bold"> {organizationMetrics.totalVendors} </div> <p className="text-xs text-muted-foreground"> Active suppliers </p> </CardContent> </Card> <Card> <CardHeader className="flex flex-row items-center justify-between space-y-0 pb-2"> <CardTitle className="text-sm font-medium">Tier</CardTitle> <Badge variant="secondary" className="text-xs"> {organization.tier.toUpperCase()} </Badge> </CardHeader> <CardContent> <div className="text-xs text-muted-foreground"> {organization.features.length} features enabled </div> </CardContent> </Card> </div> {/* Charts */} <div className="grid gap-4 grid-cols-1 lg:grid-cols-2"> {/* Spend Comparison */} <Card> <CardHeader> <CardTitle className="text-lg">Spend by Outlet</CardTitle> <CardDescription>Comparison across all outlets</CardDescription> </CardHeader> <CardContent> <ResponsiveContainer width="100%" height={300}> <BarChart data={chartData}> <CartesianGrid strokeDasharray="3 3" /> <XAxis dataKey="outlet" /> <YAxis /> <Tooltip formatter={(value) => `$${(value as number).toLocaleString()}` } /> <Bar dataKey="spend" fill="#3b82f6" /> </BarChart> </ResponsiveContainer> </CardContent> </Card> {/* Trend */} <Card> <CardHeader> <CardTitle className="text-lg">Monthly Spend Trend</CardTitle> <CardDescription>Organization-wide spending</CardDescription> </CardHeader> <CardContent> <ResponsiveContainer width="100%" height={300}> <LineChart data={trendData}> <CartesianGrid strokeDasharray="3 3" /> <XAxis dataKey="month" /> <YAxis /> <Tooltip formatter={(value) => `$${(value as number).toLocaleString()}` } /> <Line type="monotone" dataKey="spend" stroke="#3b82f6" strokeWidth={2} /> </LineChart> </ResponsiveContainer> </CardContent> </Card> </div> {/* Outlet Details Table */} <Card> <CardHeader> <CardTitle className="text-lg">Outlet Performance</CardTitle> <CardDescription> Detailed metrics for each outlet </CardDescription> </CardHeader> <CardContent> <div className="overflow-x-auto"> <table className="w-full text-sm"> <thead className="border-b"> <tr> <th className="text-left py-3 px-4 font-medium"> Outlet Name </th> <th className="text-left py-3 px-4 font-medium">Type</th> <th className="text-right py-3 px-4 font-medium"> Invoices </th> <th className="text-right py-3 px-4 font-medium"> Total Spend </th> <th className="text-right py-3 px-4 font-medium"> Avg Invoice </th> <th className="text-center py-3 px-4 font-medium"> Status </th> </tr> </thead> <tbody> {outletMetrics.map((metric) => ( <tr key={metric.outletId} className="border-b hover:bg-muted/50" > <td className="py-3 px-4">{metric.outletName}</td> <td className="py-3 px-4"> <Badge variant="secondary" className="text-xs"> {outlets.find((o) => o.id === metric.outletId) ?.type ||"N/A"} </Badge> </td> <td className="py-3 px-4 text-right"> {metric.invoiceCount} </td> <td className="py-3 px-4 text-right"> ${metric.totalSpend.toFixed(2)} </td> <td className="py-3 px-4 text-right"> ${metric.avgInvoiceValue.toFixed(2)} </td> <td className="py-3 px-4 text-center"> <Badge variant={ metric.status ==="active" ?"default" :"secondary" } > {metric.status} </Badge> </td> </tr> ))} </tbody> </table> </div> </CardContent> </Card> </div> )} {/* Outlet-Specific View */} {viewMode ==="outlet" && currentOutlet && currentOutletMetrics && ( <div className="space-y-6"> <Card> <CardHeader> <div className="flex items-start justify-between"> <div> <CardTitle className="text-2xl"> {currentOutlet.name} </CardTitle> <CardDescription className="flex items-center gap-2 mt-2"> <MapPin className="w-4 h-4" /> {currentOutlet.location.city},{""} {currentOutlet.location.state} </CardDescription> </div> <Badge variant={ currentOutlet.status ==="active" ?"default" :"secondary" } > {currentOutlet.status} </Badge> </div> </CardHeader> <CardContent> <div className="grid gap-4 grid-cols-2 sm:grid-cols-4"> <div> <p className="text-sm text-muted-foreground">Type</p> <p className="text-lg font-semibold capitalize"> {currentOutlet.type} </p> </div> <div> <p className="text-sm text-muted-foreground">Seats</p> <p className="text-lg font-semibold">{currentOutlet.seats}</p> </div> <div> <p className="text-sm text-muted-foreground">Daily Covers</p> <p className="text-lg font-semibold"> {currentOutlet.covers} </p> </div> <div> <p className="text-sm text-muted-foreground">Created</p> <p className="text-lg font-semibold"> {new Date(currentOutlet.createdAt).toLocaleDateString()} </p> </div> </div> </CardContent> </Card> <div className="grid gap-4 grid-cols-1 sm:grid-cols-3"> <Card> <CardHeader className="flex flex-row items-center justify-between space-y-0 pb-2"> <CardTitle className="text-sm font-medium">Invoices</CardTitle> <Package className="h-4 w-4 text-muted-foreground" /> </CardHeader> <CardContent> <div className="text-2xl font-bold"> {currentOutletMetrics.invoiceCount} </div> </CardContent> </Card> <Card> <CardHeader className="flex flex-row items-center justify-between space-y-0 pb-2"> <CardTitle className="text-sm font-medium"> Total Spend </CardTitle> <DollarSign className="h-4 w-4 text-muted-foreground" /> </CardHeader> <CardContent> <div className="text-2xl font-bold"> ${(currentOutletMetrics.totalSpend / 1000).toFixed(1)}K </div> </CardContent> </Card> <Card> <CardHeader className="flex flex-row items-center justify-between space-y-0 pb-2"> <CardTitle className="text-sm font-medium"> Avg Invoice </CardTitle> <TrendingUp className="h-4 w-4 text-muted-foreground" /> </CardHeader> <CardContent> <div className="text-2xl font-bold"> ${currentOutletMetrics.avgInvoiceValue.toFixed(2)} </div> </CardContent> </Card> </div> <Card> <CardHeader> <CardTitle className="text-lg">Outlet Details</CardTitle> </CardHeader> <CardContent> <dl className="space-y-4"> <div className="flex justify-between"> <dt className="text-muted-foreground">Address</dt> <dd className="font-medium"> {currentOutlet.location.address} </dd> </div> <div className="flex justify-between"> <dt className="text-muted-foreground">City</dt> <dd className="font-medium">{currentOutlet.location.city}</dd> </div> <div className="flex justify-between"> <dt className="text-muted-foreground">ZIP Code</dt> <dd className="font-medium">{currentOutlet.location.zip}</dd> </div> <div className="flex justify-between"> <dt className="text-muted-foreground">Coordinates</dt> <dd className="font-medium"> {currentOutlet.location.lat.toFixed(4)}, {currentOutlet.location.lng.toFixed(4)} </dd> </div> </dl> </CardContent> </Card> </div> )} </div> </AppLayout> );
}
export default MultiOutletDashboard;
