/**
 * Cognitive Replay module
 * Panel: CognitiveReplayPanel — trace chain replay, narrated explanation, citations.
 */

import React from "react";
import CognitiveReplayPanel from "./CognitiveReplayPanel";

export default function CognitiveReplay() {
  return <CognitiveReplayPanel />;
}
