import React,{useEffect}from"react";import{useVisualSync}from"../hooks/useVisualSync.js";export default function LiveControlPanel({bpm=120,fps=60,onBeat}){const{phase,tick}=useVisualSync({bpm,fps});useEffect(()=>{if(onBeat)onBeat(tick)},[tick]);return(<div className="live-control p-3 rounded-2xl border border-white/10"><div className="text-xs opacity-70">VisualSync LiveControl</div><div className="text-sm mt-1">BPM {bpm} • Beat #{tick}</div><div className="h-2 mt-2 rounded bg-white/10 overflow-hidden"><div className="h-full bg-white/70" style={{width:`${(phase*100).toFixed(1)}%`}}/></div></div>)}