import StickyNotesContent from "@/components/modules/StickyNotesContent";

export default function StickyNotesPage() {
  return (
    <main className="min-h-screen bg-background">
      <section className="container px-4 py-8">
        <StickyNotesContent />
      </section>
    </main>
  );
}
