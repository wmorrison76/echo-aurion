import React from 'react';

/**
 * Schedule context - placeholder for schedule-related state.
 * Wire up to your scheduling stores (useSchedulingIntegration, useScheduleStore, etc.) as needed.
 */
export function ScheduleProvider({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
