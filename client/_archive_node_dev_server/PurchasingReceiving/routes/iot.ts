/** * IoT Server Routes * Handles all IoT device, sensor reading, and alert endpoints */ import { Router, Request, Response } from "express";
import { z } from "zod";
import { getSupabaseServiceClient } from "../lib/supabase";
import { logger, sanitizeError } from "../lib/logger";
import { validateRequest, UUIDSchema, IoTDeviceSchema, IoTSensorReadingSchema,
} from "../middleware/validation";
import { paginationMiddleware, applyPaginationRange, buildPaginatedResponse, countTotal,
} from "../middleware/pagination";
import { IoTDevice, SensorReading, IoTAlert, RFIDTag } from "@shared/types/iot"; const router = Router(); // Lazy initialization helper to defer Supabase client creation
const getClient = () => getSupabaseServiceClient(); // ============================================================================
// IOT DEVICES
// ============================================================================ // Get devices (paginated)
router.get("/iot-devices", paginationMiddleware, async (req: Request, res: Response) => { try { const supabase = getClient(); const pagination = res.locals.pagination; const { organization_id, outlet_id, status, device_type } = req.query; let query = supabase .from("iot_devices") .select("*") .eq("organization_id", organization_id as string); if (outlet_id) query = query.eq("outlet_id", outlet_id); if (status) query = query.eq("status", status); if (device_type) query = query.eq("type", device_type); const { data, error } = await applyPaginationRange( query.order("created_at", { ascending: false }), pagination, ); if (error) throw error; // Get total count let countQuery = supabase .from("iot_devices") .select("id", { count:"exact", head: true }) .eq("organization_id", organization_id as string); if (outlet_id) countQuery = countQuery.eq("outlet_id", outlet_id); if (status) countQuery = countQuery.eq("status", status); if (device_type) countQuery = countQuery.eq("type", device_type); const total = await countTotal(countQuery); res.json( buildPaginatedResponse( data || [], total, pagination.limit, pagination.offset, ), ); } catch (error) { res.status(500).json({ error: (error as Error).message }); } },
); // Get single device
router.get("/iot-devices/:deviceId", async (req: Request, res: Response) => { try { const supabase = getClient(); const { data, error } = await supabase .from("iot_devices") .select("*") .eq("id", req.params.deviceId) .single(); if (error) throw error; res.json(data); } catch (error) { res.status(500).json({ error: (error as Error).message }); }
}); // Create device
router.post("/iot-devices", validateRequest({ body: IoTDeviceSchema }), async (req: Request, res: Response) => { try { const supabase = getClient(); const device: Omit<IoTDevice,"id" |"created_at" |"updated_at"> = req.body; const { data, error } = await supabase .from("iot_devices") .insert([device]) .select() .single(); if (error) { logger.error("Failed to create IoT device", { error: sanitizeError(error), }); throw error; } logger.info("IoT device created", { deviceId: data?.id }); res.status(201).json(data); } catch (error) { logger.error("POST /iot-devices failed", { error: sanitizeError(error) }); res.status(500).json({ error: (error as Error).message }); } },
); // Update device
router.patch("/iot-devices/:deviceId", validateRequest({ params: UUIDSchema.transform((obj) => ({ deviceId: obj })), body: IoTDeviceSchema.partial(), }), async (req: Request, res: Response) => { try { const supabase = getClient(); const { data, error } = await supabase .from("iot_devices") .update({ ...req.body, updated_at: new Date().toISOString(), }) .eq("id", req.params.deviceId) .select() .single(); if (error) { logger.error("Failed to update IoT device", { deviceId: req.params.deviceId, error: sanitizeError(error), }); throw error; } logger.info("IoT device updated", { deviceId: req.params.deviceId }); res.json(data); } catch (error) { logger.error("PATCH /iot-devices/:deviceId failed", { error: sanitizeError(error), }); res.status(500).json({ error: (error as Error).message }); } },
); // Delete device
router.delete("/iot-devices/:deviceId", validateRequest({ params: z.object({ deviceId: UUIDSchema }) }), async (req: Request, res: Response) => { try { const supabase = getClient(); const { error } = await supabase .from("iot_devices") .delete() .eq("id", req.params.deviceId); if (error) { logger.error("Failed to delete IoT device", { deviceId: req.params.deviceId, error: sanitizeError(error), }); throw error; } logger.info("IoT device deleted", { deviceId: req.params.deviceId }); res.status(204).send(); } catch (error) { logger.error("DELETE /iot-devices/:deviceId failed", { error: sanitizeError(error), }); res.status(500).json({ error: (error as Error).message }); } },
); // Get device status view
router.get("/iot-devices/:deviceId/status", async (req: Request, res: Response) => { try { const supabase = getClient(); const { data, error } = await supabase .from("device_status_view") .select("*") .eq("id", req.params.deviceId) .single(); if (error) throw error; res.json(data); } catch (error) { res.status(500).json({ error: (error as Error).message }); } },
); // Get organization device status
router.get("/organizations/:organizationId/device-status", paginationMiddleware, async (req: Request, res: Response) => { try { const supabase = getClient(); const pagination = res.locals.pagination; const { data, error } = await applyPaginationRange( supabase .from("device_status_view") .select("*") .eq("organization_id", req.params.organizationId) .order("created_at", { ascending: false }), pagination, ); if (error) throw error; // Get total count const total = await countTotal( supabase .from("device_status_view") .select("id", { count:"exact", head: true }) .eq("organization_id", req.params.organizationId), ); res.json(buildPaginatedResponse(data || [], total, pagination.limit, pagination.offset)); } catch (error) { res.status(500).json({ error: (error as Error).message }); } },
); // ============================================================================
// SENSOR READINGS
// ============================================================================ // Get sensor readings (paginated)
router.get("/sensor-readings", paginationMiddleware, async (req: Request, res: Response) => { try { const supabase = getClient(); const pagination = res.locals.pagination; const { organization_id, outlet_id, device_id, reading_type, } = req.query; let query = supabase .from("sensor_readings") .select("*") .eq("organization_id", organization_id as string); if (outlet_id) query = query.eq("outlet_id", outlet_id); if (device_id) query = query.eq("device_id", device_id); if (reading_type) query = query.eq("reading_type", reading_type); const { data, error } = await applyPaginationRange( query.order("read_at", { ascending: false }), pagination ); if (error) throw error; // Get total count let countQuery = supabase .from("sensor_readings") .select("id", { count:"exact", head: true }) .eq("organization_id", organization_id as string); if (outlet_id) countQuery = countQuery.eq("outlet_id", outlet_id); if (device_id) countQuery = countQuery.eq("device_id", device_id); if (reading_type) countQuery = countQuery.eq("reading_type", reading_type); const total = await countTotal(countQuery); res.json(buildPaginatedResponse(data || [], total, pagination.limit, pagination.offset)); } catch (error) { res.status(500).json({ error: (error as Error).message }); }
}); // Create sensor reading
router.post("/sensor-readings", validateRequest({ body: IoTSensorReadingSchema }), async (req: Request, res: Response) => { try { const supabase = getClient(); const reading: Omit<SensorReading,"id" |"read_at"> = req.body; const { data, error } = await supabase .from("sensor_readings") .insert([{ ...reading, read_at: new Date().toISOString() }]) .select() .single(); if (error) { logger.error("Failed to create sensor reading", { error: sanitizeError(error), }); throw error; } logger.info("Sensor reading created", { sensorId: data?.id, deviceId: reading.device_id, }); res.status(201).json(data); } catch (error) { logger.error("POST /sensor-readings failed", { error: sanitizeError(error), }); res.status(500).json({ error: (error as Error).message }); } },
); // Bulk create sensor readings
router.post("/sensor-readings/bulk", validateRequest({ body: z.object({ readings: z.array(IoTSensorReadingSchema) }), }), async (req: Request, res: Response) => { try { const supabase = getClient(); const { readings } = req.body; const now = new Date().toISOString(); const { data, error } = await supabase .from("sensor_readings") .insert(readings.map((r: any) => ({ ...r, read_at: now }))) .select(); if (error) { logger.error("Failed to bulk create sensor readings", { count: readings.length, error: sanitizeError(error), }); throw error; } logger.info("Sensor readings bulk created", { created: data?.length || 0, }); res.status(201).json({ created: data?.length || 0, failed: 0 }); } catch (error) { logger.error("POST /sensor-readings/bulk failed", { error: sanitizeError(error), }); res.status(500).json({ error: (error as Error).message }); } },
); // Get sensor readings daily aggregates (paginated)
router.get("/sensor-readings-daily", paginationMiddleware, async (req: Request, res: Response) => { try { const supabase = getClient(); const pagination = res.locals.pagination; const { organization_id, outlet_id, device_id, reading_type } = req.query; let query = supabase .from("sensor_readings_daily") .select("*") .eq("organization_id", organization_id as string); if (outlet_id) query = query.eq("outlet_id", outlet_id); if (device_id) query = query.eq("device_id", device_id); if (reading_type) query = query.eq("reading_type", reading_type); const { data, error } = await applyPaginationRange( query.order("day_start", { ascending: false }), pagination ); if (error) throw error; // Get total count let countQuery = supabase .from("sensor_readings_daily") .select("id", { count:"exact", head: true }) .eq("organization_id", organization_id as string); if (outlet_id) countQuery = countQuery.eq("outlet_id", outlet_id); if (device_id) countQuery = countQuery.eq("device_id", device_id); if (reading_type) countQuery = countQuery.eq("reading_type", reading_type); const total = await countTotal(countQuery); res.json(buildPaginatedResponse(data || [], total, pagination.limit, pagination.offset)); } catch (error) { res.status(500).json({ error: (error as Error).message }); }
}); // ============================================================================
// SPOILAGE PREDICTIONS
// ============================================================================ // Get spoilage predictions (paginated)
router.get("/spoilage-predictions", paginationMiddleware, async (req: Request, res: Response) => { try { const supabase = getClient(); const pagination = res.locals.pagination; const { organization_id, outlet_id, risk_level } = req.query; let query = supabase .from("spoilage_predictions") .select("*") .eq("organization_id", organization_id as string); if (outlet_id) query = query.eq("outlet_id", outlet_id); if (risk_level) query = query.eq("risk_level", risk_level); const { data, error } = await applyPaginationRange( query.order("created_at", { ascending: false }), pagination ); if (error) throw error; // Get total count let countQuery = supabase .from("spoilage_predictions") .select("id", { count:"exact", head: true }) .eq("organization_id", organization_id as string); if (outlet_id) countQuery = countQuery.eq("outlet_id", outlet_id); if (risk_level) countQuery = countQuery.eq("risk_level", risk_level); const total = await countTotal(countQuery); res.json(buildPaginatedResponse(data || [], total, pagination.limit, pagination.offset)); } catch (error) { res.status(500).json({ error: (error as Error).message }); }
}); // Create spoilage prediction
router.post("/spoilage-predictions", async (req: Request, res: Response) => { try { const supabase = getClient(); const { data, error } = await supabase .from("spoilage_predictions") .insert([req.body]) .select() .single(); if (error) throw error; res.status(201).json(data); } catch (error) { res.status(500).json({ error: (error as Error).message }); }
}); // ============================================================================
// IOT ALERTS
// ============================================================================ // Get alerts (paginated)
router.get("/iot-alerts", paginationMiddleware, async (req: Request, res: Response) => { try { const supabase = getClient(); const pagination = res.locals.pagination; const { organization_id, outlet_id, status, severity, } = req.query; let query = supabase .from("iot_alerts") .select("*") .eq("organization_id", organization_id as string); if (outlet_id) query = query.eq("outlet_id", outlet_id); if (status) query = query.eq("status", status); if (severity) query = query.eq("severity", severity); const { data, error } = await applyPaginationRange( query.order("created_at", { ascending: false }), pagination ); if (error) throw error; // Get total count let countQuery = supabase .from("iot_alerts") .select("id", { count:"exact", head: true }) .eq("organization_id", organization_id as string); if (outlet_id) countQuery = countQuery.eq("outlet_id", outlet_id); if (status) countQuery = countQuery.eq("status", status); if (severity) countQuery = countQuery.eq("severity", severity); const total = await countTotal(countQuery); res.json(buildPaginatedResponse(data || [], total, pagination.limit, pagination.offset)); } catch (error) { res.status(500).json({ error: (error as Error).message }); }
}); // Get single alert
router.get("/iot-alerts/:alertId", async (req: Request, res: Response) => { try { const supabase = getClient(); const { data, error } = await supabase .from("iot_alerts") .select("*") .eq("id", req.params.alertId) .single(); if (error) throw error; res.json(data); } catch (error) { res.status(500).json({ error: (error as Error).message }); }
}); // Create alert
router.post("/iot-alerts", async (req: Request, res: Response) => { try { const supabase = getClient(); const alert: Omit<IoTAlert,"id" |"created_at" |"updated_at"> = req.body; const { data, error } = await supabase .from("iot_alerts") .insert([alert]) .select() .single(); if (error) throw error; // Trigger notifications if needed if (alert.alert_rule_id) { // TODO: Implement notification dispatch based on rule settings } res.status(201).json(data); } catch (error) { res.status(500).json({ error: (error as Error).message }); }
}); // Acknowledge alert
router.post("/iot-alerts/:alertId/acknowledge", async (req: Request, res: Response) => { try { const supabase = getClient(); const { acknowledged_by } = req.body; const { data, error } = await supabase .from("iot_alerts") .update({ status:"acknowledged", acknowledged_by, acknowledged_at: new Date().toISOString(), updated_at: new Date().toISOString(), }) .eq("id", req.params.alertId) .select() .single(); if (error) throw error; res.json(data); } catch (error) { res.status(500).json({ error: (error as Error).message }); } },
); // Resolve alert
router.post("/iot-alerts/:alertId/resolve", async (req: Request, res: Response) => { try { const supabase = getClient(); const { resolved_by, resolution_notes } = req.body; const { data, error } = await supabase .from("iot_alerts") .update({ status:"resolved", resolved_by, resolved_at: new Date().toISOString(), resolution_notes, updated_at: new Date().toISOString(), }) .eq("id", req.params.alertId) .select() .single(); if (error) throw error; res.json(data); } catch (error) { res.status(500).json({ error: (error as Error).message }); } },
); // ============================================================================
// ALERT RULES
// ============================================================================ // Get alert rules (paginated)
router.get("/iot-alert-rules", paginationMiddleware, async (req: Request, res: Response) => { try { const supabase = getClient(); const pagination = res.locals.pagination; const { organization_id, outlet_id } = req.query; let query = supabase .from("iot_alert_rules") .select("*") .eq("organization_id", organization_id as string); if (outlet_id) query = query.eq("outlet_id", outlet_id); const { data, error } = await applyPaginationRange( query.order("created_at", { ascending: false }), pagination ); if (error) throw error; // Get total count let countQuery = supabase .from("iot_alert_rules") .select("id", { count:"exact", head: true }) .eq("organization_id", organization_id as string); if (outlet_id) countQuery = countQuery.eq("outlet_id", outlet_id); const total = await countTotal(countQuery); res.json(buildPaginatedResponse(data || [], total, pagination.limit, pagination.offset)); } catch (error) { res.status(500).json({ error: (error as Error).message }); }
}); // Create alert rule
router.post("/iot-alert-rules", async (req: Request, res: Response) => { try { const supabase = getClient(); const { data, error } = await supabase .from("iot_alert_rules") .insert([{ ...req.body, created_by:"system" }]) .select() .single(); if (error) throw error; res.status(201).json(data); } catch (error) { res.status(500).json({ error: (error as Error).message }); }
}); // ============================================================================
// RFID TAGS
// ============================================================================ // Get RFID tags (paginated)
router.get("/rfid-tags", paginationMiddleware, async (req: Request, res: Response) => { try { const supabase = getClient(); const pagination = res.locals.pagination; const { organization_id, status } = req.query; let query = supabase .from("rfid_tags") .select("*") .eq("organization_id", organization_id as string); if (status) query = query.eq("status", status); const { data, error } = await applyPaginationRange( query.order("created_at", { ascending: false }), pagination ); if (error) throw error; // Get total count let countQuery = supabase .from("rfid_tags") .select("id", { count:"exact", head: true }) .eq("organization_id", organization_id as string); if (status) countQuery = countQuery.eq("status", status); const total = await countTotal(countQuery); res.json(buildPaginatedResponse(data || [], total, pagination.limit, pagination.offset)); } catch (error) { res.status(500).json({ error: (error as Error).message }); }
}); // Create RFID tag
router.post("/rfid-tags", async (req: Request, res: Response) => { try { const supabase = getClient(); const tag: Omit<RFIDTag,"id" |"created_at" |"updated_at"> = req.body; const { data, error } = await supabase .from("rfid_tags") .insert([tag]) .select() .single(); if (error) throw error; res.status(201).json(data); } catch (error) { res.status(500).json({ error: (error as Error).message }); }
}); // Bulk create RFID tags
router.post("/rfid-tags/bulk", async (req: Request, res: Response) => { try { const supabase = getClient(); const { tags } = req.body; const { data, error } = await supabase .from("rfid_tags") .insert(tags) .select(); if (error) throw error; res.status(201).json({ created: data?.length || 0, failed: 0 }); } catch (error) { res.status(500).json({ error: (error as Error).message }); }
}); // Record tag location
router.post("/tag-locations", async (req: Request, res: Response) => { try { const supabase = getClient(); const { data, error } = await supabase .from("tag_locations") .insert([ { ...req.body, read_at: new Date().toISOString(), is_latest: true }, ]) .select() .single(); if (error) throw error; res.status(201).json(data); } catch (error) { res.status(500).json({ error: (error as Error).message }); }
}); // Bulk record tag locations
router.post("/tag-locations/bulk", async (req: Request, res: Response) => { try { const supabase = getClient(); const { locations } = req.body; const now = new Date().toISOString(); const { data, error } = await supabase .from("tag_locations") .insert( locations.map((l: any) => ({ ...l, read_at: now, is_latest: true })), ) .select(); if (error) throw error; res.status(201).json({ recorded: data?.length || 0, failed: 0 }); } catch (error) { res.status(500).json({ error: (error as Error).message }); }
}); export default router;
