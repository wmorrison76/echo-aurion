/** * Accounting & Accounts Payable Routes * Handles all AP operations, GL integration, P&L reporting with Supabase */ import { Router, Request, Response } from "express";
import { supabase, handleSupabaseError } from "../lib/supabase";
import { processPayment } from "../lib/stripe";
import { validateRequest, PaymentSchema, PaymentFilterSchema, GLEntrySchema, UUIDSchema,
} from "../middleware/validation";
import { paginationMiddleware, applyPaginationRange, buildPaginatedResponse, parsePaginationParams, countTotal,
} from "../middleware/pagination";
import { logger, logAuditEvent, logDatabaseOperation, sanitizeError,
} from "../lib/logger";
import type { InvoicePayment, PaymentSchedule, GLEntry,
} from "@/shared/types/accounting"; const router = Router(); /** * PAYMENT MANAGEMENT */ // GET: Retrieve all payments for organization (paginated)
router.get("/payments", paginationMiddleware, validateRequest({ query: PaymentFilterSchema }), async (req: Request, res: Response) => { try { const startTime = Date.now(); const pagination = res.locals.pagination; const { organization_id, vendor_id, status, outlet_id, start_date, end_date, } = req.query as any; // Build filtered query for data fetch let query = supabase.from("invoice_payments").select("*"); if (organization_id) { query = query.eq("organization_id", organization_id); } if (outlet_id) { query = query.eq("outlet_id", outlet_id); } if (vendor_id) { query = query.eq("vendor_id", vendor_id); } if (status) { query = query.eq("status", status); } if (start_date) { query = query.gte("due_date", start_date); } if (end_date) { query = query.lte("due_date", end_date); } // Apply pagination and order const { data, error } = await applyPaginationRange( query.order("due_date", { ascending: false }), pagination, ); if (error) throw error; // Get total count (parallel query for better performance) const countQuery = supabase .from("invoice_payments") .select("id", { count:"exact", head: true }); let countFiltered = countQuery; if (organization_id) countFiltered = countFiltered.eq("organization_id", organization_id); if (outlet_id) countFiltered = countFiltered.eq("outlet_id", outlet_id); if (vendor_id) countFiltered = countFiltered.eq("vendor_id", vendor_id); if (status) countFiltered = countFiltered.eq("status", status); if (start_date) countFiltered = countFiltered.gte("due_date", start_date); if (end_date) countFiltered = countFiltered.lte("due_date", end_date); const total = await countTotal(countFiltered); const duration = Date.now() - startTime; logDatabaseOperation("query","invoice_payments","success", duration, undefined, data?.length || 0, ); res.json( buildPaginatedResponse( data || [], total, pagination.limit, pagination.offset, ), ); } catch (error) { logger.error("GET /payments error", sanitizeError(error)); res.status(500).json({ error: handleSupabaseError(error) }); } },
); // POST: Create a new payment
router.post("/payments", validateRequest({ body: PaymentSchema }), async (req: Request, res: Response) => { try { const startTime = Date.now(); const { data, error } = await supabase .from("invoice_payments") .insert([req.body]) .select(); if (error) throw error; const duration = Date.now() - startTime; logDatabaseOperation("insert","invoice_payments","success", duration, undefined, 1, ); logAuditEvent("create_payment","invoice_payments", (req.query.user_id as string) ||"system", { amount: req.body.amount, vendor_id: req.body.vendor_id, },"success", ); res.status(201).json(data?.[0] || req.body); } catch (error) { logger.error("POST /payments error", sanitizeError(error)); res.status(500).json({ error: handleSupabaseError(error) }); } },
); // GET: Get a specific payment
router.get("/payments/:id", async (req: Request, res: Response) => { try { const { data, error } = await supabase .from("invoice_payments") .select("*") .eq("id", req.params.id) .single(); if (error) { if (error.code ==="PGRST116") { return res.status(404).json({ error:"Payment not found" }); } throw error; } res.json(data); } catch (error) { logger.error("GET /payments/:id error:", error); res.status(500).json({ error: handleSupabaseError(error) }); }
}); // PATCH: Update a payment
router.patch("/payments/:id", async (req: Request, res: Response) => { try { const { data, error } = await supabase .from("invoice_payments") .update({ ...req.body, updated_at: new Date().toISOString(), }) .eq("id", req.params.id) .select() .single(); if (error) { if (error.code ==="PGRST116") { return res.status(404).json({ error:"Payment not found" }); } throw error; } res.json(data); } catch (error) { logger.error("PATCH /payments/:id error:", error); res.status(500).json({ error: handleSupabaseError(error) }); }
}); // DELETE: Delete a payment
router.delete("/payments/:id", async (req: Request, res: Response) => { try { const { data, error } = await supabase .from("invoice_payments") .delete() .eq("id", req.params.id) .select() .single(); if (error) { if (error.code ==="PGRST116") { return res.status(404).json({ error:"Payment not found" }); } throw error; } res.json(data); } catch (error) { logger.error("DELETE /payments/:id error:", error); res.status(500).json({ error: handleSupabaseError(error) }); }
}); // POST: Suggest payment run optimization
router.post("/payments/suggest-run", async (req: Request, res: Response) => { try { const { organization_id, outlet_id, include_early_pay_discounts = true, } = req.body; const now = new Date(); // Get urgent payments (overdue) const { data: urgentPayments, error: error1 } = await supabase .from("invoice_payments") .select("*") .eq("organization_id", organization_id) .eq("status","pending") .lte("due_date", now.toISOString().split("T")[0]); if (error1) throw error1; // Get upcoming payments (within 7 days) const sevenDaysFromNow = new Date(now.getTime() + 7 * 24 * 60 * 60 * 1000) .toISOString() .split("T")[0]; const { data: upcomingPayments, error: error2 } = await supabase .from("invoice_payments") .select("*") .eq("organization_id", organization_id) .eq("status","pending") .gt("due_date", now.toISOString().split("T")[0]) .lte("due_date", sevenDaysFromNow); if (error2) throw error2; // Get early pay opportunities let earlyPayOpportunities: any[] = []; if (include_early_pay_discounts) { const { data, error } = await supabase .from("invoice_payments") .select("*") .eq("organization_id", organization_id) .eq("status","pending") .not("early_discount_percent","is", null) .gt("early_discount_percent", 0); if (error) throw error; earlyPayOpportunities = data || []; } const allPayments = [ ...(urgentPayments || []), ...(upcomingPayments || []), ...earlyPayOpportunities, ]; const totalAmount = allPayments.reduce((sum, p) => sum + p.amount, 0); const potentialSavings = earlyPayOpportunities.reduce( (sum, p) => sum + (p.amount * (p.early_discount_percent || 0)) / 100, 0, ); res.json({ urgentPayments: urgentPayments || [], upcomingPayments: upcomingPayments || [], earlyPayOpportunities, totalAmount, potentialSavings, }); } catch (error) { logger.error("POST /payments/suggest-run error:", error); res.status(500).json({ error: handleSupabaseError(error) }); }
}); // POST: Process batch payment
router.post("/payments/batch", async (req: Request, res: Response) => { try { const { payment_ids, status ="processing" } = req.body; const { data, error } = await supabase .from("invoice_payments") .update({ status, updated_at: new Date().toISOString(), }) .in("id", payment_ids) .select(); if (error) throw error; res.json({ updated: data || [], count: data?.length || 0 }); } catch (error) { logger.error("POST /payments/batch error:", error); res.status(500).json({ error: handleSupabaseError(error) }); }
}); /** * PAYMENT SCHEDULES */ // GET: Retrieve payment schedules (paginated)
router.get("/payment-schedules", paginationMiddleware, async (req: Request, res: Response) => { try { const pagination = res.locals.pagination; const { organization_id, outlet_id, status } = req.query; let query = supabase.from("payment_schedules").select("*"); if (organization_id) { query = query.eq("organization_id", organization_id); } if (outlet_id) { query = query.eq("outlet_id", outlet_id); } if (status) { query = query.eq("status", status); } const { data, error } = await applyPaginationRange( query.order("created_at", { ascending: false }), pagination, ); if (error) throw error; // Get total count let countQuery = supabase .from("payment_schedules") .select("id", { count:"exact", head: true }); if (organization_id) countQuery = countQuery.eq("organization_id", organization_id); if (outlet_id) countQuery = countQuery.eq("outlet_id", outlet_id); if (status) countQuery = countQuery.eq("status", status); const total = await countTotal(countQuery); res.json( buildPaginatedResponse( data || [], total, pagination.limit, pagination.offset, ), ); } catch (error) { logger.error("GET /payment-schedules error:", error); res.status(500).json({ error: handleSupabaseError(error) }); } },
); // POST: Create payment schedule
router.post("/payment-schedules", async (req: Request, res: Response) => { try { const { data, error } = await supabase .from("payment_schedules") .insert([req.body]) .select(); if (error) throw error; res.status(201).json(data?.[0] || req.body); } catch (error) { logger.error("POST /payment-schedules error:", error); res.status(500).json({ error: handleSupabaseError(error) }); }
}); // POST: Run payment schedule
router.post("/payment-schedules/:id/run", async (req: Request, res: Response) => { try { // Get the schedule const { data: schedule, error: scheduleError } = await supabase .from("payment_schedules") .select("*") .eq("id", req.params.id) .single(); if (scheduleError) throw scheduleError; if (!schedule) { return res.status(404).json({ error:"Schedule not found" }); } // Log the run const { data: run, error: runError } = await supabase .from("payment_schedule_runs") .insert([ { schedule_id: schedule.id, payment_ids: [], total_amount: 0, total_discount_savings: 0, status:"completed", successful_count: 0, failed_count: 0, executed_at: new Date().toISOString(), }, ]) .select() .single(); if (runError) throw runError; // Update schedule with last/next run times await supabase .from("payment_schedules") .update({ last_run_at: new Date().toISOString(), updated_at: new Date().toISOString(), }) .eq("id", schedule.id); res.json({ schedule_id: schedule.id, run_id: run.id, status:"completed", executed_at: new Date().toISOString(), }); } catch (error) { logger.error("POST /payment-schedules/:id/run error:", error); res.status(500).json({ error: handleSupabaseError(error) }); } },
); /** * GL INTEGRATION */ // GET: GL accounts (paginated)
router.get("/gl-accounts", paginationMiddleware, async (req: Request, res: Response) => { try { const pagination = res.locals.pagination; const { organization_id, account_type } = req.query; let query = supabase.from("gl_accounts").select("*"); if (organization_id) { query = query.eq("organization_id", organization_id); } if (account_type) { query = query.eq("type", account_type); } const { data, error } = await applyPaginationRange( query.eq("is_active", true), pagination, ); if (error) throw error; // Get total count let countQuery = supabase .from("gl_accounts") .select("id", { count:"exact", head: true }) .eq("is_active", true); if (organization_id) countQuery = countQuery.eq("organization_id", organization_id); if (account_type) countQuery = countQuery.eq("type", account_type); const total = await countTotal(countQuery); res.json( buildPaginatedResponse( data || [], total, pagination.limit, pagination.offset, ), ); } catch (error) { logger.error("GET /gl-accounts error:", error); res.status(500).json({ error: handleSupabaseError(error) }); } },
); // GET: GL trial balance
router.get("/gl-accounts/trial-balance", async (req: Request, res: Response) => { try { const { organization_id } = req.query; const { data, error } = await supabase .from("gl_accounts") .select("id, code, name, balance, type") .eq("organization_id", organization_id); if (error) throw error; const totalDebits = (data || []) .filter((a: any) => ["asset","expense","cogs"].includes(a.type)) .reduce((sum: number, a: any) => sum + (a.balance || 0), 0); const totalCredits = (data || []) .filter((a: any) => ["liability","equity","revenue"].includes(a.type)) .reduce((sum: number, a: any) => sum + (a.balance || 0), 0); res.json({ organization_id, period:"current", accounts: data || [], total_debits: totalDebits, total_credits: totalCredits, is_balanced: Math.abs(totalDebits - totalCredits) < 0.01, }); } catch (error) { logger.error("GET /gl-accounts/trial-balance error:", error); res.status(500).json({ error: handleSupabaseError(error) }); } },
); // POST: Record GL entry
router.post("/gl-entries", async (req: Request, res: Response) => { try { const { data, error } = await supabase .from("gl_entries") .insert([ { ...req.body, status:"posted", posted_at: new Date().toISOString(), }, ]) .select(); if (error) throw error; res.status(201).json(data?.[0] || req.body); } catch (error) { logger.error("POST /gl-entries error:", error); res.status(500).json({ error: handleSupabaseError(error) }); }
}); /** * REPORTING */ // GET: P&L Statement
router.get("/reporting/pandl", async (req: Request, res: Response) => { try { const { organization_id, outlet_id, start_date, end_date } = req.query; let query = supabase.from("pandl_statements").select("*"); if (organization_id) { query = query.eq("organization_id", organization_id); } if (outlet_id) { query = query.eq("outlet_id", outlet_id); } if (start_date) { query = query.gte("period_start", start_date); } if (end_date) { query = query.lte("period_end", end_date); } const { data, error } = await query .eq("is_draft", false) .order("period_start", { ascending: false }) .limit(1); if (error) throw error; res.json(data?.[0] || null); } catch (error) { logger.error("GET /reporting/pandl error:", error); res.status(500).json({ error: handleSupabaseError(error) }); }
}); // GET: Financial metrics
router.get("/reporting/metrics", async (req: Request, res: Response) => { try { const { organization_id, outlet_id } = req.query; let query = supabase.from("financial_metrics").select("*"); if (organization_id) { query = query.eq("organization_id", organization_id); } if (outlet_id) { query = query.eq("outlet_id", outlet_id); } const { data, error } = await query .order("metric_date", { ascending: false }) .limit(1); if (error) throw error; res.json(data?.[0] || null); } catch (error) { logger.error("GET /reporting/metrics error:", error); res.status(500).json({ error: handleSupabaseError(error) }); }
}); // GET: AP Dashboard
router.get("/ap-dashboard", async (req: Request, res: Response) => { try { const { organization_id, outlet_id } = req.query; const now = new Date(); // Get overdue payments const { data: overdue, error: error1 } = await supabase .from("invoice_payments") .select("*") .eq("organization_id", organization_id) .neq("status","completed") .lte("due_date", now.toISOString().split("T")[0]); if (error1) throw error1; const overduAmount = (overdue || []).reduce((sum, p) => sum + p.amount, 0); // Get total payable const { data: allPayments, error: error2 } = await supabase .from("invoice_payments") .select("*") .eq("organization_id", organization_id) .neq("status","completed"); if (error2) throw error2; const totalPayable = (allPayments || []).reduce( (sum, p) => sum + p.amount, 0, ); res.json({ organization_id, outlet_id, total_payable: totalPayable, overdue_amount: overduAmount, overdue_invoices: overdue?.length || 0, due_within_7_days: 0, early_pay_opportunities: (allPayments || []).filter( (p) => p.early_discount_percent, ).length, cash_position: 250000, days_payable_outstanding: 35, updated_at: new Date().toISOString(), }); } catch (error) { logger.error("GET /ap-dashboard error:", error); res.status(500).json({ error: handleSupabaseError(error) }); }
}); // GET: Vendor analysis
router.get("/vendors/analysis", async (req: Request, res: Response) => { try { const { organization_id, limit = 10 } = req.query; const { data, error } = await supabase .from("vendor_analysis") .select("*") .eq("organization_id", organization_id) .order("total_amount", { ascending: false }) .limit(Number(limit)); if (error) throw error; res.json(data || []); } catch (error) { logger.error("GET /vendors/analysis error:", error); res.status(500).json({ error: handleSupabaseError(error) }); }
}); // GET: Top vendors
router.get("/vendors/top", async (req: Request, res: Response) => { try { const { organization_id, limit = 5 } = req.query; const { data, error } = await supabase .from("invoice_payments") .select("vendor_id, amount") .eq("organization_id", organization_id) .eq("status","completed") .order("amount", { ascending: false }); if (error) throw error; // Aggregate by vendor const vendorMap = new Map<string, { spend: number; count: number }>(); (data || []).forEach((payment: any) => { const existing = vendorMap.get(payment.vendor_id) || { spend: 0, count: 0, }; vendorMap.set(payment.vendor_id, { spend: existing.spend + payment.amount, count: existing.count + 1, }); }); const topVendors = Array.from(vendorMap.entries()) .slice(0, Number(limit)) .map(([id, data]) => ({ id, name: id, spend: data.spend, count: data.count, })); res.json(topVendors); } catch (error) { logger.error("GET /vendors/top error:", error); res.status(500).json({ error: handleSupabaseError(error) }); }
}); // GET: Real-time AP updates
router.get("/payments/updates", async (req: Request, res: Response) => { try { const { organization_id, since } = req.query; const sinceDate = since ? new Date(since as string) : new Date(Date.now() - 3600000); const { data, error } = await supabase .from("invoice_payments") .select("*") .eq("organization_id", organization_id) .gte("updated_at", sinceDate.toISOString()); if (error) throw error; res.json({ updates: data || [], count: data?.length || 0, last_check: new Date().toISOString(), }); } catch (error) { logger.error("GET /payments/updates error:", error); res.status(500).json({ error: handleSupabaseError(error) }); }
}); /** * PAYMENT PROCESSING */ // POST: Process payment via Stripe
router.post("/payments/:id/process-payment", async (req: Request, res: Response) => { try { const { stripe_payment_method_id } = req.body; if (!stripe_payment_method_id) { return res .status(400) .json({ error:"Stripe payment method ID required" }); } // Get payment details const { data: payment, error: paymentError } = await supabase .from("invoice_payments") .select("*") .eq("id", req.params.id) .single(); if (paymentError || !payment) { return res.status(404).json({ error:"Payment not found" }); } // Process through Stripe const stripeResult = await processPayment( payment.amount, payment.currency, stripe_payment_method_id, payment.organization_id, payment.id, ); // Update payment record const { data: updated, error: updateError } = await supabase .from("invoice_payments") .update({ status: stripeResult.status ==="succeeded" ?"completed" :"processing", transaction_id: stripeResult.id, payment_date: new Date().toISOString().split("T")[0], updated_at: new Date().toISOString(), }) .eq("id", req.params.id) .select() .single(); if (updateError) throw updateError; res.json({ payment: updated, stripe_status: stripeResult.status, transaction_id: stripeResult.id, }); } catch (error) { logger.error("POST /payments/:id/process-payment error:", error); res.status(500).json({ error: String(error) }); } },
); // POST: Stripe webhook handler
router.post("/webhooks/stripe", async (req: Request, res: Response) => { try { const sig = req.headers["stripe-signature"] as string; const webhookSecret = process.env.STRIPE_WEBHOOK_SECRET ||""; if (!webhookSecret) { return res.status(400).json({ error:"Webhook secret not configured" }); } const event = stripe.webhooks.constructEvent(req.body, sig, webhookSecret); // Handle the event switch (event.type) { case"payment_intent.succeeded": { const paymentIntent = event.data.object as any; const invoicePaymentId = paymentIntent.metadata.invoicePaymentId; await supabase .from("invoice_payments") .update({ status:"completed", payment_date: new Date().toISOString().split("T")[0], updated_at: new Date().toISOString(), }) .eq("id", invoicePaymentId); } break; case"payment_intent.payment_failed": { const paymentIntent = event.data.object as any; const invoicePaymentId = paymentIntent.metadata.invoicePaymentId; await supabase .from("invoice_payments") .update({ status:"failed", updated_at: new Date().toISOString(), }) .eq("id", invoicePaymentId); } break; } res.json({ received: true }); } catch (error) { logger.error("Webhook error:", error); res.status(400).json({ error:"Webhook processing failed" }); }
}); export const accountingRouter = router;
