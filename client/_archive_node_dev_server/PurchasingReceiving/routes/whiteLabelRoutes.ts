import { Router, Request, Response } from "express";
import { whiteLabelService } from "../lib/whiteLabelService";
import { WhiteLabelCustomization } from "../../shared/whiteLabelConfig";
import { logger, sanitizeError } from "../lib/logger"; const router = Router(); // Get white-label config by domain
router.get("/config", async (req: Request, res: Response) => { try { const { domain } = req.query; if (!domain || typeof domain !=="string") { return res.status(400).json({ error:"Domain parameter is required" }); } const config = await whiteLabelService.getWhiteLabelConfig(domain); res.json(config); } catch (error) { logger.error("Error fetching white-label config:", error); res.status(500).json({ error:"Failed to fetch configuration" }); }
}); // Get white-label config by ID
router.get("/configs/:id", async (req: Request, res: Response) => { try { const { id } = req.params; const config = await whiteLabelService.getConfigById(id); if (!config) { return res.status(404).json({ error:"Configuration not found" }); } res.json(config); } catch (error) { logger.error("Error fetching white-label config:", error); res.status(500).json({ error:"Failed to fetch configuration" }); }
}); // List all white-label configs (admin only)
router.get("/configs", async (req: Request, res: Response) => { try { // TODO: Add authentication check for admin role const configs = await whiteLabelService.listAllConfigs(); res.json(configs); } catch (error) { logger.error("Error listing white-label configs:", error); res.status(500).json({ error:"Failed to list configurations" }); }
}); // Create white-label config (admin only)
router.post("/configs", async (req: Request, res: Response) => { try { // TODO: Add authentication check for admin role const config = req.body as Partial<WhiteLabelCustomization>; const result = await whiteLabelService.createWhiteLabelConfig(config); if (!result) { return res.status(400).json({ error:"Failed to create configuration" }); } res.status(201).json(result); } catch (error) { logger.error("Error creating white-label config:", error); res.status(500).json({ error:"Failed to create configuration", details: error instanceof Error ? error.message :"Unknown error", }); }
}); // Update white-label config (admin only)
router.patch("/configs/:id", async (req: Request, res: Response) => { try { // TODO: Add authentication check for admin role const { id } = req.params; const updates = req.body as Partial<WhiteLabelCustomization>; const result = await whiteLabelService.updateWhiteLabelConfig(id, updates); if (!result) { return res.status(404).json({ error:"Configuration not found" }); } res.json(result); } catch (error) { logger.error("Error updating white-label config:", error); res.status(500).json({ error:"Failed to update configuration", details: error instanceof Error ? error.message :"Unknown error", }); }
}); // Delete white-label config (admin only)
router.delete("/configs/:id", async (req: Request, res: Response) => { try { // TODO: Add authentication check for admin role const { id } = req.params; const success = await whiteLabelService.deleteWhiteLabelConfig(id); if (!success) { return res.status(404).json({ error:"Configuration not found" }); } res.json({ success: true }); } catch (error) { logger.error("Error deleting white-label config:", error); res.status(500).json({ error:"Failed to delete configuration" }); }
}); // Activate white-label config (admin only)
router.post("/configs/:id/activate", async (req: Request, res: Response) => { try { // TODO: Add authentication check for admin role const { id } = req.params; const success = await whiteLabelService.activateWhiteLabelConfig(id); if (!success) { return res.status(404).json({ error:"Configuration not found" }); } res.json({ success: true }); } catch (error) { logger.error("Error activating white-label config:", error); res.status(500).json({ error:"Failed to activate configuration" }); }
}); // Validate CSS (admin only)
router.post("/validate-css", async (req: Request, res: Response) => { try { // TODO: Add authentication check for admin role const { css } = req.body; if (!css || typeof css !=="string") { return res.status(400).json({ error:"CSS content is required" }); } const validation = await whiteLabelService.validateCustomCSS(css); res.json(validation); } catch (error) { logger.error("Error validating CSS:", error); res.status(500).json({ error:"Failed to validate CSS" }); }
}); // Clear white-label cache (admin only)
router.post("/cache/clear", async (req: Request, res: Response) => { try { // TODO: Add authentication check for admin role const { domain } = req.body; whiteLabelService.clearCache(domain); res.json({ success: true, message: domain ? `Cache cleared for domain: ${domain}` :"All caches cleared", }); } catch (error) { logger.error("Error clearing cache:", error); res.status(500).json({ error:"Failed to clear cache" }); }
}); export default router;
