import { Router, Request, Response } from"express";
import type { MultiDomainTrainingSession, DomainTrainingState,
} from"../../shared/multi-domain-training";
import { MULTI_DOMAIN_TRAINING_PROFILES, initializeMultiDomainSession, getTrainingProfile,
} from"../lib/multi-domain-training-config";
import { storeKnowledgeBatch, identifyKnowledgeGaps,
} from"../lib/knowledge-vector-service";
import { saveTrainingSession, saveDomainTrainingState, saveLearnedKnowledgeToPinecone, loadTrainingSession, getCompletedTrainingSessions,
} from"../lib/training-persistence-service";
import { recordCompletedTrainingDomains } from"../lib/complete-training-domains";
import { getPineconeStatus, verifyTrainingVectors, storeTrainingDataToPinecone,
} from"../lib/pinecone-verification-service";
import { validateTrainingData, deduplicateTrainingData, getExistingTrainingFingerprints, logTrainingEvent } from"../lib/training-deduplication-service";
import type { AnyKnowledge } from"../../client/echo/types/knowledge"; const router = Router();
const openaiApiKey = process.env.OPENAI_API_KEY; // Store active sessions in memory (in production, use database)
const activeSessions = new Map<string, MultiDomainTrainingSession>(); /** * Simple concurrency limiter for controlled parallelization */
function createConcurrencyLimiter(maxConcurrent: number) { let running = 0; const queue: Array<() => Promise<any>> = []; const run = async (fn: () => Promise<any>) => { while (running >= maxConcurrent) { await new Promise((resolve) => setTimeout(resolve, 10)); } running++; try { return await fn(); } finally { running--; } }; return { run };
} /** * POST /api/multi-domain-training/start * Initialize and start autonomous multi-domain training */
router.post("/start", async (_req: Request, res: Response) => { try { if (!openaiApiKey) { return res.status(500).json({ error:"OpenAI API key not configured" }); } const session = initializeMultiDomainSession(); activeSessions.set(session.id, session); console.log("[MultiDomainTraining] Starting session:", session.id); return res.json({ success: true, session, message:"Multi-domain training session started", }); } catch (error: any) { console.error("[MultiDomainTraining] Start failed:", error); return res.status(500).json({ success: false, error: error.message ||"Failed to start training", }); }
}); /** * POST /api/multi-domain-training/run-autonomous * Run autonomous training for a specific domain */
router.post("/run-autonomous", async (req: Request, res: Response) => { try { const { sessionId, profileId } = req.body as { sessionId: string; profileId: string; }; if (!sessionId || !profileId) { return res.status(400).json({ error:"sessionId and profileId are required", }); } const session = activeSessions.get(sessionId); if (!session) { return res.status(404).json({ error:"Session not found" }); } const profile = getTrainingProfile(profileId); if (!profile) { return res.status(404).json({ error:"Training profile not found" }); } const domainState = session.domainStates[profileId]; domainState.status ="in_progress"; domainState.startedAt = new Date().toISOString(); console.log( `[MultiDomainTraining] Starting autonomous training for ${profile.name}`, ); let totalKnowledgeExtracted = 0; // Run the configured number of exchanges for this domain for (let i = 0; i < profile.exchangeCount; i++) { try { const result = await conductAutonomousExchange( profile, i + 1, profile.exchangeCount, ); if (result.success && result.knowledge) { totalKnowledgeExtracted += result.knowledge.length; domainState.exchangesCompleted = i + 1; domainState.knowledgeItemsLearned = totalKnowledgeExtracted; // Store knowledge immediately if (result.knowledge.length > 0) { await storeKnowledgeBatch(result.knowledge); console.log( `[MultiDomainTraining] Stored ${result.knowledge.length} knowledge items from exchange ${i + 1}/${profile.exchangeCount}`, ); } } // Small delay between exchanges to avoid rate limiting await new Promise((resolve) => setTimeout(resolve, 500)); } catch (exchangeError: any) { console.error( `[MultiDomainTraining] Exchange ${i + 1} failed:`, exchangeError, ); // Continue with next exchange even if one fails } } domainState.status ="completed"; domainState.completedAt = new Date().toISOString(); session.totalKnowledgeLearned += totalKnowledgeExtracted; session.overallProgress = calculateSessionProgress(session); console.log( `[MultiDomainTraining] Completed ${profile.name}: ${totalKnowledgeExtracted} items learned`, ); return res.json({ success: true, profile, domainState, totalKnowledgeExtracted, message: `Training completed for ${profile.name}`, }); } catch (error: any) { console.error("[MultiDomainTraining] Autonomous training failed:", error); return res.status(500).json({ success: false, error: error.message ||"Autonomous training failed", }); }
}); /** * GET /api/multi-domain-training/session/:sessionId * Get current session status */
router.get("/session/:sessionId", (req: Request, res: Response) => { const { sessionId } = req.params; const session = activeSessions.get(sessionId); if (!session) { return res.status(404).json({ error:"Session not found" }); } return res.json({ success: true, session, });
}); /** * POST /api/multi-domain-training/run-all-sequential * Run all domains sequentially */
router.post("/run-all-sequential", async (req: Request, res: Response) => { try { if (!openaiApiKey) { return res.status(500).json({ error:"OpenAI API key not configured" }); } const { sessionId } = req.body as { sessionId: string }; if (!sessionId) { return res.status(400).json({ error:"sessionId is required" }); } const session = activeSessions.get(sessionId); if (!session) { return res.status(404).json({ error:"Session not found" }); } session.status ="in_progress"; session.startedAt = new Date().toISOString(); console.log( `[MultiDomainTraining] Starting sequential training for all ${MULTI_DOMAIN_TRAINING_PROFILES.length} domains`, ); // Send initial response immediately res.json({ success: true, message:"Sequential multi-domain training started", sessionId, totalDomains: MULTI_DOMAIN_TRAINING_PROFILES.length, }); // Run training asynchronously in background with optimized parallelization (async () => { try { const limiter = createConcurrencyLimiter(3); // Max 3 concurrent exchanges const allExchanges: Array<{ profile: (typeof MULTI_DOMAIN_TRAINING_PROFILES)[0]; exchangeIndex: number; }> = []; // Build queue of all exchanges across all domains for (const profile of MULTI_DOMAIN_TRAINING_PROFILES) { for (let i = 0; i < profile.exchangeCount; i++) { allExchanges.push({ profile, exchangeIndex: i }); } } console.log( `[MultiDomainTraining] Starting ${allExchanges.length} exchanges across ${MULTI_DOMAIN_TRAINING_PROFILES.length} domains with 3x parallelization`, ); // Run all exchanges with controlled concurrency const exchangePromises = allExchanges.map((exchange) => limiter.run(async () => { const { profile, exchangeIndex } = exchange; const domainState = session.domainStates[profile.id]; if (domainState.status ==="pending") { domainState.status ="in_progress"; domainState.startedAt = new Date().toISOString(); } try { const result = await conductAutonomousExchange( profile, exchangeIndex + 1, profile.exchangeCount, ); if (result.success && result.knowledge && result.knowledge.length > 0) { domainState.exchangesCompleted = Math.min( domainState.exchangesCompleted + 1, profile.exchangeCount, ); domainState.knowledgeItemsLearned += result.knowledge.length; session.totalKnowledgeLearned += result.knowledge.length; console.log( `[MultiDomainTraining] ✓ ${profile.name} exchange ${exchangeIndex + 1}/${profile.exchangeCount}: ` + `Extracted ${result.knowledge.length} items (total session: ${session.totalKnowledgeLearned})`, ); // Store knowledge with detailed logging const storageResult = await storeKnowledgeBatch(result.knowledge, 5); console.log( `[MultiDomainTraining] ${profile.name} exchange ${exchangeIndex + 1}/${profile.exchangeCount}: ` + `Stored ${storageResult.success} successfully${storageResult.failed > 0 ? `, ${storageResult.failed} failed` :""}`, ); if (storageResult.failed > 0) { console.warn( `[MultiDomainTraining] Storage failures in ${profile.name}: ${JSON.stringify(storageResult.errors?.slice(0, 3) || [])}`, ); } } else if (!result.success) { console.warn( `[MultiDomainTraining] ${profile.name} exchange ${exchangeIndex + 1} failed to extract knowledge`, ); } else if (!result.knowledge || result.knowledge.length === 0) { console.warn( `[MultiDomainTraining] ${profile.name} exchange ${exchangeIndex + 1} returned 0 knowledge items`, ); } // Reduced delay from 500ms to 200ms await new Promise((resolve) => setTimeout(resolve, 200)); } catch (exchangeError) { console.error( `[MultiDomainTraining] ${profile.name} exchange ${exchangeIndex + 1} failed:`, exchangeError, ); domainState.status ="failed"; domainState.error = (exchangeError as any).message; } }), ); await Promise.all(exchangePromises); // Mark all domains as completed for (const profile of MULTI_DOMAIN_TRAINING_PROFILES) { const domainState = session.domainStates[profile.id]; if (domainState.status !=="failed") { domainState.status ="completed"; domainState.completedAt = new Date().toISOString(); } } session.overallProgress = calculateSessionProgress(session); // Mark session as complete session.status ="completed"; session.completedAt = new Date().toISOString(); console.log( `[MultiDomainTraining] ✓ All training completed. Total knowledge learned: ${session.totalKnowledgeLearned}`, ); } catch (error: any) { console.error("[MultiDomainTraining] Optimized training error:", error); session.status ="failed"; session.completedAt = new Date().toISOString(); } })(); } catch (error: any) { console.error("[MultiDomainTraining] Sequential training setup failed:", error, ); return res.status(500).json({ success: false, error: error.message ||"Failed to setup sequential training", }); }
}); /** * Helper: Conduct a single autonomous exchange for a domain */
async function conductAutonomousExchange( profile: (typeof MULTI_DOMAIN_TRAINING_PROFILES)[0], exchangeNumber: number, totalExchanges: number,
): Promise<{ success: boolean; knowledge?: AnyKnowledge[] }> { if (!openaiApiKey) { throw new Error("OpenAI API key not configured"); } // Generate Echo's question for this exchange const echoPrompt = generateEchoQuestion( profile, exchangeNumber, totalExchanges, ); // Get OpenAI's comprehensive response const openaiResponse = await getOpenAIResponse(echoPrompt); if (!openaiResponse) { return { success: false }; } // Extract knowledge from OpenAI's response const knowledge = await extractKnowledgeFromResponse(openaiResponse, profile); return { success: true, knowledge, };
} /** * Generate structured questions for Echo to ask OpenAI */
function generateEchoQuestion( profile: (typeof MULTI_DOMAIN_TRAINING_PROFILES)[0], exchangeNumber: number, totalExchanges: number,
): string { const progressText = `(Exchange ${exchangeNumber}/${totalExchanges})`; if (exchangeNumber === 1) { return `As a training partner, provide a comprehensive overview of ${profile.name}. Focus on: ${profile.focusAreas.slice(0, 2).join(",")} Include:
1. Core principles and concepts
2. Key terminology and definitions
3. Practical applications
4. Common challenges and solutions Make this detailed and actionable for a culinary professional. ${progressText}`; } else if (exchangeNumber === totalExchanges) { return `Provide advanced insights and best practices for ${profile.name}. Build on previous exchanges and go deeper into: ${profile.focusAreas.slice(2).join(",")} Include:
1. Advanced techniques and methodologies
2. Real-world case studies and examples
3. Industry trends and innovations
4. Integration with other culinary systems Focus on expertise and mastery. ${progressText}`; } else { const focusArea = profile.focusAreas[exchangeNumber % profile.focusAreas.length]; return `Dive deeper into ${focusArea} for ${profile.name}. Provide practical, detailed information on:
1. Technical expertise in this area
2. Common pitfalls and how to avoid them
3. Performance optimization techniques
4. Real-world applications and examples Make this highly practical and actionable. ${progressText}`; }
} /** * Get comprehensive response from OpenAI */
async function getOpenAIResponse(prompt: string): Promise<string | null> { if (!openaiApiKey) { throw new Error("OpenAI API key not configured"); } try { const response = await fetch("https://api.openai.com/v1/chat/completions", { method:"POST", headers: {"Content-Type":"application/json", Authorization: `Bearer ${openaiApiKey}`, }, body: JSON.stringify({ model:"gpt-4-turbo-preview", messages: [ { role:"system", content:"You are an expert culinary and hospitality instructor. Provide detailed, practical, and comprehensive knowledge that can be applied immediately.", }, { role:"user", content: prompt, }, ], temperature: 0.8, max_tokens: 2500, }), }); if (!response.ok) { console.error( `[MultiDomainTraining] OpenAI API error: ${response.status}`, ); return null; } const data = (await response.json()) as any; return data.choices?.[0]?.message?.content || null; } catch (error) { console.error("[MultiDomainTraining] Failed to get OpenAI response:", error, ); return null; }
} /** * Extract structured knowledge from OpenAI response */
async function extractKnowledgeFromResponse( openaiResponse: string, profile: (typeof MULTI_DOMAIN_TRAINING_PROFILES)[0],
): Promise<AnyKnowledge[]> { if (!openaiApiKey) { throw new Error("OpenAI API key not configured"); } try { const extractionPrompt = `Extract ALL important knowledge items from this response about ${profile.name}. Response:"${openaiResponse}" Extract every significant concept, technique, principle, definition, or piece of actionable information. For each item, provide:
1. Clear, concise title
2. Detailed explanation (150-300 words)
3. Relevant keywords/tags
4. Type: technique, concept, principle, procedure, terminology, or guideline Return as JSON array with at least 4-6 items:
[ {"title":"Knowledge Title","content":"Detailed explanation...","type":"technique|concept|principle|procedure|terminology|guideline","tags": ["keyword1","keyword2"] }
] Return ONLY valid JSON array.`; const response = await fetch("https://api.openai.com/v1/chat/completions", { method:"POST", headers: {"Content-Type":"application/json", Authorization: `Bearer ${openaiApiKey}`, }, body: JSON.stringify({ model:"gpt-4-turbo-preview", messages: [ { role:"user", content: extractionPrompt, }, ], temperature: 0.7, max_tokens: 3000, }), }); if (!response.ok) { console.error( `[MultiDomainTraining] Knowledge extraction error: ${response.status}`, ); return []; } const data = (await response.json()) as any; const content = data.choices?.[0]?.message?.content; if (!content) { return []; } const knowledge: AnyKnowledge[] = []; try { const jsonMatch = content.match(/\[[\s\S]*\]/); if (jsonMatch) { const parsed = JSON.parse(jsonMatch[0]); if (Array.isArray(parsed)) { for (const item of parsed) { if (item.title && item.content) { knowledge.push({ id: `knowledge-${Date.now()}-${Math.random().toString(36).substr(2, 9)}`, type: item.type ||"concept", title: item.title.substring(0, 200), description: item.content.substring(0, 1000), content: item.content.substring(0, 2000), source:"multi-domain-training", sourceType:"openai", tags: [ profile.name, ...(item.tags || []), ...profile.focusAreas.slice(0, 2), ], domain: profile.domain, createdAt: new Date().toISOString(), updatedAt: new Date().toISOString(), confidence: 0.9, } as AnyKnowledge); } } } } } catch (parseError) { console.warn("[MultiDomainTraining] Failed to parse knowledge extraction:", parseError, ); } return knowledge; } catch (error) { console.error("[MultiDomainTraining] Knowledge extraction failed:", error); return []; }
} /** * POST /api/multi-domain-training/save-session * Save current session to Supabase and Pinecone */
router.post("/save-session", async (req: Request, res: Response) => { try { const { sessionId } = req.body as { sessionId: string }; if (!sessionId) { return res.status(400).json({ error:"sessionId is required" }); } const session = activeSessions.get(sessionId); if (!session) { return res.status(404).json({ error:"Session not found" }); } // Save to Supabase await saveTrainingSession(session); // Save domain states for (const [profileId, state] of Object.entries(session.domainStates)) { await saveDomainTrainingState(sessionId, state); } console.log("[MultiDomainTraining] Session saved to Supabase:", sessionId); return res.json({ success: true, message:"Training session saved", sessionId, }); } catch (error: any) { console.error("[MultiDomainTraining] Save session failed:", error); return res.status(500).json({ success: false, error: error.message ||"Failed to save session", }); }
}); /** * POST /api/multi-domain-training/record-completed * Record the 3 completed training domains (Culinary, Pastry, Beverage) */
router.post("/record-completed", async (_req: Request, res: Response) => { try { console.log("[MultiDomainTraining] Recording completed training domains...", ); const session = await recordCompletedTrainingDomains(); console.log("[MultiDomainTraining] Successfully recorded completed domains", ); return res.json({ success: true, message:"Completed training domains recorded", session, }); } catch (error: any) { console.error("[MultiDomainTraining] Record completed failed:", error); return res.status(500).json({ success: false, error: error.message ||"Failed to record completed domains", }); }
}); /** * GET /api/multi-domain-training/sessions/completed * Get all completed training sessions */
router.get("/sessions/completed", async (_req: Request, res: Response) => { try { const completedSessions = await getCompletedTrainingSessions(); return res.json({ success: true, sessions: completedSessions, count: completedSessions.length, }); } catch (error: any) { console.error("[MultiDomainTraining] Get completed sessions failed:", error, ); return res.status(500).json({ success: false, error: error.message ||"Failed to get completed sessions", }); }
}); /** * GET /api/multi-domain-training/session/:sessionId * Load a training session from Supabase */
router.get("/session/:sessionId", async (req: Request, res: Response) => { try { const { sessionId } = req.params; if (!sessionId) { return res.status(400).json({ error:"sessionId is required" }); } const session = activeSessions.get(sessionId) || (await loadTrainingSession(sessionId)); if (!session) { return res.status(404).json({ error:"Session not found" }); } return res.json({ success: true, session, }); } catch (error: any) { console.error("[MultiDomainTraining] Load session failed:", error); return res.status(500).json({ success: false, error: error.message ||"Failed to load session", }); }
}); /** * GET /api/multi-domain-training/pinecone/status * Check Pinecone connection and training data storage status */
router.get("/pinecone/status", async (_req: Request, res: Response) => { try { const status = await getPineconeStatus(); return res.json({ success: true, pinecone: status, }); } catch (error: any) { console.error("[MultiDomainTraining] Pinecone status check failed:", error); // Return partial response instead of 500 error to avoid breaking the UI return res.status(200).json({ success: true, pinecone: { connected: false, indexName:"echo-knowledge", indexStats: null, trainingDataVectors: null, error: error.message ||"Failed to check Pinecone status", }, }); }
}); /** * GET /api/multi-domain-training/pinecone/verify * Verify all training vectors are stored in Pinecone */
router.get("/pinecone/verify", async (_req: Request, res: Response) => { try { const verification = await verifyTrainingVectors(); return res.json({ success: true, verification, }); } catch (error: any) { console.error("[MultiDomainTraining] Pinecone verification failed:", error); return res.status(500).json({ success: false, error: error.message ||"Failed to verify training vectors", }); }
}); /** * POST /api/multi-domain-training/pinecone/sync * Sync training data to Pinecone (in case some vectors were missed) */
router.post("/pinecone/sync", async (req: Request, res: Response) => { try { const { sessionId, trainingData } = req.body as { sessionId: string; trainingData: Array<{ profileId: string; domain: string; title: string; content: string; confidence: number; }>; }; if (!sessionId || !trainingData) { return res.status(400).json({ error:"sessionId and trainingData are required", }); } const result = await storeTrainingDataToPinecone(sessionId, trainingData); return res.json({ success: result.success, stored: result.stored, error: result.error, }); } catch (error: any) { console.error("[MultiDomainTraining] Pinecone sync failed:", error); return res.status(500).json({ success: false, error: error.message ||"Failed to sync training data", }); }
}); /** * POST /api/multi-domain-training/pinecone/store-completed * Store training vectors to Pinecone from actual training sessions * Accepts training data as input instead of using hardcoded data */
router.post("/pinecone/store-completed", async (req: Request, res: Response) => { try { const { trainingData, sessionId } = req.body as { trainingData?: Array<{ profileId: string; domain: string; title: string; content: string; confidence: number }>; sessionId?: string; }; if (!trainingData || trainingData.length === 0) { return res.json({ success: false, stored: 0, message:"No training data provided. POST with trainingData array in body.", error:"EMPTY_TRAINING_DATA", }); } const finalSessionId = sessionId || `training-session-${Date.now()}-${Math.random().toString(36).substring(7)}`; console.log( `[MultiDomainTraining] Storing ${trainingData.length} training vectors to Pinecone (sessionId: ${finalSessionId})`, ); const result = await storeTrainingDataToPinecone(finalSessionId, trainingData); return res.json({ success: result.success, stored: result.stored, total: trainingData.length, sessionId: finalSessionId, message: result.success ? `Successfully stored ${result.stored}/${trainingData.length} training vectors to Pinecone` : `Failed to store training vectors: ${result.error}`, error: result.error, }); } catch (error: any) { console.error("[MultiDomainTraining] Store completed training failed:", error, ); return res.status(500).json({ success: false, error: error.message ||"Failed to store completed training", }); } },
); /** * POST /api/multi-domain-training/submit-training * Safe endpoint to submit training data with validation and deduplication */
router.post("/submit-training", async (req: Request, res: Response) => { try { const { trainingData, sessionId, skipDuplicateCheck } = req.body as { trainingData?: Array<{ profileId: string; domain: string; title: string; content: string; confidence: number; sourceId?: string; }>; sessionId?: string; skipDuplicateCheck?: boolean; }; if (!trainingData || trainingData.length === 0) { return res.status(400).json({ success: false, error:"No training data provided", }); } const finalSessionId = sessionId || `session-${Date.now()}-${Math.random().toString(36).substring(7)}`; // Step 1: Validate training data const { valid, invalid } = validateTrainingData(trainingData); if (invalid.length > 0) { console.warn(`[Training] Validation failed for ${invalid.length} items`); return res.status(400).json({ success: false, error:"Training data validation failed", invalidItems: invalid.map((i) => ({ title: i.item.title, reason: i.reason, })), validCount: valid.length, }); } // Step 2: Deduplicate training data (if not skipped) let uniqueData = valid; let duplicateCount = 0; if (!skipDuplicateCheck) { const existingFingerprints = await getExistingTrainingFingerprints(); const { unique, duplicates } = deduplicateTrainingData(valid, existingFingerprints); uniqueData = unique; duplicateCount = duplicates.length; if (duplicates.length > 0) { console.log( `[Training] Found ${duplicates.length} duplicate training items, skipping`, ); } } if (uniqueData.length === 0) { return res.json({ success: true, stored: 0, sessionId: finalSessionId, message:"All training data items were duplicates. No new vectors stored.", duplicatesFound: duplicateCount, validation: { submitted: trainingData.length, valid: valid.length, duplicates: duplicateCount, }, }); } // Step 3: Store to Pinecone logTrainingEvent("SUBMISSION_START", { sessionId: finalSessionId, itemsSubmitted: trainingData.length, }); const result = await storeTrainingDataToPinecone(finalSessionId, uniqueData); logTrainingEvent("SUBMISSION_COMPLETE", { sessionId: finalSessionId, itemsSubmitted: trainingData.length, itemsStored: result.stored, duplicatesFound: duplicateCount, error: result.error, }); return res.json({ success: result.success, stored: result.stored, sessionId: finalSessionId, message: result.success ? `Successfully stored ${result.stored}/${uniqueData.length} training vectors` : `Failed to store training vectors: ${result.error}`, validation: { submitted: trainingData.length, valid: valid.length, duplicates: duplicateCount, unique: uniqueData.length, }, error: result.error, }); } catch (error: any) { console.error("[Training] Submit training failed:", error); return res.status(500).json({ success: false, error: error.message ||"Failed to submit training data", }); }
}); /** * POST /api/multi-domain-training/clear-duplicates * Admin endpoint to check and report duplicate training data */
router.post("/clear-duplicates", async (_req: Request, res: Response) => { try { const status = await getPineconeStatus(); if (!status.connected) { return res.status(500).json({ success: false, error:"Pinecone not connected", }); } const totalVectors = status.trainingDataVectors?.total || 0; return res.json({ success: true, message:"Duplicate check completed", stats: { totalTrainingVectors: totalVectors, byDomain: status.trainingDataVectors?.byDomain || {}, }, note:"To avoid duplicates in future, use /submit-training endpoint with validation enabled", }); } catch (error: any) { console.error("[Training] Clear duplicates failed:", error); return res.status(500).json({ success: false, error: error.message ||"Failed to check duplicates", }); }
}); /** * Calculate overall session progress */
function calculateSessionProgress(session: MultiDomainTrainingSession): number { const totalStates = Object.values(session.domainStates).length; const completedStates = Object.values(session.domainStates).filter( (state) => state.status ==="completed", ).length; return Math.round((completedStates / totalStates) * 100);
} export const multiDomainTrainingRouter = router;
