import type { Request, Response } from"express";
import { webRecipeCrawler } from"../lib/web-recipe-crawler";
import { ingredientRegionalCrawler } from"../lib/ingredient-regional-crawler";
import { llmKnowledgeEnricher } from"../lib/llm-knowledge-enricher";
import { knowledgeUpdater } from"../lib/knowledge-updater";
import { globalCrawlerManager } from"../lib/crawler-framework";
import { siteCrawlers } from"../lib/site-crawlers";
import { flavorMatrixService } from"../lib/flavor-matrix-service";
import { storeKnowledgeVector } from"../lib/knowledge-vector-service";
import type { AnyKnowledge } from"../../client/echo/types/knowledge"; interface CrawlerProgressEvent { type:"start" |"recipe" |"knowledge" |"learning" |"complete" |"error"; timestamp: number; data: { currentUrl?: string; currentRecipe?: string; recipesProcessed?: number; totalRecipes?: number; ingredientsFound?: string[]; techniqueFound?: string[]; termsBeingLearned?: string[]; termsLearned?: number; termsFailedToLearn?: number; knowledgeUpdates?: { ingredientsTaught: number; techniquesLearned: number; flavorProfilesAnalyzed: number; unknownTermsIdentified: string[]; autoLearningComplete?: boolean; autoLearningStats?: { successful: number; failed: number; }; }; message?: string; error?: string; };
} // Store active SSE connections for a user session
const activeConnections = new Map<string, Response>(); // Initialize Phase 4 global crawler with all site adapters
let crawlerInitialized = false; function initializeGlobalCrawler() { if (crawlerInitialized) return; console.log("[Crawler] Initializing global crawler with site adapters..."); console.log( `[Crawler] siteCrawlers array length: ${siteCrawlers?.length || 0}`, ); if (!siteCrawlers || siteCrawlers.length === 0) { console.warn("[Crawler] No site crawlers found to register!"); return; } for (const crawler of siteCrawlers) { try { globalCrawlerManager.registerAdapter(crawler); console.log( `[Crawler] ✓ Registered: ${crawler.name} (${crawler.domain})`, ); } catch (error) { console.error(`[Crawler] ✗ Failed to register ${crawler.name}:`, error); } } const registeredCount = globalCrawlerManager.getAllAdapters().length; console.log( `[Crawler] ✓ Total adapters registered: ${registeredCount}/${siteCrawlers.length}`, ); crawlerInitialized = true;
} // Initialize on module load
try { initializeGlobalCrawler();
} catch (error) { console.error("[Crawler] Failed to initialize global crawler:", error);
} /** * GET /api/echo/crawler/progress?sessionId=xxx * Server-Sent Events endpoint for real-time crawler progress */
export async function getCrawlerProgress(req: Request, res: Response) { const sessionId = req.query.sessionId as string; if (!sessionId) { return res.status(400).json({ error:"sessionId required" }); } // Setup SSE headers res.setHeader("Content-Type","text/event-stream"); res.setHeader("Cache-Control","no-cache"); res.setHeader("Connection","keep-alive"); res.setHeader("Access-Control-Allow-Origin","*"); res.setHeader("Access-Control-Allow-Credentials","true"); // Send initial connection confirmation sendEvent(res, { type:"start", timestamp: Date.now(), data: { message:"Connected to crawler progress stream", }, }); console.log(`[SSE] Client connected for session: ${sessionId}`); // Store connection activeConnections.set(sessionId, res); // Cleanup on disconnect const cleanup = () => { console.log(`[SSE] Client disconnected for session: ${sessionId}`); activeConnections.delete(sessionId); clearInterval(keepAliveInterval); res.end(); }; req.on("close", cleanup); res.on("close", cleanup); res.on("error", (error) => { console.error(`[SSE] Error for session ${sessionId}:`, error); cleanup(); }); // Keep connection alive with more frequent pings const keepAliveInterval = setInterval(() => { if (!res.destroyed && !res.writableEnded) { sendEvent(res, { type:"ping", timestamp: Date.now(), data: { message:"Connection active" }, }); } }, 15000);
} /** * POST /api/echo/crawler/start-crawl * Start crawling with real-time progress reporting * Supports Phase 1 (legacy) and Phase 4 (global) crawling modes */
export async function startCrawlerSession(req: Request, res: Response) { const { sessionId, maxRecipes = 500, cuisines = [], sources = [], mode ="legacy", // 'legacy' or 'global' extractFlavorData = false, autoLearn = true, } = req.body; if (!sessionId) { return res.status(400).json({ error:"sessionId required" }); } res.json({ success: true, sessionId, mode, message: `Crawler session started in ${mode} mode. Connect to /api/echo/crawler/progress for updates.`, }); // Start crawling asynchronously (with delay to let SSE connection establish) setTimeout(() => { if (mode ==="global") { crawlGlobalAndReportProgress(sessionId, { maxRecipes, cuisines, sources, extractFlavorData, autoLearn, }); } else { crawlAndReportProgress(sessionId, { maxRecipes, cuisines, sources }); } }, 500);
} /** * Main crawling function with progress reporting */
async function crawlAndReportProgress( sessionId: string, options: { maxRecipes: number; cuisines: string[]; sources: string[] },
) { const knowledgeState = { flavorProfilesAnalyzed: 0, unknownTermsIdentified: new Set<string>(), ingredientsLearned: new Set<string>(), techniquesLearned: new Set<string>(), }; // Helper to get current connection (handles race condition) const getConnection = () => activeConnections.get(sessionId); try { // Stage 1: Crawl recipes const crawledRecipes = await webRecipeCrawler.crawlRecipes({ query:"*", limit: options.maxRecipes, }); let connection = getConnection(); if (connection) { sendEvent(connection, { type:"recipe", timestamp: Date.now(), data: { message: `Crawled ${crawledRecipes.length} recipes`, recipesProcessed: 0, totalRecipes: crawledRecipes.length, }, }); } // Stage 2: Process each recipe for knowledge extraction for (let i = 0; i < crawledRecipes.length; i++) { const recipe = crawledRecipes[i]; // Extract ingredients and check for unknown terms const unknownTerms: string[] = []; if (recipe.ingredients) { for (const ingredient of recipe.ingredients) { knowledgeState.ingredientsLearned.add(ingredient.name); // Simple check: if ingredient contains unusual characters or is longer than typical, flag it if ( ingredient.name.length > 20 || /[^a-z\s\-]/i.test(ingredient.name) ) { unknownTerms.push(ingredient.name); } } } // Extract techniques if (recipe.techniques) { for (const technique of recipe.techniques) { knowledgeState.techniquesLearned.add(technique); } } // Update flavor profile knowledge if (recipe.flavor) { knowledgeState.flavorProfilesAnalyzed++; } // Add unknown terms to tracking unknownTerms.forEach((term) => knowledgeState.unknownTermsIdentified.add(term), ); if (connection) { // Always send recipe progress sendEvent(connection, { type:"recipe", timestamp: Date.now(), data: { currentUrl: recipe.url, currentRecipe: recipe.title, recipesProcessed: i + 1, totalRecipes: crawledRecipes.length, message: `Processing: ${recipe.title}`, }, }); // Send knowledge update frequently if ( (i + 1) % 5 === 0 || unknownTerms.length > 0 || i === crawledRecipes.length - 1 ) { sendEvent(connection, { type:"knowledge", timestamp: Date.now(), data: { currentRecipe: recipe.title, ingredientsFound: Array.from( knowledgeState.ingredientsLearned, ).slice(-10), techniqueFound: Array.from( knowledgeState.techniquesLearned, ).slice(-5), knowledgeUpdates: { ingredientsTaught: knowledgeState.ingredientsLearned.size, techniquesLearned: knowledgeState.techniquesLearned.size, flavorProfilesAnalyzed: knowledgeState.flavorProfilesAnalyzed, unknownTermsIdentified: Array.from( knowledgeState.unknownTermsIdentified, ), }, }, }); } } } // Stage 3: Auto-Learning - Query unknown terms via OpenAI const unknownTermsList = Array.from(knowledgeState.unknownTermsIdentified); let learningStats = { successful: 0, failed: 0 }; if (unknownTermsList.length > 0 && connection) { // Initialize learning session knowledgeUpdater.createSession(sessionId,"crawler"); // Send learning start message sendEvent(connection, { type:"learning", timestamp: Date.now(), data: { message: `🧠 Starting auto-learning: Enriching ${unknownTermsList.length} unknown ingredients/terms...`, termsBeingLearned: unknownTermsList.slice(0, 10), }, }); try { // Enrich terms using LLM (with rate limiting) const enrichmentResults = await llmKnowledgeEnricher.enrichTerms( unknownTermsList, 3, // Max 3 concurrent requests ); if (enrichmentResults.length > 0) { // Extract knowledge items from enrichment results const knowledgeItems = enrichmentResults.map( (result) => result.knowledge, ); // Send progress during storage connection = getConnection(); if (connection) { sendEvent(connection, { type:"learning", timestamp: Date.now(), data: { message: `📚 Storing ${knowledgeItems.length} learned concepts...`, termsLearned: enrichmentResults.length, }, }); } // Store enriched knowledge learningStats = await knowledgeUpdater.storeEnrichedKnowledge( sessionId, knowledgeItems, ); // Send learning completion stats connection = getConnection(); if (connection) { sendEvent(connection, { type:"learning", timestamp: Date.now(), data: { message: `✅ Auto-learning complete: ${learningStats.successful} concepts learned, ${learningStats.failed} failed`, termsLearned: learningStats.successful, termsFailedToLearn: learningStats.failed, }, }); } } } catch (error) { console.error("Auto-learning failed:", error); connection = getConnection(); if (connection) { sendEvent(connection, { type:"learning", timestamp: Date.now(), data: { message: `⚠️ Auto-learning encountered an error: ${error instanceof Error ? error.message :"Unknown error"}`, termsFailedToLearn: unknownTermsList.length, }, }); } } // Complete the learning session knowledgeUpdater.completeSession(sessionId); } // Final report connection = getConnection(); if (connection) { const sessionSummary = knowledgeUpdater.getSessionStats(sessionId); sendEvent(connection, { type:"complete", timestamp: Date.now(), data: { message:"🎉 Crawl and auto-learning complete!", knowledgeUpdates: { ingredientsTaught: knowledgeState.ingredientsLearned.size, techniquesLearned: knowledgeState.techniquesLearned.size, flavorProfilesAnalyzed: knowledgeState.flavorProfilesAnalyzed, unknownTermsIdentified: Array.from( knowledgeState.unknownTermsIdentified, ), autoLearningComplete: true, autoLearningStats: { successful: learningStats.successful, failed: learningStats.failed, }, }, }, }); } } catch (error) { connection = getConnection(); if (connection) { sendEvent(connection, { type:"error", timestamp: Date.now(), data: { error: error instanceof Error ? error.message :"Unknown error occurred", message:"Crawler failed", }, }); } }
} /** * Phase 4: Global crawling function with multi-source support */
async function crawlGlobalAndReportProgress( sessionId: string, options: { maxRecipes: number; cuisines: string[]; sources: string[]; extractFlavorData: boolean; autoLearn: boolean; },
) { const knowledgeState = { flavorProfilesAnalyzed: 0, unknownTermsIdentified: new Set<string>(), ingredientsLearned: new Set<string>(), techniquesLearned: new Set<string>(), sourcesUsed: new Set<string>(), }; // Helper to get current connection (handles race condition) const getConnection = () => activeConnections.get(sessionId); try { let connection = getConnection(); if (connection) { sendEvent(connection, { type:"recipe", timestamp: Date.now(), data: { message:"🌍 Starting Phase 4 Global Crawler - Multi-source learning in progress...", }, }); } // Stage 1: Crawl from all active sources globally const crawlerOptions = { query:"*", limit: options.maxRecipes, extractFlavorData: options.extractFlavorData, }; const activeAdapters = globalCrawlerManager .getAllAdapters() .filter((a) => a.isActive); console.log("[Crawler] Starting global crawl with options:", crawlerOptions, ); console.log("[Crawler] Active adapters:", activeAdapters.map((a) => a.name).join(","), ); console.log(`[Crawler] Total active adapters: ${activeAdapters.length}`); const crawlStartTime = Date.now(); const { recipes: crawledRecipes, flavorMatrix } = await globalCrawlerManager.crawlGlobal(crawlerOptions, 5); const crawlDuration = Date.now() - crawlStartTime; console.log( `[Crawler] ✓ Crawl completed in ${crawlDuration}ms: ${crawledRecipes.length} recipes found`, ); connection = getConnection(); if (connection) { sendEvent(connection, { type:"recipe", timestamp: Date.now(), data: { message: `✅ Crawled ${crawledRecipes.length} recipes from ${globalCrawlerManager.getAllAdapters().filter((a) => a.isActive).length} global sources`, recipesProcessed: 0, totalRecipes: crawledRecipes.length, }, }); } // Stage 2: Process recipes and extract flavor data let flavorCount = 0; for (let i = 0; i < crawledRecipes.length; i++) { const recipe = crawledRecipes[i]; knowledgeState.sourcesUsed.add(recipe.source); // Extract ingredients and techniques if (recipe.ingredients) { for (const ingredient of recipe.ingredients) { knowledgeState.ingredientsLearned.add(ingredient.name); if ( ingredient.name.length > 20 || /[^a-z\s\-]/i.test(ingredient.name) ) { knowledgeState.unknownTermsIdentified.add(ingredient.name); } } } if (recipe.techniques) { for (const technique of recipe.techniques) { knowledgeState.techniquesLearned.add(technique); } } if (recipe.flavor) { flavorCount++; } // Update flavor count knowledgeState.flavorProfilesAnalyzed = flavorCount; // Send progress update connection = getConnection(); if (connection) { const message = `Processing: ${recipe.title} (${recipe.source})`; // Always send recipe progress sendEvent(connection, { type:"recipe", timestamp: Date.now(), data: { currentRecipe: recipe.title, currentUrl: recipe.url, recipesProcessed: i + 1, totalRecipes: crawledRecipes.length, message, }, }); // Send knowledge update every 5 recipes if ((i + 1) % 5 === 0 || i === crawledRecipes.length - 1) { const ingredientCount = knowledgeState.ingredientsLearned.size; const techniqueCount = knowledgeState.techniquesLearned.size; sendEvent(connection, { type:"knowledge", timestamp: Date.now(), data: { message: `Learned: ${ingredientCount} ingredients, ${techniqueCount} techniques, ${knowledgeState.flavorProfilesAnalyzed} flavor profiles`, knowledgeUpdates: { ingredientsTaught: ingredientCount, techniquesLearned: techniqueCount, flavorProfilesAnalyzed: knowledgeState.flavorProfilesAnalyzed, unknownTermsIdentified: Array.from( knowledgeState.unknownTermsIdentified, ), }, }, }); } } } // Stage 3: Store flavor matrix entries if (options.extractFlavorData && flavorMatrix.length > 0) { connection = getConnection(); if (connection) { sendEvent(connection, { type:"knowledge", timestamp: Date.now(), data: { message: `📊 Building global flavor matrix from ${flavorMatrix.length} recipes...`, }, }); } const { stored, failed } = await flavorMatrixService.storeEntries(flavorMatrix); connection = getConnection(); if (connection) { sendEvent(connection, { type:"knowledge", timestamp: Date.now(), data: { message: `✅ Flavor matrix updated: ${stored} recipes analyzed, ${failed} failed`, }, }); } } // Stage 4: Auto-Learning if (options.autoLearn) { const unknownTermsList = Array.from( knowledgeState.unknownTermsIdentified, ); let learningStats = { successful: 0, failed: 0 }; if (unknownTermsList.length > 0) { knowledgeUpdater.createSession(sessionId,"crawler"); connection = getConnection(); if (connection) { sendEvent(connection, { type:"learning", timestamp: Date.now(), data: { message: `🧠 Auto-Learning: Enriching ${unknownTermsList.length} unknown terms from global recipes...`, termsBeingLearned: unknownTermsList.slice(0, 10), }, }); } try { const enrichmentResults = await llmKnowledgeEnricher.enrichTerms( unknownTermsList, 3, ); if (enrichmentResults.length > 0) { const knowledgeItems = enrichmentResults.map( (result) => result.knowledge, ); connection = getConnection(); if (connection) { sendEvent(connection, { type:"learning", timestamp: Date.now(), data: { message: `📚 Storing ${knowledgeItems.length} globally-sourced concepts...`, termsLearned: enrichmentResults.length, }, }); } // Directly store knowledge items to Pinecone with retry logic console.log( `[Crawler] Storing ${knowledgeItems.length} knowledge items to Pinecone...`, ); learningStats = { successful: 0, failed: 0 }; for (const item of knowledgeItems) { try { // Ensure knowledge item has required fields const knowledgeToStore: AnyKnowledge = { ...item, id: item.id || `crawler-${Date.now()}-${Math.random()}`, type: item.type ||"ingredient", title: item.title ||"Unknown Term", description: item.description ||"", domain: item.domain ||"culinary", sourceType:"openai" as const, tags: item.tags || [], createdAt: item.createdAt || new Date().toISOString(), }; // Store directly to Pinecone await storeKnowledgeVector(knowledgeToStore); learningStats.successful++; console.log( `[Crawler] ✅ Stored knowledge: ${knowledgeToStore.title}`, ); } catch (itemError) { console.error(`[Crawler] Failed to store item:`, itemError); learningStats.failed++; } } connection = getConnection(); if (connection) { sendEvent(connection, { type:"learning", timestamp: Date.now(), data: { message: `✅ Global learning complete: ${learningStats.successful} concepts learned`, termsLearned: learningStats.successful, termsFailedToLearn: learningStats.failed, }, }); } } } catch (error) { console.error("Global auto-learning failed:", error); } knowledgeUpdater.completeSession(sessionId); } } // Final report connection = getConnection(); if (connection) { const flavorMatrixStats = flavorMatrixService.getStatistics(); sendEvent(connection, { type:"complete", timestamp: Date.now(), data: { message: `🎉 Phase 4 Global Crawl Complete! 🌍`, knowledgeUpdates: { ingredientsTaught: knowledgeState.ingredientsLearned.size, techniquesLearned: knowledgeState.techniquesLearned.size, flavorProfilesAnalyzed: knowledgeState.flavorProfilesAnalyzed, unknownTermsIdentified: Array.from( knowledgeState.unknownTermsIdentified, ), sourcesUsed: Array.from(knowledgeState.sourcesUsed), flavorMatrixStats: { totalRecipes: flavorMatrixStats.totalRecipes, totalCuisines: flavorMatrixStats.totalCuisines, totalIngredients: flavorMatrixStats.totalIngredients, totalTechniques: flavorMatrixStats.totalTechniques, }, }, }, }); } } catch (error) { connection = getConnection(); if (connection) { sendEvent(connection, { type:"error", timestamp: Date.now(), data: { error: error instanceof Error ? error.message :"Unknown error occurred", message:"Global crawler failed", }, }); } console.error("Global crawling error:", error); }
} /** * Helper function to send SSE events */
function sendEvent(connection: Response, event: CrawlerProgressEvent) { try { if (!connection.writableEnded && !connection.destroyed) { const data = JSON.stringify(event); connection.write(`data: ${data}\n\n`); } } catch (e) { console.error("Error sending SSE event:", e); // Connection may be closed }
} /** * GET /api/echo/crawler/stats * Get current crawler statistics */
export async function getCrawlerStats(req: Request, res: Response) { const stats = { activeConnections: activeConnections.size, timestamp: Date.now(), }; res.json(stats);
} /** * GET /api/echo/crawler/flavor-matrix * Get global flavor matrix data */
export async function getFlavorMatrix(req: Request, res: Response) { const flavorMatrix = flavorMatrixService.getMatrix(); const statistics = flavorMatrixService.getStatistics(); res.json({ matrix: flavorMatrix, statistics, topCuisines: flavorMatrixService.getTopCuisines(10), topSensoryDescriptors: flavorMatrixService.getTopSensoryDescriptors(15), topIngredients: flavorMatrixService.getMostDocumentedIngredients(15), topTechniques: flavorMatrixService.getMostDocumentedTechniques(10), });
} /** * GET /api/echo/crawler/ingredient-flavor/:ingredient * Get flavor profile for a specific ingredient */
export async function getIngredientFlavorProfile(req: Request, res: Response) { const { ingredient } = req.params; const profile = flavorMatrixService.getIngredientProfile(ingredient); const similar = flavorMatrixService.findSimilarIngredients(ingredient, 10); if (!profile) { return res.status(404).json({ error: `No flavor profile found for ingredient: ${ingredient}`, similar, }); } res.json({ ingredient, profile, similar, });
} /** * GET /api/echo/crawler/cuisine-flavor/:cuisine * Get flavor pattern for a specific cuisine */
export async function getCuisineFlavor(req: Request, res: Response) { const { cuisine } = req.params; const profile = flavorMatrixService.getCulturalFlavorPattern(cuisine); if (!profile) { return res.status(404).json({ error: `No flavor pattern found for cuisine: ${cuisine}`, availableCuisines: flavorMatrixService.getTopCuisines(20), }); } res.json({ cuisine, profile, });
}
