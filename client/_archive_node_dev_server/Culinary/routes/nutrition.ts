import type { Request, Response } from"express";
import { DENSITY_CUP_G, EACH_WEIGHT_G, INGREDIENT_SYNONYMS, NUTRITION_DATABASE, STOP_WORDS, UNIT_TO_GRAMS, resolveIngredientKey, type NutritionProfile,
} from"../../shared/nutrition"; const TBSP_PER_CUP = 16;
const TSP_PER_TBSP = 3; const COOKING_FALLBACKS: Array<{ pattern: RegExp; yieldFactor: number }> = [ { pattern: /fried|grill|roast|bake|sear|broil/, yieldFactor: 0.88 }, { pattern: /poach|boil|simmer|stew|steam|blanch/, yieldFactor: 0.95 }, { pattern: /.*/, yieldFactor: 0.92 },
]; const ZERO_MACROS = () => ({ calories: 0, fat: 0, saturatedFat: 0, transFat: 0, carbs: 0, fiber: 0, sugars: 0, protein: 0, sodium: 0,
}); type MacroTotals = ReturnType<typeof ZERO_MACROS>; type ParsedIngredient = { quantity: number; unit: string; item: string; prep: string;
}; type BreakdownRow = { original: string; normalized: string; matchKey: string | null; confidence: number; grams: number; rawGrams: number; yieldFactor: number; macros: MacroTotals;
}; type UnknownRow = { original: string; suggestion: string;
}; const unicodeFractions: Record<string, string> = {"¼":"1/4","½":"1/2","¾":"3/4","⅐":"1/7","⅑":"1/9","⅒":"1/10","⅓":"1/3","⅔":"2/3","⅕":"1/5","⅖":"2/5","⅗":"3/5","⅘":"4/5","⅙":"1/6","⅚":"5/6","⅛":"1/8","⅜":"3/8","⅝":"5/8","⅞":"7/8",
}; function parseIngredientLine(line: string): ParsedIngredient { let text = line.trim().replace(/[¼½¾⅐⅑⅒⅓⅔⅕⅖⅗⅘⅙⅚⅛⅜⅝⅞]/g, (c) => unicodeFractions[c] ?? c); text = text.replace(/(\d)(\s*)(\d\/\d)/g,"$1 $3"); const match = text.match( /^\s*([0-9]+(?:\.[0-9]+)?(?:\s+[0-9]+\/\d+)?|[0-9]+\/\d+)\s*([a-zA-Z\.\-]+)?\s*(.*)$/, // quantity + optional unit + remainder ); if (match) { const [_, quantityRaw, unitRaw ="", remainder] = match; let quantity = 0; const parts = quantityRaw.split(""); if (parts.length === 2 && /\d+\/\d+/.test(parts[1])) { const [whole, fraction] = parts; const [num, denom] = fraction.split("/").map(Number); quantity = Number(whole) + (denom ? num / denom : 0); } else if (/\d+\/\d+/.test(quantityRaw)) { const [num, denom] = quantityRaw.split("/").map(Number); quantity = denom ? num / denom : Number(quantityRaw); } else { quantity = Number(quantityRaw); } const unit = (unitRaw ||"").toLowerCase(); let item = remainder.trim(); let prep =""; const commaIndex = item.indexOf(","); if (commaIndex >= 0) { prep = item.slice(commaIndex + 1).trim().toLowerCase(); item = item.slice(0, commaIndex).trim(); } const leadPrep = /^(chopped|diced|minced|sliced|grated|crushed|pureed|melted|softened|cubed|julienned|shredded|mashed)\s+(.*)$/i; const leadMatch = item.match(leadPrep); if (leadMatch) { prep = prep || leadMatch[1].toLowerCase(); item = leadMatch[2].trim(); } return { quantity, unit: unit || (quantity > 0 ?"each" : unit), item, prep, }; } const parts = line.split(","); if (parts.length > 1) { return { quantity: 1, unit:"each", item: parts[0]!.trim(), prep: parts.slice(1).join(",").trim().toLowerCase(), }; } return { quantity: 1, unit:"each", item: line.trim(), prep:"", };
} const ALIAS_ORDER = INGREDIENT_SYNONYMS.map(([, key]) => key); function estimateGrams(matchKey: string | null, unit: string, quantity: number): number { const lowerUnit = unit.toLowerCase(); if (!Number.isFinite(quantity) || quantity <= 0) return 0; const direct = UNIT_TO_GRAMS[lowerUnit as keyof typeof UNIT_TO_GRAMS]; if (direct && direct > 0) { return quantity * direct; } if (matchKey) { if ((lowerUnit ==="cup" || lowerUnit ==="cups") && DENSITY_CUP_G[matchKey]) { return quantity * DENSITY_CUP_G[matchKey]; } if ( (lowerUnit ==="tbsp" || lowerUnit ==="tablespoon" || lowerUnit ==="tablespoons" || lowerUnit ==="tbsp.") && DENSITY_CUP_G[matchKey] ) { return quantity * (DENSITY_CUP_G[matchKey] / TBSP_PER_CUP); } if ( (lowerUnit ==="tsp" || lowerUnit ==="teaspoon" || lowerUnit ==="teaspoons" || lowerUnit ==="tsp.") && DENSITY_CUP_G[matchKey] ) { return quantity * (DENSITY_CUP_G[matchKey] / (TBSP_PER_CUP * TSP_PER_TBSP)); } if ((lowerUnit ==="each" || lowerUnit ==="ea") && EACH_WEIGHT_G[matchKey]) { return quantity * EACH_WEIGHT_G[matchKey]; } } if (lowerUnit ==="each" || lowerUnit ==="ea") { return quantity * 30; // generic single unit fallback } if (lowerUnit ==="pinch" || lowerUnit ==="dash") { return quantity * 0.5; } if (lowerUnit ==="sprig" || lowerUnit ==="sprigs") { return quantity * 2; } if (lowerUnit ==="bunch" || lowerUnit ==="bunches") { return quantity * 85; } return quantity * 28.3495; // fallback to 1oz equivalent
} function multiplyProfile(profile: NutritionProfile, grams: number): MacroTotals { const factor = grams / 100; return { calories: profile.calories * factor, fat: profile.fat * factor, saturatedFat: profile.saturatedFat * factor, transFat: profile.transFat * factor, carbs: profile.carbs * factor, fiber: profile.fiber * factor, sugars: profile.sugars * factor, protein: profile.protein * factor, sodium: profile.sodium * factor, };
} function addTotals(target: MacroTotals, delta: MacroTotals) { target.calories += delta.calories; target.fat += delta.fat; target.saturatedFat += delta.saturatedFat; target.transFat += delta.transFat; target.carbs += delta.carbs; target.fiber += delta.fiber; target.sugars += delta.sugars; target.protein += delta.protein; target.sodium += delta.sodium;
} function scaleTotals(source: MacroTotals, factor: number): MacroTotals { if (!Number.isFinite(factor) || factor <= 0) return ZERO_MACROS(); return { calories: source.calories * factor, fat: source.fat * factor, saturatedFat: source.saturatedFat * factor, transFat: source.transFat * factor, carbs: source.carbs * factor, fiber: source.fiber * factor, sugars: source.sugars * factor, protein: source.protein * factor, sodium: source.sodium * factor, };
} function roundTotals(source: MacroTotals): MacroTotals { return { calories: Math.round(source.calories), fat: Number(source.fat.toFixed(2)), saturatedFat: Number(source.saturatedFat.toFixed(2)), transFat: Number(source.transFat.toFixed(2)), carbs: Number(source.carbs.toFixed(2)), fiber: Number(source.fiber.toFixed(2)), sugars: Number(source.sugars.toFixed(2)), protein: Number(source.protein.toFixed(2)), sodium: Math.round(source.sodium), };
} function computeYieldFactor(index: number, providedYields: (number | null)[], ingredientKey: string | null, fallback: number): number { const provided = providedYields[index]; if (typeof provided ==="number" && Number.isFinite(provided) && provided >= 0) { return Math.max(0, Math.min(1, provided / 100)); } if (ingredientKey && /salt|spice|pepper|powder/.test(ingredientKey)) { return 1; } return fallback;
} function inferFallbackYield(prepText: string): number { const normalized = prepText.toLowerCase(); for (const entry of COOKING_FALLBACKS) { if (entry.pattern.test(normalized)) return entry.yieldFactor; } return 0.92;
} export async function handleNutritionAnalyze(req: Request, res: Response) { try { const { ingr, yields = [], yieldQty = 1, yieldUnit ="SERVING", prepMethod ="", } = req.body as { ingr: string[]; yields?: (number | null)[]; yieldQty?: number; yieldUnit?: string; prepMethod?: string; }; if (!Array.isArray(ingr) || !ingr.length) { return res.status(400).json({ error:"No ingredients provided" }); } const fallbackYield = inferFallbackYield(prepMethod ||""); const totals = ZERO_MACROS(); let totalWeight = 0; let matchedWeight = 0; const breakdown: BreakdownRow[] = []; const unknown: UnknownRow[] = []; for (let index = 0; index < ingr.length; index++) { const original = ingr[index] ??""; const parsed = parseIngredientLine(original); const match = resolveIngredientKey(parsed.item); const matchKey = match.key; let gramsRaw = estimateGrams(matchKey, parsed.unit, parsed.quantity); const yieldFactor = computeYieldFactor(index, yields, matchKey, fallbackYield); const grams = gramsRaw * yieldFactor; totalWeight += grams; if (matchKey && NUTRITION_DATABASE[matchKey]) { matchedWeight += grams; const macros = multiplyProfile(NUTRITION_DATABASE[matchKey], grams); addTotals(totals, macros); breakdown.push({ original, normalized: matchKey, matchKey, confidence: match.confidence, grams, rawGrams: gramsRaw, yieldFactor, macros: roundTotals(macros), }); } else { breakdown.push({ original, normalized: match.normalized, matchKey: null, confidence: match.confidence, grams, rawGrams: gramsRaw, yieldFactor, macros: ZERO_MACROS(), }); unknown.push({ original, suggestion: suggestClosestMatch(match.normalized), }); } } const servings = Number.isFinite(yieldQty) && yieldQty > 0 ? yieldQty : 1; const perServing = scaleTotals(totals, 1 / servings); const per100g = totalWeight > 0 ? scaleTotals(totals, 100 / totalWeight) : ZERO_MACROS(); const totalNutrients = { ENERC_KCAL: { label:"Energy", quantity: totals.calories, unit:"kcal", }, FAT: { label:"Total Fat", quantity: totals.fat, unit:"g", }, FASAT: { label:"Saturated Fat", quantity: totals.saturatedFat, unit:"g", }, FATRN: { label:"Trans Fat", quantity: totals.transFat, unit:"g", }, CHOCDF: { label:"Total Carbohydrate", quantity: totals.carbs, unit:"g", }, FIBTG: { label:"Dietary Fiber", quantity: totals.fiber, unit:"g", }, SUGAR: { label:"Sugars", quantity: totals.sugars, unit:"g", }, PROCNT: { label:"Protein", quantity: totals.protein, unit:"g", }, NA: { label:"Sodium", quantity: totals.sodium, unit:"mg", }, }; const response = { calories: Math.round(totals.calories), totals: roundTotals(totals), perServing: roundTotals(perServing), per100g: roundTotals(per100g), totalNutrients, totalWeight, matchedWeight, coverage: totalWeight > 0 ? matchedWeight / totalWeight : 0, yieldQty: servings, yieldUnit, breakdown, unknown, }; res.json(response); } catch (error: any) { res.status(500).json({ error: error?.message ||"Nutrition analysis failed" }); }
} function suggestClosestMatch(normalized: string): string { if (!normalized) return""; const tokens = normalized .split(/\s+/) .map((token) => token.trim()) .filter((token) => token && !STOP_WORDS.has(token)); for (const token of tokens) { const aliasIndex = ALIAS_ORDER.findIndex((key) => key.includes(token)); if (aliasIndex >= 0) return ALIAS_ORDER[aliasIndex] ||""; } return tokens[0] || normalized;
}
