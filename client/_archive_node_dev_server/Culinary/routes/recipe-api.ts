/** * Recipe API Endpoints * Provides recipe data from multiple sources for knowledge crawler training */ import { Router, Request, Response } from"express";
import RecipeAPIIntegration from"../lib/recipe-api-integration"; const router = Router();
const recipeAPI = new RecipeAPIIntegration(); /** * POST /api/recipes/search * Search for recipes from multiple sources */
router.post("/search", async (req: Request, res: Response) => { try { const { query, sources, limit = 10 } = req.body as { query: string; sources?: string[]; limit?: number; }; if (!query) { return res.status(400).json({ error:"query is required" }); } const recipes = await recipeAPI.searchRecipesByQuery(query, { sources, limit, }); return res.json({ success: true, recipes, count: recipes.length, query, }); } catch (error: any) { console.error("[RecipeAPI] Search failed:", error); return res.status(500).json({ success: false, error: error.message ||"Failed to search recipes", }); }
}); /** * POST /api/recipes/nutrition * Search for nutritional data from USDA */
router.post("/nutrition", async (req: Request, res: Response) => { try { const { query, limit = 10 } = req.body as { query: string; limit?: number; }; if (!query) { return res.status(400).json({ error:"query is required" }); } const foods = await recipeAPI.searchByNutrition(query, limit); return res.json({ success: true, foods, count: foods.length, source:"USDA FoodData Central", query, }); } catch (error: any) { console.error("[RecipeAPI] Nutrition search failed:", error); return res.status(500).json({ success: false, error: error.message ||"Failed to search nutrition data", }); }
}); /** * POST /api/recipes/menus * Search for restaurant menus and dishes */
router.post("/menus", async (req: Request, res: Response) => { try { const { query, limit = 10 } = req.body as { query: string; limit?: number; }; if (!query) { return res.status(400).json({ error:"query is required" }); } // Simulated menu data extraction const menus = [ { restaurant:"Michelin-Starred Restaurant", dish: query, description: `Fine dining preparation of ${query}`, technique:"sous-vide, molecular gastronomy", ingredients: ["premium ingredients","seasonal produce"], }, { restaurant:"Casual Bistro", dish: query, description: `Home-style ${query} preparation`, technique:"traditional cooking methods", ingredients: ["fresh ingredients","classic combinations"], }, ]; return res.json({ success: true, menus, count: menus.length, query, }); } catch (error: any) { console.error("[RecipeAPI] Menu search failed:", error); return res.status(500).json({ success: false, error: error.message ||"Failed to search menus", }); }
}); /** * POST /api/recipes/videos * Search for cooking technique videos */
router.post("/videos", async (req: Request, res: Response) => { try { const { query, chefs = [], limit = 10 } = req.body as { query: string; chefs?: string[]; limit?: number; }; if (!query) { return res.status(400).json({ error:"query is required" }); } // Simulated video data const videos = [ { title: `How to ${query} - Master Class`, duration: 1200, techniques: ["cutting","cooking","plating"], difficulty:"intermediate", }, { title: `Professional ${query} Technique`, duration: 1800, techniques: ["advanced plating","temperature control"], difficulty:"advanced", }, ]; return res.json({ success: true, videos, count: videos.length, query, }); } catch (error: any) { console.error("[RecipeAPI] Video search failed:", error); return res.status(500).json({ success: false, error: error.message ||"Failed to search videos", }); }
}); /** * POST /api/recipes/blogs * Search for culinary blogs and techniques */
router.post("/blogs", async (req: Request, res: Response) => { try { const { domain, query, limit = 10 } = req.body as { domain: string; query: string; limit?: number; }; if (!query) { return res.status(400).json({ error:"query is required" }); } // Simulated blog data const posts = [ { title: `The Science of ${query}`, author:"Food Scientist", domain: domain ||"seriouseats.com", topics: ["technique","science","chemistry"], }, { title: `${query}: From Market to Table`, author:"Chef & Food Writer", domain: domain ||"bonappetitmag.com", topics: ["sourcing","technique","flavor"], }, ]; return res.json({ success: true, posts, count: posts.length, query, }); } catch (error: any) { console.error("[RecipeAPI] Blog search failed:", error); return res.status(500).json({ success: false, error: error.message ||"Failed to search blogs", }); }
}); /** * POST /api/recipes/suppliers * Search for ingredient suppliers and products */
router.post("/suppliers", async (req: Request, res: Response) => { try { const { domain, query, limit = 10 } = req.body as { domain: string; query: string; limit?: number; }; if (!query) { return res.status(400).json({ error:"query is required" }); } // Simulated supplier data const products = [ { name: `Premium ${query}`, supplier:"Fine Foods Inc.", price: 25.99, quality:"A-Grade", origin:"Artisanal Source", description: `Hand-selected ${query} from sustainable sources`, }, { name: `Organic ${query}`, supplier:"Farm Direct", price: 18.99, quality:"Organic Certified", origin:"Local Farm", description: `Certified organic ${query} harvested at peak ripeness`, }, ]; return res.json({ success: true, products, count: products.length, query, }); } catch (error: any) { console.error("[RecipeAPI] Supplier search failed:", error); return res.status(500).json({ success: false, error: error.message ||"Failed to search suppliers", }); }
}); /** * POST /api/recipes/analyze * Analyze a recipe for balance, texture, flavor profile */
router.post("/analyze", async (req: Request, res: Response) => { try { const { recipe } = req.body as { recipe: any }; if (!recipe) { return res.status(400).json({ error:"recipe is required" }); } const analyzer = recipeAPI.getAnalyzer(); const analysis = { balance: analyzer.analyzeBalance(recipe), texture: analyzer.analyzeTexture(recipe), flavor: analyzer.analyzeFlavorProfile(recipe), }; return res.json({ success: true, analysis, recipe: recipe.title, }); } catch (error: any) { console.error("[RecipeAPI] Analysis failed:", error); return res.status(500).json({ success: false, error: error.message ||"Failed to analyze recipe", }); }
}); /** * POST /api/recipes/culinary-search * Search for recipes by cuisine and technique */
router.post("/culinary-search", async (req: Request, res: Response) => { try { const { cuisine, technique, limit = 5 } = req.body as { cuisine: string; technique: string; limit?: number; }; if (!cuisine || !technique) { return res.status(400).json({ error:"cuisine and technique are required", }); } const recipes = await recipeAPI.searchCulinaryRecipes( cuisine, technique, limit, ); return res.json({ success: true, recipes, count: recipes.length, cuisine, technique, }); } catch (error: any) { console.error("[RecipeAPI] Culinary search failed:", error); return res.status(500).json({ success: false, error: error.message ||"Failed to search culinary recipes", }); }
}); export const recipeAPIRouter = router;
