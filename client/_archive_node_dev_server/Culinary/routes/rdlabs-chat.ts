import { Router } from "express";

const router = Router();

// TODO: Fix syntax error in this file (temporarily disabled)
// The file had a malformed try-catch structure that prevented backend from starting

export const rdLabsChatRouter = router;
export default router;
