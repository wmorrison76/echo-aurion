/** * PDF Library Import API * Handles PDF file uploads, text extraction, and integration with Echo's master culinary dictionary */ import type { Request, Response } from"express";
import { Router } from"express";
import multer from"multer";
import { masterCulinaryDictionary } from"../lib/master-culinary-dictionary";
import { processPDFUpload, processPDFBatch, calculateImportStats, extractTextFromPDFBuffer, extractPDFMetadata,
} from"../lib/pdf-upload-handler";
import { convertPDFToMasterTerms } from"../lib/pdf-knowledge-extractor";
import type { PDFMetadata } from"../lib/pdf-knowledge-extractor"; export const pdfLibraryImportRouter = Router(); // Configure multer for in-memory PDF storage
const upload = multer({ storage: multer.memoryStorage(), limits: { fileSize: 50 * 1024 * 1024 }, // 50MB limit per file fileFilter: (req, file, cb) => { if (file.mimetype ==="application/pdf") { cb(null, true); } else { cb(new Error("Only PDF files allowed")); } },
}); /** * POST /api/pdf-library/upload-multipart * Accept multipart PDF upload (alternative to base64) * Use FormData with 'pdf' field to upload * Optional fields in FormData: * - title: Book title (optional) * - author: Book author (optional) * - cuisine: Cuisine type (optional) * - language: Language (optional, default: English) */
export async function uploadPDFMultipart(req: Request, res: Response) { try { if (!req.file) { return res.status(400).json({ status:"error", message:"No PDF file provided", instruction:"Send multipart form with 'pdf' field containing the PDF file", }); } const buffer = req.file.buffer; const fileName = req.file.originalname; const { title, author, cuisine, language } = req.body; console.log( `[PDF Import] Processing multipart upload: ${fileName} (${buffer.length} bytes)`, ); // Extract text from PDF let pdfText: string; try { pdfText = await extractTextFromPDFBuffer(buffer, fileName); } catch (error) { const errorMessage = error instanceof Error ? error.message :"Unknown error"; console.error(`[PDF Import] Text extraction failed: ${errorMessage}`); if ( errorMessage.includes("no readable text") || errorMessage.includes("empty") ) { return res.status(400).json({ status:"error", error:"PDF has no readable text", recommendation:"The PDF appears to be image-based. Try using OCR mode or ensure the PDF contains selectable text.", fileName, }); } return res.status(400).json({ status:"error", error: `Failed to extract text from PDF: ${errorMessage}`, fileName, }); } if (pdfText.trim().length < 50) { return res.status(400).json({ status:"error", error:"PDF extraction returned insufficient text", recommendation:"The extracted text is too small. Try a different PDF or use OCR for scanned documents.", fileName, extractedLength: pdfText.length, }); } // Create metadata const metadata: PDFMetadata = { title: title || extractPDFMetadata(fileName, pdfText).title, author: author || undefined, cuisine: cuisine || undefined, language: language ||"English", specialization:"culinary-book", }; console.log(`[PDF Import] Extracted metadata: ${JSON.stringify(metadata)}`); // Convert to master culinary terms const extraction = convertPDFToMasterTerms(pdfText, metadata); console.log( `[PDF Import] Conversion complete: ${extraction.terms.length} terms identified`, ); // Add all extracted terms to master dictionary let addedCount = 0; const failedTerms: string[] = []; for (const term of extraction.terms) { try { masterCulinaryDictionary.addTerm(term.term.toLowerCase(), term); addedCount++; } catch (error) { failedTerms.push(term.term); console.error(`[PDF Import] Failed to add term"${term.term}":`, error); } } const stats = masterCulinaryDictionary.getStatistics(); console.log( `[PDF Import] Complete: Added ${addedCount}/${extraction.terms.length} terms`, ); res.json({ status:"success", import: { file: fileName, source: metadata.title, author: metadata.author, cuisine: metadata.cuisine, language: metadata.language, termsExtracted: extraction.terms.length, termsAdded: addedCount, textExtracted: pdfText.length, averageConfidence: extraction.metadata.confidence, failedTerms: failedTerms.length > 0 ? failedTerms : undefined, timestamp: new Date().toISOString(), }, dictionaryUpdate: { totalTerms: stats.totalTerms, categories: stats.categories, message: `🎓 Echo learned ${addedCount} culinary terms from"${metadata.title}"!`, }, masteryBreakdown: { fundamental: stats.masteryLevels.fundamental || 0, intermediate: stats.masteryLevels.intermediate || 0, advanced: stats.masteryLevels.advanced || 0, expert: stats.masteryLevels.expert || 0, master: stats.masteryLevels.master || 0, }, }); } catch (error) { console.error("[PDF Import] Multipart upload error:", error); return res.status(500).json({ status:"error", error: error instanceof Error ? error.message :"Upload failed", message:"Failed to process PDF upload", }); }
} /** * POST /api/pdf-library/upload * Upload a single PDF file and import knowledge * Expects JSON body with: * - pdfBase64: Base64-encoded PDF file content (required) * - pdfName: Original PDF filename (required) * - title: (optional) Book title override * - author: (optional) Book author * - cuisine: (optional) Cuisine type * - publicationYear: (optional) Publication year */
export async function uploadPDFFile(req: Request, res: Response) { try { const { pdfBase64, pdfName, title, author, cuisine, publicationYear, language, } = req.body; if (!pdfBase64 || !pdfName) { return res.status(400).json({ status:"error", message:"PDF base64 content and filename are required", example: { pdfBase64:"JVBERi0xLjQK...", pdfName:"food-lovers-companion.pdf", title:"The Food Lovers Companion", }, }); } // Convert base64 to buffer let pdfBuffer: Buffer; try { pdfBuffer = Buffer.from(pdfBase64,"base64"); } catch (error) { return res.status(400).json({ status:"error", message:"Invalid base64 encoding for PDF", }); } // Extract text from PDF let pdfText: string; try { pdfText = await extractTextFromPDFBuffer(pdfBuffer, pdfName); } catch (error) { return res.status(400).json({ status:"error", message: `Failed to extract text from PDF: ${error instanceof Error ? error.message :"Unknown error"}`, suggestion:"The PDF might be image-based or encrypted. Ensure it contains selectable text.", pdfName, }); } // Extract or create metadata const metadata: PDFMetadata = { title: title || extractPDFMetadata(pdfName, pdfText).title, author, publicationYear: publicationYear ? parseInt(publicationYear, 10) : undefined, language: language ||"English", cuisine, specialization:"culinary-book", }; // Convert PDF to master culinary terms const extraction = convertPDFToMasterTerms(pdfText, metadata); // Add all extracted terms to master dictionary let addedCount = 0; const failedTerms: string[] = []; for (const term of extraction.terms) { try { masterCulinaryDictionary.addTerm(term.term.toLowerCase(), term); addedCount++; } catch (error) { failedTerms.push(term.term); console.error(`Failed to add term"${term.term}":`, error); } } const stats = masterCulinaryDictionary.getStatistics(); res.json({ status:"success", import: { file: pdfName, source: metadata.title, author: metadata.author, cuisine: metadata.cuisine, termsExtracted: extraction.terms.length, termsAdded: addedCount, textExtracted: pdfText.length, averageConfidence: extraction.metadata.confidence, failedTerms: failedTerms.length > 0 ? failedTerms : undefined, timestamp: new Date().toISOString(), }, dictionaryUpdate: { totalTerms: stats.totalTerms, categories: stats.categories, message: `🎓 Echo learned ${addedCount} culinary terms from"${metadata.title}"!`, }, masteryBreakdown: { fundamental: stats.masteryLevels.fundamental || 0, intermediate: stats.masteryLevels.intermediate || 0, advanced: stats.masteryLevels.advanced || 0, expert: stats.masteryLevels.expert || 0, master: stats.masteryLevels.master || 0, }, }); } catch (error) { console.error("Error uploading PDF file:", error); res.status(500).json({ status:"error", message:"Failed to upload and process PDF", error: error instanceof Error ? error.message :"Unknown error", }); }
} /** * POST /api/pdf-library/upload-batch * Upload multiple PDF files at once * Expects JSON body with: * - pdfs: Array of { pdfBase64, pdfName, title?, author?, cuisine? } */
export async function uploadPDFBatch(req: Request, res: Response) { try { const { pdfs } = req.body as { pdfs: Array<{ pdfBase64: string; pdfName: string; title?: string; author?: string; cuisine?: string; }>; }; if (!Array.isArray(pdfs) || pdfs.length === 0) { return res.status(400).json({ status:"error", message:"Array of PDFs required", example: { pdfs: [ { pdfBase64:"JVBERi0xLjQK...", pdfName:"book1.pdf", title:"Book Title", }, ], }, }); } const importResults = []; let totalAdded = 0; let totalExtracted = 0; const failedFiles = []; // Process each PDF for (const pdfData of pdfs) { try { if (!pdfData.pdfBase64 || !pdfData.pdfName) { failedFiles.push({ file: pdfData.pdfName ||"unknown", error:"Missing pdfBase64 or pdfName", }); continue; } // Convert base64 to buffer const pdfBuffer = Buffer.from(pdfData.pdfBase64,"base64"); // Extract text from PDF const pdfText = await extractTextFromPDFBuffer( pdfBuffer, pdfData.pdfName, ); // Create metadata const metadata: PDFMetadata = { title: pdfData.title || extractPDFMetadata(pdfData.pdfName, pdfText).title, author: pdfData.author, cuisine: pdfData.cuisine, language:"English", specialization:"culinary-book", }; // Convert to master culinary terms const extraction = convertPDFToMasterTerms(pdfText, metadata); totalExtracted += extraction.terms.length; // Add to dictionary let addedCount = 0; for (const term of extraction.terms) { try { masterCulinaryDictionary.addTerm(term.term.toLowerCase(), term); addedCount++; } catch (error) { console.error(`Failed to add term"${term.term}":`, error); } } totalAdded += addedCount; importResults.push({ file: pdfData.pdfName, source: metadata.title, termsExtracted: extraction.terms.length, termsAdded: addedCount, confidence: extraction.metadata.confidence, }); } catch (error) { failedFiles.push({ file: pdfData.pdfName, error: error instanceof Error ? error.message :"Unknown error", }); } } const stats = masterCulinaryDictionary.getStatistics(); res.json({ status:"success", import: { totalFiles: pdfs.length, successfulFiles: importResults.length, failedFiles: failedFiles.length, totalTermsExtracted, totalTermsAdded, timestamp: new Date().toISOString(), }, results: importResults, errors: failedFiles.length > 0 ? failedFiles : undefined, dictionaryUpdate: { totalTerms: stats.totalTerms, categories: stats.categories, message: `📚 Echo imported ${totalAdded} culinary terms from ${importResults.length} PDFs!`, }, masteryBreakdown: { fundamental: stats.masteryLevels.fundamental || 0, intermediate: stats.masteryLevels.intermediate || 0, advanced: stats.masteryLevels.advanced || 0, expert: stats.masteryLevels.expert || 0, master: stats.masteryLevels.master || 0, }, }); } catch (error) { console.error("Error uploading PDF batch:", error); res.status(500).json({ status:"error", message:"Failed to process PDF batch", error: error instanceof Error ? error.message :"Unknown error", }); }
} /** * GET /api/pdf-library/import-status * Get the current status of PDF library imports and Echo's knowledge */
export async function getPDFImportStatus(req: Request, res: Response) { try { const stats = masterCulinaryDictionary.getStatistics(); const masteryPercentages = { fundamental: Math.round( ((stats.masteryLevels.fundamental || 0) / stats.totalTerms) * 100, ), intermediate: Math.round( ((stats.masteryLevels.intermediate || 0) / stats.totalTerms) * 100, ), advanced: Math.round( ((stats.masteryLevels.advanced || 0) / stats.totalTerms) * 100, ), expert: Math.round( ((stats.masteryLevels.expert || 0) / stats.totalTerms) * 100, ), master: Math.round( ((stats.masteryLevels.master || 0) / stats.totalTerms) * 100, ), }; const categoryBreakdown = Object.entries(stats.categories).map( ([category, count]) => ({ category, count, percentage: Math.round((count / stats.totalTerms) * 100), }), ); res.json({ status:"success", library: { totalTermsImported: stats.totalTerms, goalTerms: 10000, progressPercentage: Math.round((stats.totalTerms / 10000) * 100), completionStatus: stats.totalTerms >= 10000 ?"✓ Complete" : `${stats.totalTerms} / 10,000 terms`, }, masteryLevels: { breakdown: masteryPercentages, levels: { fundamental: { count: stats.masteryLevels.fundamental || 0, description:"Essential cooking basics and foundational techniques", percentage: masteryPercentages.fundamental, }, intermediate: { count: stats.masteryLevels.intermediate || 0, description:"Professional cooking knowledge and standard methods", percentage: masteryPercentages.intermediate, }, advanced: { count: stats.masteryLevels.advanced || 0, description:"Specialized techniques and culinary theory", percentage: masteryPercentages.advanced, }, expert: { count: stats.masteryLevels.expert || 0, description:"Master-level knowledge and rare specializations", percentage: masteryPercentages.expert, }, master: { count: stats.masteryLevels.master || 0, description:"Authority-level understanding and culinary mastery", percentage: masteryPercentages.master, }, }, }, categoryBreakdown: categoryBreakdown.sort((a, b) => b.count - a.count), averageConfidence: stats.averageConfidence.toFixed(2), capabilities: { search:"✓ Search all terms and definitions", relatedTerms:"✓ Find related terms and concepts", applications:"✓ Learn applications and usage context", etymology:"✓ Understand term origins and history", masterPaths:"✓ Follow mastery learning paths", }, message: `🍽️ Echo's master culinary dictionary: ${stats.totalTerms} authoritative terms ready for culinary excellence!`, }); } catch (error) { res.status(500).json({ status:"error", message:"Failed to get PDF import status", error: error instanceof Error ? error.message :"Unknown error", }); }
} /** * POST /api/pdf-library/import-from-text * Import knowledge directly from PDF text (without file upload) * Useful for when text is already extracted via OCR or other methods * Body: { pdfText: string, metadata: PDFMetadata } */
export async function importFromText(req: Request, res: Response) { try { const { pdfText, metadata } = req.body; if (!pdfText || !metadata || !metadata.title) { return res.status(400).json({ status:"error", message:"PDF text and metadata with title are required", }); } const defaultMetadata: PDFMetadata = { title: metadata.title, author: metadata.author, publicationYear: metadata.publicationYear, language: metadata.language ||"English", cuisine: metadata.cuisine, specialization: metadata.specialization ||"culinary-book", }; // Convert to master culinary terms const extraction = convertPDFToMasterTerms(pdfText, defaultMetadata); // Add to dictionary let addedCount = 0; for (const term of extraction.terms) { try { masterCulinaryDictionary.addTerm(term.term.toLowerCase(), term); addedCount++; } catch (error) { console.error(`Failed to add term"${term.term}":`, error); } } const stats = masterCulinaryDictionary.getStatistics(); res.json({ status:"success", import: { source: metadata.title, author: metadata.author, termsExtracted: extraction.terms.length, termsAdded: addedCount, textLength: pdfText.length, confidence: extraction.metadata.confidence, timestamp: new Date().toISOString(), }, dictionaryUpdate: { totalTerms: stats.totalTerms, message: `📚 Echo imported ${addedCount} terms from"${metadata.title}"!`, }, }); } catch (error) { console.error("Error importing from text:", error); res.status(500).json({ status:"error", message:"Failed to import knowledge from text", error: error instanceof Error ? error.message :"Unknown error", }); }
} /** * POST /api/pdf-library/debug * Debug PDF extraction issues * Expects JSON body with: * - pdfBase64: Base64-encoded PDF file content (required) * - pdfName: Original PDF filename (required) * - title: (optional) Book title */
export async function debugPDFExtraction(req: Request, res: Response) { try { const { pdfBase64, pdfName, title } = req.body; if (!pdfBase64 || !pdfName) { return res.status(400).json({ status:"error", message:"PDF base64 content and filename are required", }); } // Convert base64 to buffer let pdfBuffer: Buffer; try { pdfBuffer = Buffer.from(pdfBase64,"base64"); } catch (error) { return res.status(400).json({ status:"error", message:"Invalid base64 encoding for PDF", }); } // Step 1: Extract text let pdfText: string =""; let textExtractionError: string | null = null; try { pdfText = await extractTextFromPDFBuffer(pdfBuffer, pdfName); } catch (error) { textExtractionError = error instanceof Error ? error.message :"Unknown error"; } // Step 2: Extract definitions const metadata: PDFMetadata = { title: title || pdfName, language:"English", specialization:"culinary-book", }; let extraction = null; let extractionError: string | null = null; try { if (pdfText.length > 0) { extraction = convertPDFToMasterTerms(pdfText, metadata); } } catch (error) { extractionError = error instanceof Error ? error.message :"Unknown error"; } // Return detailed debug info res.json({ status:"debug", file: { filename: pdfName, sizeBytes: pdfBuffer.length, }, textExtraction: { success: textExtractionError === null, error: textExtractionError, textLength: pdfText.length, lineCount: pdfText.split("\n").length, preview: pdfText.substring(0, 500), sampleLines: pdfText .split("\n") .slice(0, 10) .map((line, i) => ({ lineNum: i + 1, content: line.substring(0, 100), length: line.length, })), }, definitionExtraction: { success: extractionError === null, error: extractionError, termsExtracted: extraction?.terms.length || 0, averageConfidence: extraction?.metadata.confidence || 0, sampleTerms: extraction?.terms.slice(0, 5).map((t) => ({ term: t.term, definition: t.definition.substring(0, 80), categories: t.categories, confidence: t.confidence, })) || [], }, recommendations: generateDebugRecommendations( textExtractionError, extractionError, pdfText.length, extraction?.terms.length || 0, ), }); } catch (error) { console.error("Error debugging PDF file:", error); res.status(500).json({ status:"error", message:"Failed to debug PDF", error: error instanceof Error ? error.message :"Unknown error", }); }
} /** * Generate debugging recommendations based on extraction results */
function generateDebugRecommendations( textError: string | null, extractionError: string | null, textLength: number, termCount: number,
): string[] { const recommendations: string[] = []; if (textError) { recommendations.push(`Text extraction failed: ${textError}`); recommendations.push("The PDF might be image-based or encrypted. Try converting it with OCR first.", ); } else if (textLength < 500) { recommendations.push("Very little text was extracted (less than 500 chars). The PDF might be mostly images or have encoding issues.", ); } if (extractionError) { recommendations.push(`Definition extraction failed: ${extractionError}`); } else if (termCount === 0) { recommendations.push("No definitions were extracted. This might be because:", ); recommendations.push(" - The PDF format doesn't match expected glossary patterns", ); recommendations.push(" - Terms may be formatted differently than expected", ); recommendations.push(" - Try checking the PDF format and structure"); } else if (termCount < 10) { recommendations.push( `Only ${termCount} terms extracted. The glossary patterns might not match this PDF format well.`, ); } if (recommendations.length === 0) { recommendations.push("PDF extraction appears to be working correctly."); } return recommendations;
} /** * GET /api/pdf-library/definition/:term * Retrieve a specific culinary term definition from the master dictionary * Used to access definitions imported from books like"The Food Lover's Companion" */
export async function getTermDefinition(req: Request, res: Response) { try { const { term } = req.params; if (!term || term.trim().length === 0) { return res.status(400).json({ status:"error", message:"Term parameter is required", }); } const termDef = masterCulinaryDictionary.getTerm(term); if (!termDef) { return res.status(404).json({ status:"not_found", message: `Definition for"${term}" not found in master dictionary`, suggestion:"Try searching with searchTerms endpoint or check spelling", }); } res.json({ status:"success", definition: termDef, relatedTerms: masterCulinaryDictionary.getRelatedTerms(term), }); } catch (error) { console.error("Error retrieving term definition:", error); res.status(500).json({ status:"error", message:"Failed to retrieve term definition", error: error instanceof Error ? error.message :"Unknown error", }); }
} /** * GET /api/pdf-library/search * Search for culinary terms in the master dictionary * Query parameters: * - q: Search query (required) * - limit: Max results to return (default 10) */
export async function searchDefinitions(req: Request, res: Response) { try { const { q, limit ="10" } = req.query; if (!q || typeof q !=="string" || q.trim().length === 0) { return res.status(400).json({ status:"error", message:"Search query (q) parameter is required", example:"/api/pdf-library/search?q=blanch&limit=5", }); } const searchLimit = Math.min(parseInt(limit as string, 10) || 10, 100); const results = masterCulinaryDictionary .searchTerms(q) .slice(0, searchLimit); res.json({ status:"success", query: q, resultCount: results.length, results: results.map((term) => ({ term: term.term, definition: term.definition, categories: term.categories, confidence: term.confidence, masteryLevel: term.masteryLevel, })), }); } catch (error) { console.error("Error searching definitions:", error); res.status(500).json({ status:"error", message:"Failed to search definitions", error: error instanceof Error ? error.message :"Unknown error", }); }
} /** * GET /api/pdf-library/definitions/related/:term * Get terms related to a specific culinary term */
export async function getRelatedDefinitions(req: Request, res: Response) { try { const { term } = req.params; if (!term || term.trim().length === 0) { return res.status(400).json({ status:"error", message:"Term parameter is required", }); } const mainTerm = masterCulinaryDictionary.getTerm(term); if (!mainTerm) { return res.status(404).json({ status:"not_found", message: `Term"${term}" not found in master dictionary`, }); } const relatedTerms = masterCulinaryDictionary.getRelatedTerms(term); res.json({ status:"success", term: mainTerm.term, relatedTermCount: relatedTerms.length, relatedTerms: relatedTerms.map((t) => ({ term: t.term, definition: t.definition.substring(0, 150) +"...", categories: t.categories, })), }); } catch (error) { console.error("Error retrieving related definitions:", error); res.status(500).json({ status:"error", message:"Failed to retrieve related definitions", error: error instanceof Error ? error.message :"Unknown error", }); }
} /** * GET /api/pdf-library/definitions/browse * Browse all terms in a specific category * Query parameters: * - category: One of 'technique', 'ingredient', 'method', 'equipment', 'theory', 'cuisine', 'safety', 'service', 'tradition' * - limit: Max results (default 20, max 100) */
export async function browseDefinitionsByCategory(req: Request, res: Response) { try { const { category, limit ="20" } = req.query; if (!category || typeof category !=="string") { return res.status(400).json({ status:"error", message:"Category parameter is required", validCategories: ["technique","ingredient","method","equipment","theory","cuisine","safety","service","tradition", ], example:"/api/pdf-library/definitions/browse?category=ingredient&limit=20", }); } const browseLimit = Math.min(parseInt(limit as string, 10) || 20, 100); const terms = masterCulinaryDictionary.getTermsByCategory(category); res.json({ status:"success", category, totalInCategory: terms.length, showing: Math.min(browseLimit, terms.length), terms: terms.slice(0, browseLimit).map((t) => ({ term: t.term, definition: t.definition.substring(0, 120) +"...", categories: t.categories, confidence: t.confidence, })), }); } catch (error) { console.error("Error browsing definitions:", error); res.status(500).json({ status:"error", message:"Failed to browse definitions", error: error instanceof Error ? error.message :"Unknown error", }); }
} // Register routes
pdfLibraryImportRouter.post("/pdf-library/upload-multipart", upload.single("pdf"), uploadPDFMultipart,
);
pdfLibraryImportRouter.post("/pdf-library/upload", uploadPDFFile);
pdfLibraryImportRouter.post("/pdf-library/upload-batch", uploadPDFBatch);
pdfLibraryImportRouter.get("/pdf-library/status", getPDFImportStatus);
pdfLibraryImportRouter.post("/pdf-library/import-from-text", importFromText);
pdfLibraryImportRouter.post("/pdf-library/debug", debugPDFExtraction); // Definition Access Routes
pdfLibraryImportRouter.get("/pdf-library/definition/:term", getTermDefinition);
pdfLibraryImportRouter.get("/pdf-library/search", searchDefinitions);
pdfLibraryImportRouter.get("/pdf-library/definitions/related/:term", getRelatedDefinitions,
);
pdfLibraryImportRouter.get("/pdf-library/definitions/browse", browseDefinitionsByCategory,
); export default pdfLibraryImportRouter;
