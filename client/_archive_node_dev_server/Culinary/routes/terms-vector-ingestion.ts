/** * Terms Vector Ingestion API * Ingests culinary terms from master dictionary to both Supabase pgvector and Pinecone * Handles 181,000+ uploaded terms with batch processing and error recovery */ import { Router, Request, Response } from"express";
import { masterCulinaryDictionary } from"../lib/master-culinary-dictionary";
import { uploadedTermsStore } from"../lib/uploaded-terms-store";
import { storeInternalKnowledgeBatch } from"../lib/internal-knowledge-service";
import { generateEmbedding } from"../lib/pinecone-service";
import { getPineconeStatus } from"../lib/pinecone-verification-service";
import { storeKnowledgeBatch } from"../lib/knowledge-vector-service"; const router = Router(); // Helper to wrap async route handlers and catch errors
const asyncHandler = (fn: (req: any, res: any) => Promise<any>) => (req: any, res: any, next: any) => { Promise.resolve(fn(req, res)).catch(next); }; interface IngestionProgress { totalTerms: number; processedTerms: number; supabaseSuccess: number; supabaseErrors: number; pineconeSuccess: number; pineconeErrors: number; overallProgress: number; currentPhase:"fetching" |"embedding" |"supabase" |"pinecone" |"complete"; message: string; errors: string[]; startTime: number; estimatedTimeRemaining: number; embeddingsGenerated: number; embeddingsFailed: number;
} let currentProgress: IngestionProgress | null = null;
let ingestionInProgress = false; /** * GET /api/terms/ingestion/progress * Check ingestion progress */
router.get("/ingestion/progress", asyncHandler(async (req: Request, res: Response) => { try { if (!currentProgress) { return res.json({ status:"idle", message:"No ingestion in progress", }); } return res.json({ status:"in_progress", progress: currentProgress, }); } catch (error) { const errorMsg = error instanceof Error ? error.message : String(error); console.error("[TermIngestion] Error getting progress:", error); return res.status(500).json({ success: false, error: errorMsg, }); } }),
); /** * POST /api/terms/ingest/start * Start ingesting all terms to both Supabase and Pinecone */
router.post("/ingest/start", asyncHandler(async (req: Request, res: Response) => { // Prevent multiple concurrent ingestions if (ingestionInProgress) { return res.status(409).json({ success: false, error:"Ingestion already in progress", progress: currentProgress, }); } // Start ingestion in background ingestionInProgress = true; startIngestion().finally(() => { ingestionInProgress = false; }); return res.json({ success: true, message:"Ingestion started", message2:"Check /api/terms/ingestion/progress for updates", }); }),
); /** * Main ingestion function - runs in background */
async function startIngestion(): Promise<void> { const startTime = Date.now(); // Ensure uploaded terms store is loaded await uploadedTermsStore.ensureLoaded(); // Combine master dictionary terms with uploaded terms const masterTerms = masterCulinaryDictionary.getAllTerms(); const uploadedTerms = uploadedTermsStore.getAllTerms(); // Remove duplicates by using a Map with term names as keys const allTermsMap = new Map<string, (typeof masterTerms)[0]>(); // Add master terms first for (const term of masterTerms) { allTermsMap.set(term.term.toLowerCase(), term); } // Add uploaded terms (will overwrite any duplicates) for (const term of uploadedTerms) { allTermsMap.set(term.term.toLowerCase(), term); } const allTerms = Array.from(allTermsMap.values()); currentProgress = { totalTerms: allTerms.length, processedTerms: 0, supabaseSuccess: 0, supabaseErrors: 0, pineconeSuccess: 0, pineconeErrors: 0, overallProgress: 0, currentPhase:"fetching", message: `Starting ingestion of ${allTerms.length} terms...`, errors: [], startTime, estimatedTimeRemaining: 0, embeddingsGenerated: 0, embeddingsFailed: 0, }; console.log( `[TermIngestion] Starting ingestion of ${allTerms.length} terms to Supabase and Pinecone`, ); try { // Phase 1: Fetch and prepare terms - using controlled sequential embedding generation currentProgress.currentPhase ="embedding"; currentProgress.message ="Generating embeddings..."; const termsWithEmbeddings: Array<{ term: any; embedding: number[] }> = []; const batchSize = 5; // Process in small batches for stability for (let i = 0; i < allTerms.length; i++) { const term = allTerms[i]; try { const textToEmbed = `${term.term} ${term.definition}`; const embedding = await generateEmbedding(textToEmbed); termsWithEmbeddings.push({ term, embedding, }); currentProgress.embeddingsGenerated++; currentProgress.processedTerms = currentProgress.embeddingsGenerated + currentProgress.embeddingsFailed; currentProgress.overallProgress = Math.round( (currentProgress.processedTerms / allTerms.length) * 25, // Embedding is ~25% of total work ); // Update message periodically if (currentProgress.embeddingsGenerated % 50 === 0) { currentProgress.message = `Generated embeddings for ${currentProgress.embeddingsGenerated}/${allTerms.length} terms`; console.log( `[TermIngestion] Progress: ${currentProgress.embeddingsGenerated}/${allTerms.length} embeddings generated`, ); } } catch (error) { const errorMsg = error instanceof Error ? error.message : String(error); currentProgress.embeddingsFailed++; currentProgress.processedTerms = currentProgress.embeddingsGenerated + currentProgress.embeddingsFailed; const errorStr = `Failed to embed term"${term.term}": ${errorMsg}`; if (currentProgress.errors.length < 10) { currentProgress.errors.push(errorStr); } console.error(`[TermIngestion] ${errorStr}`); } // Small delay between requests to avoid rate limiting if ((i + 1) % batchSize === 0) { await new Promise((resolve) => setTimeout(resolve, 50)); } } console.log( `[TermIngestion] Generated embeddings: ${currentProgress.embeddingsGenerated} success, ${currentProgress.embeddingsFailed} failed`, ); // Phase 2: Ingest to Supabase pgvector currentProgress.currentPhase ="supabase"; currentProgress.message ="Ingesting to Supabase pgvector..."; const supabaseItems = termsWithEmbeddings.map(({ term }) => ({ title: term.term, content: term.definition, description: `${term.usage?.primary || term.definition}`, sourceType:"culinary-dictionary" as const, source:"master-culinary-dictionary", metadata: { categories: term.categories, masteryLevel: term.masteryLevel, confidence: term.confidence, etymology: term.etymology, relatedTerms: term.relatedTerms, }, })); const supabaseEmbeddings = termsWithEmbeddings.map( ({ embedding }) => embedding, ); try { const supabaseStartTime = Date.now(); const supabaseResult = await storeInternalKnowledgeBatch( supabaseItems, 5, supabaseEmbeddings, ); currentProgress.supabaseSuccess = supabaseResult.success; currentProgress.supabaseErrors = supabaseResult.failed; currentProgress.overallProgress = Math.round( 25 + (50 * supabaseResult.success) / supabaseItems.length, ); currentProgress.message = `Ingested ${supabaseResult.success} terms to Supabase`; console.log( `[TermIngestion] Supabase ingestion complete: ${supabaseResult.success} success, ${supabaseResult.failed} errors in ${Date.now() - supabaseStartTime}ms`, ); } catch (error) { const errorMsg = error instanceof Error ? error.message : String(error); currentProgress.errors.push(`Supabase ingestion failed: ${errorMsg}`); console.error("[TermIngestion] Supabase ingestion error:", error); } // Phase 3: Ingest to Pinecone currentProgress.currentPhase ="pinecone"; currentProgress.message ="Ingesting to Pinecone..."; try { const pineconeStartTime = Date.now(); const pineconeStatus = await getPineconeStatus(); if (!pineconeStatus.connected) { throw new Error( `Pinecone not connected: ${pineconeStatus.error ||"Unknown error"}`, ); } // Convert to Pinecone format (as TerminologyKnowledge) const pineconeItems = termsWithEmbeddings.map(({ term }, idx) => { const knowledgeItem = { id: `terminology-${idx}-${Date.now()}`, type:"terminology" as const, title: term.term, description: `${term.usage?.primary || term.definition}`, content: term.definition, source:"culinary-dictionary", sourceType:"user_imported" as const, tags: term.categories, domain:"culinary" as const, createdAt: new Date().toISOString(), updatedAt: new Date().toISOString(), confidence: term.confidence, definition: term.definition, etymology: term.etymology?.originalWord || term.term, context: term.usage?.context ||"culinary", synonyms: term.relatedTerms, }; return knowledgeItem; }); const pineconeEmbeddings = termsWithEmbeddings.map( ({ embedding }) => embedding, ); const pineconeResult = await storeKnowledgeBatch( pineconeItems, 3, pineconeEmbeddings, ); currentProgress.pineconeSuccess = pineconeResult.success; currentProgress.pineconeErrors = pineconeResult.failed; currentProgress.overallProgress = Math.round( 75 + (25 * pineconeResult.success) / pineconeItems.length, ); currentProgress.message = `Ingested ${pineconeResult.success} terms to Pinecone`; console.log( `[TermIngestion] Pinecone ingestion complete: ${pineconeResult.success} success, ${pineconeResult.failed} errors in ${Date.now() - pineconeStartTime}ms`, ); } catch (error) { const errorMsg = error instanceof Error ? error.message : String(error); currentProgress.errors.push(`Pinecone ingestion failed: ${errorMsg}`); console.error("[TermIngestion] Pinecone ingestion error:", error); } // Phase 4: Complete currentProgress.currentPhase ="complete"; currentProgress.overallProgress = 100; const endTime = Date.now(); const duration = (endTime - startTime) / 1000; currentProgress.message = `✓ Ingestion complete! Processed ${currentProgress.supabaseSuccess + currentProgress.pineconeSuccess} terms in ${duration.toFixed(1)}s`; console.log( `[TermIngestion] Ingestion complete: Embeddings=${currentProgress.embeddingsGenerated}, Supabase=${currentProgress.supabaseSuccess}, Pinecone=${currentProgress.pineconeSuccess}, Duration=${duration.toFixed(1)}s`, ); // Keep progress available for 5 minutes then clear setTimeout( () => { currentProgress = null; }, 5 * 60 * 1000, ); } catch (error) { const errorMsg = error instanceof Error ? error.message : String(error); if (currentProgress) { currentProgress.currentPhase ="complete"; currentProgress.message = `✗ Ingestion failed: ${errorMsg}`; currentProgress.errors.push(errorMsg); } console.error("[TermIngestion] Fatal ingestion error:", error); // Keep error state for 5 minutes setTimeout( () => { currentProgress = null; }, 5 * 60 * 1000, ); }
} /** * POST /api/terms/ingest/all * Immediate synchronous ingestion (for smaller datasets) */
router.post("/ingest/all", asyncHandler(async (req: Request, res: Response) => { try { // Ensure uploaded terms store is loaded await uploadedTermsStore.ensureLoaded(); // Combine master dictionary terms with uploaded terms const masterTerms = masterCulinaryDictionary.getAllTerms(); const uploadedTerms = uploadedTermsStore.getAllTerms(); // Remove duplicates const allTermsMap = new Map<string, (typeof masterTerms)[0]>(); for (const term of masterTerms) { allTermsMap.set(term.term.toLowerCase(), term); } for (const term of uploadedTerms) { allTermsMap.set(term.term.toLowerCase(), term); } const allTerms = Array.from(allTermsMap.values()); if (allTerms.length === 0) { return res.status(400).json({ success: false, error:"No terms available in master dictionary or uploaded terms", }); } console.log( `[TermIngestion] Direct ingestion of ${allTerms.length} terms started`, ); // Generate embeddings first const termsWithEmbeddings: Array<{ term: any; embedding: number[] }> = []; for (const term of allTerms) { try { const textToEmbed = `${term.term} ${term.definition}`; const embedding = await generateEmbedding(textToEmbed); termsWithEmbeddings.push({ term, embedding }); } catch (error) { console.warn( `[TermIngestion] Failed to embed term"${term.term}": ${error instanceof Error ? error.message : String(error)}`, ); } } const supabaseItems = termsWithEmbeddings.map(({ term }) => ({ title: term.term, content: term.definition, description: `${term.usage?.primary || term.definition}`, sourceType:"culinary-dictionary" as const, source:"master-culinary-dictionary", metadata: { categories: term.categories, masteryLevel: term.masteryLevel, confidence: term.confidence, etymology: term.etymology, relatedTerms: term.relatedTerms, }, })); const embeddings = termsWithEmbeddings.map(({ embedding }) => embedding); const supabaseResult = await storeInternalKnowledgeBatch( supabaseItems, 5, embeddings, ); return res.json({ success: true, message: `Ingested ${supabaseResult.success} terms to Supabase`, result: { success: supabaseResult.success, failed: supabaseResult.failed, totalTerms: allTerms.length, embeddingsGenerated: termsWithEmbeddings.length, }, }); } catch (error) { const errorMsg = error instanceof Error ? error.message : String(error); console.error("[TermIngestion] Direct ingestion error:", error); return res.status(500).json({ success: false, error: errorMsg, }); } }),
); /** * GET /api/terms/count * Get total number of terms available for ingestion */
router.get("/count", asyncHandler(async (req: Request, res: Response) => { try { // Ensure uploaded terms store is loaded await uploadedTermsStore.ensureLoaded(); // Combine master dictionary terms with uploaded terms const masterTerms = masterCulinaryDictionary.getAllTerms(); const uploadedTerms = uploadedTermsStore.getAllTerms(); // Remove duplicates const allTermsMap = new Map<string, (typeof masterTerms)[0]>(); for (const term of masterTerms) { allTermsMap.set(term.term.toLowerCase(), term); } for (const term of uploadedTerms) { allTermsMap.set(term.term.toLowerCase(), term); } const totalTerms = allTermsMap.size; const uploadedCount = uploadedTermsStore.getCount(); return res.json({ success: true, totalTerms, uploadedTerms: uploadedCount, masterTerms: masterTerms.length, termsReady: true, message: `${totalTerms} terms available for ingestion (${uploadedCount} uploaded, ${masterTerms.length} built-in)`, }); } catch (error) { const errorMsg = error instanceof Error ? error.message : String(error); console.error("[TermIngestion] Error getting terms count:", error); return res.status(500).json({ success: false, error: errorMsg, }); } }),
); export { router as termsVectorIngestionRouter };
export default router;
