/** * Knowledge Diagnostics API Routes * Provides detailed information about all knowledge sources and storage status */ import { Router, Request, Response } from"express";
import { getInternalKnowledgeStats } from"../lib/internal-knowledge-service";
import { masterCulinaryDictionary } from"../lib/master-culinary-dictionary";
import { uploadedTermsStore } from"../lib/uploaded-terms-store";
import { knowledgeInitializer } from"../lib/knowledge-initialization";
import { ingestionController } from"../lib/knowledge-ingestion-service"; const router = Router(); /** * GET /api/knowledge/diagnostics * Get comprehensive knowledge system diagnostics */
router.get("/diagnostics", async (req: Request, res: Response) => { try { const internalStats = await getInternalKnowledgeStats(); const masterDictStats = masterCulinaryDictionary.getStatistics(); const initStatus = knowledgeInitializer.getStatus(); const ingestionProgress = ingestionController.getProgress(); // Load uploaded terms count await uploadedTermsStore.ensureLoaded(); const uploadedTermsCount = uploadedTermsStore.getCount(); const diagnostics = { timestamp: new Date().toISOString(), supabaseStatus: { configured: internalStats.total > 0 || internalStats.message ==="Success", totalVectors: internalStats.total, bySourceType: internalStats.bySourceType, byDomain: internalStats.byDomain, }, masterDictionary: { totalTerms: masterDictStats.totalTerms, categories: masterDictStats.categories, masteryLevels: masterDictStats.masteryLevels, averageConfidence: masterDictStats.averageConfidence, }, uploadedTerms: { totalTerms: uploadedTermsCount, source:"user-uploaded", }, initialization: { initialized: initStatus.initialized, progress: initStatus.progress, }, ingestion: { currentProgress: ingestionProgress, }, summary: { readiness: calculateReadiness(internalStats, masterDictStats), nextSteps: getNextSteps(internalStats, masterDictStats), }, }; res.json({ status:"success", diagnostics, }); } catch (error) { console.error("[Diagnostics] Error getting diagnostics:", error); res.status(500).json({ status:"error", message:"Failed to get knowledge diagnostics", error: error instanceof Error ? error.message :"Unknown error", }); }
}); /** * GET /api/knowledge/status * Quick status check */
router.get("/status", async (req: Request, res: Response) => { try { const internalStats = await getInternalKnowledgeStats(); const masterDictStats = masterCulinaryDictionary.getStatistics(); // Load uploaded terms count await uploadedTermsStore.ensureLoaded(); const uploadedTermsCount = uploadedTermsStore.getCount(); const status = { internal_vectors: internalStats.total, master_dictionary_terms: masterDictStats.totalTerms, uploaded_terms: uploadedTermsCount, total_terms: (masterDictStats.totalTerms || 0) + (uploadedTermsCount || 0), ready: internalStats.total > 0 && (masterDictStats.totalTerms > 0 || uploadedTermsCount > 0), }; res.json({ status:"success", data: status, }); } catch (error) { console.error("[Diagnostics] Error getting status:", error); res.status(500).json({ status:"error", message:"Failed to get knowledge status", error: error instanceof Error ? error.message :"Unknown error", }); }
}); /** * POST /api/knowledge/verify * Verify all knowledge sources and check health */
router.post("/verify", async (req: Request, res: Response) => { try { console.log("[Diagnostics] Starting knowledge verification..."); const checks = { masterDictionary: await verifyMasterDictionary(), internalStorage: await verifyInternalStorage(), pinecone: await verifyPinecone(), }; const allHealthy = Object.values(checks).every((check) => check.healthy); res.json({ status:"success", timestamp: new Date().toISOString(), allHealthy, checks, recommendations: generateRecommendations(checks), }); } catch (error) { console.error("[Diagnostics] Error during verification:", error); res.status(500).json({ status:"error", message:"Verification failed", error: error instanceof Error ? error.message :"Unknown error", }); }
}); // Helper functions function calculateReadiness(internalStats: any, masterDictStats: any): string { const internalVectors = internalStats.total || 0; const masterTerms = masterDictStats.totalTerms || 0; if (internalVectors > 1000 && masterTerms > 5000) { return"EXCELLENT - All sources loaded"; } else if (internalVectors > 100 && masterTerms > 1000) { return"GOOD - Core knowledge available"; } else if (masterTerms > 1000) { return"READY - Master Dictionary loaded, internal storage needs configuration"; } else if (internalVectors > 0) { return"PARTIAL - Only internal storage active"; } else { return"NEEDS_SETUP - No knowledge sources loaded"; }
} function getNextSteps(internalStats: any, masterDictStats: any): string[] { const steps: string[] = []; const internalVectors = internalStats.total || 0; const masterTerms = masterDictStats.totalTerms || 0; if (internalVectors === 0) { steps.push("✓ Master Dictionary is loaded in memory"); steps.push("⚠️ Connect Supabase via MCP to enable persistent internal vector storage", ); steps.push("⚠️ Once Supabase connected, POST /api/echo/knowledge/ingest/master-dictionary to vectorize dictionary", ); steps.push("⚠��� Once Supabase connected, POST /api/echo/knowledge/ingest/pinecone to migrate Pinecone data", ); } else if (internalVectors < 5000) { steps.push("✓ Internal storage configured"); steps.push(`✓ ${internalVectors} vectors currently stored`); steps.push("→ POST /api/echo/knowledge/ingest/all to load remaining sources", ); } else { steps.push("✓ All knowledge sources loaded successfully"); steps.push("→ Monitor ingestion progress at GET /api/echo/knowledge/ingest/progress", ); } return steps;
} async function verifyMasterDictionary(): Promise<{ healthy: boolean; terms: number; message: string;
}> { try { const stats = masterCulinaryDictionary.getStatistics(); return { healthy: stats.totalTerms > 0, terms: stats.totalTerms, message: `Master Dictionary loaded with ${stats.totalTerms} terms`, }; } catch (error) { return { healthy: false, terms: 0, message: `Error loading Master Dictionary: ${error instanceof Error ? error.message : String(error)}`, }; }
} async function verifyInternalStorage(): Promise<{ healthy: boolean; vectors: number; message: string;
}> { try { const stats = await getInternalKnowledgeStats(); const healthy = stats.total > 0 || stats.message?.includes("not configured"); return { healthy, vectors: stats.total, message: healthy ? stats.total > 0 ? `Internal storage configured with ${stats.total} vectors` :"Supabase not configured (use internal storage only)" :"Error accessing internal storage", }; } catch (error) { return { healthy: false, vectors: 0, message: `Error accessing internal storage: ${error instanceof Error ? error.message : String(error)}`, }; }
} async function verifyPinecone(): Promise<{ healthy: boolean; available: boolean; message: string;
}> { try { const pineconeKey = process.env.PINECONE_API_KEY; if (!pineconeKey) { return { healthy: true, available: false, message:"Pinecone API key not configured (optional)", }; } // Quick verification by checking if we can import the client try { const { Pinecone } = await import("@pinecone-database/pinecone"); return { healthy: true, available: true, message:"Pinecone configured and available", }; } catch (error) { return { healthy: false, available: false, message: `Pinecone configured but unavailable: ${error instanceof Error ? error.message : String(error)}`, }; } } catch (error) { return { healthy: false, available: false, message: `Error checking Pinecone: ${error instanceof Error ? error.message : String(error)}`, }; }
} function generateRecommendations(checks: any): string[] { const recommendations: string[] = []; if (!checks.masterDictionary.healthy) { recommendations.push("⚠️ Master Dictionary is not loading properly"); } if (!checks.internalStorage.healthy) { recommendations.push("⚠️ Internal storage (Supabase) is not configured"); recommendations.push("→ Connect to Supabase via MCP to enable persistent vector storage", ); } else if (checks.internalStorage.vectors === 0) { recommendations.push("→ Supabase is configured but empty"); recommendations.push("→ POST /api/echo/knowledge/ingest/master-dictionary to populate", ); } if (!checks.pinecone.available) { recommendations.push("ℹ️ Pinecone is not configured (optional for cost optimization)", ); } else if (!checks.pinecone.healthy) { recommendations.push("⚠️ Pinecone is configured but may not be accessible"); } if (recommendations.length === 0) { recommendations.push("✓ All knowledge sources are healthy and configured"); } return recommendations;
} /** * GET /api/knowledge/stats * Get simple knowledge statistics for UI display */
router.get("/stats", async (req: Request, res: Response) => { try { const internalStats = await getInternalKnowledgeStats(); const masterDictStats = masterCulinaryDictionary.getStatistics(); // Load uploaded terms count await uploadedTermsStore.ensureLoaded(); const uploadedTermsCount = uploadedTermsStore.getCount(); res.json({ success: true, stats: { approvedItems: internalStats.total || 0, masterDictionaryTerms: masterDictStats.totalTerms || 0, uploadedTerms: uploadedTermsCount || 0, totalTerms: (masterDictStats.totalTerms || 0) + (uploadedTermsCount || 0), totalVectors: internalStats.total || 0, bySourceType: internalStats.bySourceType || {}, byDomain: internalStats.byDomain || {}, categories: masterDictStats.categories || {}, masteryLevels: masterDictStats.masteryLevels || {}, averageConfidence: masterDictStats.averageConfidence || 0, }, }); } catch (error) { console.error("[Knowledge Stats] Error:", error); res.status(500).json({ success: false, error: error instanceof Error ? error.message :"Unknown error", }); }
}); /** * POST /api/knowledge/reset-ingestion * Force reset stuck ingestion state */
router.post("/reset-ingestion", async (req: Request, res: Response) => { try { ingestionController.resetIngestionState(); res.json({ success: true, message:"Ingestion state reset successfully", }); } catch (error) { console.error("[Knowledge Reset] Error:", error); res.status(500).json({ success: false, error: error instanceof Error ? error.message :"Unknown error", }); }
}); export default router;
