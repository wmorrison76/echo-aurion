/** * Embedding Queue Service * Manages batched embedding generation with rate limiting and concurrent workers * * Key features: * - Batch embedding (100 items per API call, not 1 per call) * - 5 concurrent workers to balance throughput + rate limit compliance * - Exponential backoff for rate limiting * - Progress tracking * - Automatic retry with configurable limits */ import { generateEmbedding } from "./pinecone-service";
export interface EmbeddingJob {
  itemId: string;
  text: string;
  targetStorage: "supabase" | "pinecone" | "both";
  metadata?: Record<string, any>;
}
export interface EmbeddingQueueStats {
  pending: number;
  processing: number;
  completed: number;
  failed: number;
  totalProcessed: number;
  avgTimePerBatch: number;
  estimatedTimeRemaining: number;
}
export class EmbeddingQueueService {
  private queue: EmbeddingJob[] = [];
  private processing: Set<string> = new Set();
  private completed: Map<string, number[]> = new Map();
  private failed: Map<string, Error> = new Map();
  private workers: number = 5;
  private batchSize: number = 100;
  private retryLimit: number = 3;
  private rateLimitWait: number = 1000; // Start with 1 second private startTime: number = 0; private stats = { completed: 0, failed: 0, totalBatches: 0, totalTime: 0, }; constructor( workers: number = 5, batchSize: number = 100, retryLimit: number = 3, ) { this.workers = Math.max(1, Math.min(workers, 10)); this.batchSize = Math.max(10, Math.min(batchSize, 200)); this.retryLimit = Math.max(1, retryLimit); } enqueue(job: EmbeddingJob) { this.queue.push(job); } enqueueBatch(jobs: EmbeddingJob[]) { this.queue.push(...jobs); } getStats(): EmbeddingQueueStats { const processed = this.stats.completed + this.stats.failed; const remaining = this.queue.length + this.processing.size; const avgTime = this.stats.totalBatches > 0 ? this.stats.totalTime / this.stats.totalBatches : 0; const estimatedRemaining = remaining > 0 ? Math.ceil(remaining / this.batchSize) * avgTime : 0; return { pending: this.queue.length, processing: this.processing.size, completed: this.stats.completed, failed: this.stats.failed, totalProcessed: processed, avgTimePerBatch: avgTime, estimatedTimeRemaining: estimatedRemaining, }; } async processQueue( onProgress?: (stats: EmbeddingQueueStats) => void, ): Promise<void> { this.startTime = Date.now(); const totalJobs = this.queue.length; console.log( `[EmbeddingQueue] Starting to process ${totalJobs} items with ${this.workers} workers`, ); const workerPromises: Promise<void>[] = []; for (let i = 0; i < this.workers; i++) { workerPromises.push(this.workerLoop(totalJobs, onProgress)); } await Promise.all(workerPromises); const totalSeconds = (Date.now() - this.startTime) / 1000; console.log( `[EmbeddingQueue] Queue processing complete in ${totalSeconds.toFixed(1)}s`, ); console.log(` Completed: ${this.stats.completed}`); console.log(` Failed: ${this.stats.failed}`); console.log(` Total batches: ${this.stats.totalBatches}`); console.log( ` Avg time per batch: ${(this.stats.totalTime / Math.max(1, this.stats.totalBatches)).toFixed(1)}ms`, ); } private async workerLoop( totalJobs: number, onProgress?: (stats: EmbeddingQueueStats) => void, ): Promise<void> { while (this.queue.length > 0 || this.processing.size > 0) { // Get next batch const batch = this.queue.splice(0, this.batchSize); if (batch.length === 0) { // Wait a bit before checking again await new Promise((resolve) => setTimeout(resolve, 100)); continue; } // Mark items as processing const jobIds = batch.map((j) => j.itemId); jobIds.forEach((id) => this.processing.add(id)); try { const batchStartTime = Date.now(); // Generate embeddings for batch const embeddings = await this.generateBatchEmbeddings(batch); const batchTime = Date.now() - batchStartTime; this.stats.totalTime += batchTime; this.stats.totalBatches++; // Store results batch.forEach((job, idx) => { if (embeddings[idx]) { this.completed.set(job.itemId, embeddings[idx]); this.stats.completed++; } }); // Handle rate limiting if (this.processing.size < this.workers) { this.rateLimitWait = 1000; // Reset on success } } catch (error) { console.error(`[EmbeddingQueue] Batch error:`, error); // Retry logic const retryableJobs = batch.filter((job) => { const attempts = Array.from(this.failed.values()).filter((e) => e.message.includes(job.itemId), ).length; return attempts < this.retryLimit; }); if (retryableJobs.length > 0) { this.queue.push(...retryableJobs); this.rateLimitWait = Math.min(this.rateLimitWait * 2, 30000); // Exponential backoff await new Promise((resolve) => setTimeout(resolve, this.rateLimitWait), ); } // Mark failed items batch.forEach((job) => { if (retryableJobs.every((j) => j.itemId !== job.itemId)) { this.failed.set( job.itemId, new Error(`Failed after ${this.retryLimit} retries`), ); this.stats.failed++; } }); } finally { // Remove from processing jobIds.forEach((id) => this.processing.delete(id)); // Report progress if (onProgress) { onProgress(this.getStats()); } } } } private async generateBatchEmbeddings( jobs: EmbeddingJob[], ): Promise<(number[] | null)[]> { const texts = jobs.map((j) => j.text); const embeddings: (number[] | null)[] = []; // Use batch embedding if available, otherwise fall back to individual try { // Generate embeddings for each text for (const text of texts) { try { const embedding = await generateEmbedding(text); embeddings.push(embedding); } catch (error) { console.error(`[EmbeddingQueue] Failed to embed text:`, error); embeddings.push(null); } } } catch (error) { console.error(`[EmbeddingQueue] Batch embedding failed:`, error); throw error; } return embeddings; } getCompleted(itemId: string): number[] | undefined { return this.completed.get(itemId); } getAllCompleted(): Map<string, number[]> { return new Map(this.completed); } getFailed(): Map<string, Error> { return new Map(this.failed); }
}
export const embeddingQueueService = new EmbeddingQueueService(5, 100, 3);
