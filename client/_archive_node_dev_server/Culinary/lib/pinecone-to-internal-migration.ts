/** * Pinecone to Internal Knowledge Migration * Orchestrates the complete migration of knowledge from Pinecone to internal pgvector storage * Handles data transformation, batch processing, and verification */ import { extractAllPineconeKnowledge, extractPineconeKnowledgeBySourceType, getPineconeIndexStats, countPineconeVectors, verifyPineconeConnection, transformPineconeToInternalFormat, type ExtractionResult, type ExtractedPineconeItem,
} from"./pinecone-extraction-service";
import { storeInternalKnowledgeBatch, getInternalKnowledgeStats, type InternalKnowledgeVector,
} from"./internal-knowledge-service"; export interface MigrationStats { totalExtracted: number; totalStored: number; totalFailed: number; startTime: string; endTime: string; durationSeconds: number; failedItems: Array<{ id: string; error: string }>;
} export interface MigrationProgress { status: |"pending" |"extracting" |"transforming" |"storing" |"verifying" |"complete" |"error"; progress: number; message: string; extractedCount?: number; storedCount?: number; failedCount?: number;
} class MigrationController { private migrationInProgress: boolean = false; private currentProgress: MigrationProgress = { status:"pending", progress: 0, message:"Migration ready to start", }; /** * Get current migration progress */ getProgress(): MigrationProgress { return this.currentProgress; } /** * Update progress status */ private updateProgress(update: Partial<MigrationProgress>) { this.currentProgress = { ...this.currentProgress, ...update }; console.log(`[Migration] ${this.currentProgress.message}`); } /** * Run complete migration from Pinecone to internal storage */ async runFullMigration(): Promise<MigrationStats> { if (this.migrationInProgress) { throw new Error("Migration already in progress"); } this.migrationInProgress = true; const startTime = Date.now(); const stats: MigrationStats = { totalExtracted: 0, totalStored: 0, totalFailed: 0, startTime: new Date().toISOString(), endTime:"", durationSeconds: 0, failedItems: [], }; try { // Step 1: Verify Pinecone connection this.updateProgress({ status:"extracting", progress: 5, message:"Verifying Pinecone connection...", }); const connectionCheck = await verifyPineconeConnection(); if (!connectionCheck.connected) { throw new Error(`Pinecone connection failed: ${connectionCheck.error}`); } console.log("[Migration] ✓ Pinecone connection verified"); // Step 2: Extract Pinecone statistics this.updateProgress({ status:"extracting", progress: 10, message:"Analyzing Pinecone index...", }); const indexStats = await getPineconeIndexStats(); const vectorCount = await countPineconeVectors(); console.log(`[Migration] Found ${vectorCount} vectors in Pinecone`); console.log(`[Migration] Index stats:`, indexStats); // Step 3: Extract all Pinecone knowledge this.updateProgress({ status:"extracting", progress: 15, message: `Extracting ${vectorCount} vectors from Pinecone...`, }); const extraction = await extractAllPineconeKnowledge(); stats.totalExtracted = extraction.totalItems; console.log( `[Migration] ✓ Extracted ${extraction.totalItems} items from Pinecone`, ); // Step 4: Transform data to internal format this.updateProgress({ status:"transforming", progress: 40, message: `Transforming ${extraction.totalItems} items to internal format...`, }); const transformedItems = extraction.items .map((item) => { try { return transformPineconeToInternalFormat(item); } catch (error) { const errorMsg = error instanceof Error ? error.message : String(error); stats.failedItems.push({ id: item.id, error: `Transformation failed: ${errorMsg}`, }); return null; } }) .filter(Boolean) as Array< Omit<InternalKnowledgeVector,"id" |"embedding"> >; console.log(`[Migration] ✓ Transformed ${transformedItems.length} items`); console.log( `[Migration] Transformation failed for ${extraction.totalItems - transformedItems.length} items`, ); // Step 5: Store in internal database in batches this.updateProgress({ status:"storing", progress: 50, message: `Storing ${transformedItems.length} items in internal database...`, }); const batchSize = 50; let storedCount = 0; for (let i = 0; i < transformedItems.length; i += batchSize) { const batch = transformedItems.slice(i, i + batchSize); const batchNum = Math.floor(i / batchSize) + 1; const totalBatches = Math.ceil(transformedItems.length / batchSize); this.updateProgress({ status:"storing", progress: 50 + Math.floor((i / transformedItems.length) * 40), message: `Storing batch ${batchNum}/${totalBatches} (${storedCount}/${transformedItems.length} items)...`, storedCount, }); const batchResult = await storeInternalKnowledgeBatch(batch, 5); storedCount += batchResult.success; stats.totalStored += batchResult.success; stats.totalFailed += batchResult.failed; for (const error of batchResult.errors) { stats.failedItems.push({ id: error.title, error: error.error, }); } console.log( `[Migration] Batch ${batchNum}: ${batchResult.success} stored, ${batchResult.failed} failed`, ); } // Step 6: Verify migration this.updateProgress({ status:"verifying", progress: 92, message:"Verifying migration success...", }); const internalStats = await getInternalKnowledgeStats(); console.log("[Migration] Internal knowledge stats:", internalStats); // Step 7: Complete const endTime = Date.now(); stats.endTime = new Date().toISOString(); stats.durationSeconds = Math.floor((endTime - startTime) / 1000); this.updateProgress({ status:"complete", progress: 100, message: `Migration complete! Stored ${stats.totalStored}/${stats.totalExtracted} items in ${stats.durationSeconds}s`, storedCount: stats.totalStored, failedCount: stats.totalFailed, }); console.log("[Migration] ✓ MIGRATION COMPLETE"); console.log( `[Migration] Summary: ${stats.totalStored} stored, ${stats.totalFailed} failed, ${stats.durationSeconds}s total`, ); return stats; } catch (error) { const errorMsg = error instanceof Error ? error.message : String(error); console.error("[Migration] ✗ MIGRATION FAILED:", errorMsg); this.updateProgress({ status:"error", progress: 0, message: `Migration failed: ${errorMsg}`, }); throw error; } finally { this.migrationInProgress = false; } } /** * Run selective migration for specific source type * Useful for migrating just PDFs, recipes, etc. */ async runSelectiveMigration(sourceType: string): Promise<MigrationStats> { if (this.migrationInProgress) { throw new Error("Migration already in progress"); } this.migrationInProgress = true; const startTime = Date.now(); const stats: MigrationStats = { totalExtracted: 0, totalStored: 0, totalFailed: 0, startTime: new Date().toISOString(), endTime:"", durationSeconds: 0, failedItems: [], }; try { this.updateProgress({ status:"extracting", progress: 15, message: `Extracting ${sourceType} knowledge from Pinecone...`, }); const extraction = await extractPineconeKnowledgeBySourceType(sourceType); stats.totalExtracted = extraction.totalItems; console.log( `[Migration] Extracted ${extraction.totalItems} items of type: ${sourceType}`, ); // Transform this.updateProgress({ status:"transforming", progress: 40, message: `Transforming ${extraction.totalItems} items...`, }); const transformedItems = extraction.items .map((item) => { try { return transformPineconeToInternalFormat(item); } catch (error) { const errorMsg = error instanceof Error ? error.message : String(error); stats.failedItems.push({ id: item.id, error: `Transformation failed: ${errorMsg}`, }); return null; } }) .filter(Boolean) as Array< Omit<InternalKnowledgeVector,"id" |"embedding"> >; // Store this.updateProgress({ status:"storing", progress: 60, message: `Storing ${transformedItems.length} items...`, }); const batchResult = await storeInternalKnowledgeBatch( transformedItems, 5, ); stats.totalStored = batchResult.success; stats.totalFailed = batchResult.failed; stats.failedItems = batchResult.errors.map((e) => ({ id: e.title, error: e.error, })); const endTime = Date.now(); stats.endTime = new Date().toISOString(); stats.durationSeconds = Math.floor((endTime - startTime) / 1000); this.updateProgress({ status:"complete", progress: 100, message: `Selective migration complete! Migrated ${stats.totalStored} items of type"${sourceType}"`, storedCount: stats.totalStored, failedCount: stats.totalFailed, }); return stats; } catch (error) { const errorMsg = error instanceof Error ? error.message : String(error); this.updateProgress({ status:"error", progress: 0, message: `Selective migration failed: ${errorMsg}`, }); throw error; } finally { this.migrationInProgress = false; } } /** * Check if migration is currently running */ isMigrationInProgress(): boolean { return this.migrationInProgress; }
} // Singleton instance
export const migrationController = new MigrationController();
