/** * PDF Upload Handler * Manages PDF file uploads and text extraction for library imports * Converts PDF files to text for culinary knowledge extraction */ import fs from 'fs';
import path from 'path';
import { convertPDFToMasterTerms } from './pdf-knowledge-extractor';
import type { PDFMetadata } from './pdf-knowledge-extractor'; export interface PDFUploadOptions { filename: string; mimeType: string; buffer: Buffer;
} export interface PDFProcessResult { filename: string; success: boolean; textLength?: number; pdfMetadata?: PDFMetadata; error?: string;
} /** * Extract text from PDF buffer using pattern matching and text stream extraction * Handles text-based PDFs without requiring external libraries * Implements multiple extraction strategies to maximize text recovery */
export async function extractTextFromPDFBuffer( buffer: Buffer, filename: string
): Promise<string> { try { let text = buffer.toString('binary'); const extractedParts: string[] = []; // Strategy 1: Extract from BT...ET text objects (text operators) { const textObjectPattern = /BT\s([\s\S]*?)ET/g; let match; while ((match = textObjectPattern.exec(text)) !== null) { const obj = match[1]; // Match text in parentheses: (text) const stringsInParens = obj.match(/\(([^()\\]|\\.)*\)/g); if (stringsInParens) { for (const str of stringsInParens) { const content = str.slice(1, -1) .replace(/\\\(/g, '(') .replace(/\\\)/g, ')') .replace(/\\\\/g, '\\'); if (content.length > 1) { extractedParts.push(content); } } } // Match hex encoded strings: <hexstring> const hexStrings = obj.match(/<([0-9A-Fa-f]+)>/g); if (hexStrings) { for (const hexStr of hexStrings) { const hex = hexStr.slice(1, -1); try { const decoded = Buffer.from(hex, 'hex').toString('latin1'); const readable = decoded.replace(/[^\x20-\x7E\t]/g, ' '); if (readable.trim().length > 1) { extractedParts.push(readable); } } catch (e) { // Skip malformed hex strings } } } } } // Strategy 2: Extract from content streams (more general) { const streamPattern = /stream\s*([\s\S]*?)\s*endstream/g; let match; let streamCount = 0; while ((match = streamPattern.exec(text)) !== null && streamCount < 1000) { let streamContent = match[1]; // Try to extract readable text // Remove PDF operators but preserve spaces and newlines streamContent = streamContent .replace(/BT[\s\S]*?ET/g, ' ') // Remove other BT...ET blocks .replace(/\/F\d+\s+\d+\s+Tf/g, ' ') // Font selection .replace(/\d+\s+Tz/g, ' ') // Horizontal scaling .replace(/\d+\s+TL/g, ' ') // Text leading .replace(/Tj|TJ|T'|T"|Td|TD|T\*|Tm|Tw|Tc/g, ' ') // Text positioning/showing .replace(/[()[\]{}]/g, ' ') // Brackets .replace(/\x00/g, ''); // Nulls // Extract readable characters but preserve word boundaries const readable = streamContent .replace(/[^\x20-\x7E\n\r\t]/g, ' ') .replace(/\s+/g, ' '); if (readable.trim().length > 20) { extractedParts.push(readable); } streamCount++; } } // Strategy 3: Look for text in dictionary values and arrays { // Look for /Contents, /Resources and other text-bearing objects const objPattern = /obj\s*([\s\S]*?)\s*endobj/g; let match; let objCount = 0; while ((match = objPattern.exec(text)) !== null && objCount < 500) { const obj = match[1]; // Only process objects that likely contain text if (/\(.*\)|<[0-9A-Fa-f]+>|\/Type/.test(obj)) { // Extract parenthetical strings const parens = obj.match(/\(([^()\\]|\\.)*\)/g); if (parens) { for (const p of parens) { const content = p.slice(1, -1) .replace(/\\\(/g, '(') .replace(/\\\)/g, ')') .replace(/\\\\/g, '\\') .replace(/\\/g, ''); if (content.length > 2 && /[a-z0-9]/i.test(content)) { extractedParts.push(content); } } } // Extract hex strings const hexes = obj.match(/<([0-9A-Fa-f]+)>/g); if (hexes) { for (const h of hexes) { const hex = h.slice(1, -1); if (hex.length > 4) { try { const decoded = Buffer.from(hex, 'hex').toString('latin1'); const clean = decoded.replace(/[^\x20-\x7E\t]/g, ' '); if (clean.trim().length > 2) { extractedParts.push(clean); } } catch (e) { // Skip } } } } } objCount++; } } // Strategy 4: As a fallback, look for any readable ASCII sequences { let current = ''; for (let i = 0; i < text.length; i++) { const code = text.charCodeAt(i); if ((code >= 32 && code <= 126) || code === 9 || code === 10 || code === 13) { // Printable ASCII or tab/newline current += text[i]; } else { if (current.length > 5) { extractedParts.push(current); } current = ''; } } if (current.length > 5) { extractedParts.push(current); } } // Combine all extracted parts let combined = extractedParts.join('\n'); // Clean up the combined text let cleaned = combined .replace(/\s+/g, ' ') // Normalize whitespace .trim(); // Remove common PDF artifacts cleaned = cleaned .replace(/\d+\s+\d+\s+obj\s+/g, '') // Object markers .replace(/\s+endobj\s+/g, '\n') // Object ends .replace(/%.*?(?=\n)/g, '') // Comments .replace(/stream\s+endstream/gi, '') // Stream markers .replace(/PDF\s+version/i, '') .replace(/%%EOF/i, '') .replace(/\s+/g, ' '); // Final normalization // Split into lines const lines = cleaned .split(/[\n\r]+/) .map(line => line.trim()) .filter(line => line.length > 1); // Remove noise: lines that are pure numbers, pure operators, etc const cleanedLines = lines.filter(line => { // Skip pure numbers or pure operators if (/^[\d\s.(),<>-]*$/.test(line)) return false; // Skip very long lines that are probably encoded data if (line.length > 500) return false; // Must have at least some alphabetic characters return /[a-zA-Z]/.test(line); }); const finalText = cleanedLines.join('\n'); if (finalText.length < 100) { throw new Error('Insufficient text extracted from PDF (less than 100 characters). The PDF may be image-based or encrypted. Please ensure the PDF contains selectable text.'); } return finalText; } catch (error) { throw new Error(`Failed to extract text from PDF: ${error instanceof Error ? error.message : 'Unknown error'}`); }
} /** * Extract metadata from filename and content */
export function extractPDFMetadata( filename: string, textContent: string, userMetadata?: Partial<PDFMetadata>
): PDFMetadata { // Clean filename to get title let title = path.basename(filename, path.extname(filename)) .replace(/[-_]/g, ' ') .replace(/([a-z])([A-Z])/g, '$1 $2') .trim(); // If title is too generic, use a better name if (title.length < 3) { title = 'Imported Culinary Book'; } // Try to detect cuisine from filename and content let cuisine: string | undefined; const cuisinePatterns = { 'French': ['french', 'français', 'classical', 'escoffier', 'larousse'], 'Italian': ['italian', 'italiano', 'risotto', 'pasta'], 'Asian': ['asian', 'japanese', 'chinese', 'thai', 'vietnamese', 'korean', 'wok'], 'Spanish': ['spanish', 'español', 'tapas', 'paella'], 'Indian': ['indian', 'curry', 'tandoor', 'spice'], 'Mediterranean': ['mediterranean', 'greek', 'lebanese', 'persian'], 'Modern': ['modern', 'molecular', 'contemporary', 'fusion'], }; const filenameAndContent = `${filename} ${textContent}`.toLowerCase(); for (const [cuisineType, patterns] of Object.entries(cuisinePatterns)) { if (patterns.some(p => filenameAndContent.includes(p))) { cuisine = cuisineType; break; } } // Detect publication year from text let publicationYear: number | undefined; const yearMatch = textContent.match(/\b(19|20)\d{2}\b/); if (yearMatch) { publicationYear = parseInt(yearMatch[0], 10); } // Try to extract author name (common pattern:"by [Author Name]") let author: string | undefined; const authorMatch = textContent.match(/by\s+([A-Z][a-z]+(?:\s+[A-Z][a-z]+)*)/i); if (authorMatch) { author = authorMatch[1]; } return { title: userMetadata?.title || title, author: userMetadata?.author || author, publicationYear: userMetadata?.publicationYear || publicationYear, language: userMetadata?.language || 'English', cuisine: userMetadata?.cuisine || cuisine, specialization: userMetadata?.specialization || 'culinary-book', };
} /** * Validate PDF file before processing */
export function validatePDFFile(file: PDFUploadOptions): { valid: boolean; error?: string } { // Check file size (max 10MB for safety) const maxSize = 10 * 1024 * 1024; if (file.buffer.length > maxSize) { return { valid: false, error: 'File size exceeds 10MB limit' }; } // Check file type if (!file.mimeType.includes('pdf') && !file.filename.endsWith('.pdf')) { return { valid: false, error: 'File must be a PDF' }; } // Check PDF magic number (first 4 bytes should be %PDF) const pdfMagic = file.buffer.slice(0, 4).toString('ascii'); if (!pdfMagic.startsWith('%PDF')) { return { valid: false, error: 'Invalid PDF file format' }; } return { valid: true };
} /** * Process a single PDF file upload */
export async function processPDFUpload( file: PDFUploadOptions, userMetadata?: Partial<PDFMetadata>
): Promise<PDFProcessResult> { try { // Validate file const validation = validatePDFFile(file); if (!validation.valid) { return { filename: file.filename, success: false, error: validation.error, }; } // Extract text from PDF const textContent = await extractTextFromPDFBuffer(file.buffer, file.filename); // Extract metadata const metadata = extractPDFMetadata(file.filename, textContent, userMetadata); return { filename: file.filename, success: true, textLength: textContent.length, pdfMetadata: metadata, }; } catch (error) { return { filename: file.filename, success: false, error: error instanceof Error ? error.message : 'Unknown error processing PDF', }; }
} /** * Process multiple PDF uploads */
export async function processPDFBatch( files: PDFUploadOptions[], userMetadataList?: Partial<PDFMetadata>[]
): Promise<PDFProcessResult[]> { const results: PDFProcessResult[] = []; for (let i = 0; i < files.length; i++) { const file = files[i]; const userMetadata = userMetadataList?.[i]; const result = await processPDFUpload(file, userMetadata); results.push(result); } return results;
} /** * Create a temporary file storage location for PDF processing * In production, use cloud storage (S3, Azure Blob, etc.) */
export function getTempPDFStoragePath(): string { const tempDir = path.join(process.cwd(), '.temp', 'pdfs'); if (!fs.existsSync(tempDir)) { fs.mkdirSync(tempDir, { recursive: true }); } return tempDir;
} /** * Save uploaded PDF to temporary storage */
export function savePDFTemporarily(file: PDFUploadOptions): string { const storageDir = getTempPDFStoragePath(); const filename = `${Date.now()}-${file.filename}`; const filepath = path.join(storageDir, filename); fs.writeFileSync(filepath, file.buffer); return filepath;
} /** * Clean up temporary PDF files */
export function cleanupTemporaryPDF(filepath: string): void { try { if (fs.existsSync(filepath)) { fs.unlinkSync(filepath); } } catch (error) { console.error(`Failed to cleanup temporary PDF file: ${error}`); }
} /** * Get PDF import statistics */
export interface PDFImportStats { totalFilesProcessed: number; successfulFiles: number; totalTextExtracted: number; averageFileSize: number; estimatedTermsExtracted: number;
} export function calculateImportStats(results: PDFProcessResult[]): PDFImportStats { const successful = results.filter(r => r.success); const totalText = successful.reduce((sum, r) => sum + (r.textLength || 0), 0); // Rough estimate: 1 term per ~50 characters of text const estimatedTerms = Math.floor(totalText / 50); return { totalFilesProcessed: results.length, successfulFiles: successful.length, totalTextExtracted: totalText, averageFileSize: totalText / successful.length || 0, estimatedTermsExtracted: estimatedTerms, };
}
