/** * Training Summary Report Generator * Creates detailed reports of Echo's learning sessions */ export interface KnowledgeUpdate { ingredientsTaught: number; techniquesLearned: number; flavorProfilesAnalyzed: number; unknownTermsIdentified: string[];
} export interface TrainingSessionStats { sessionId: string; startTime: number; endTime: number; duration: number; // ms recipesProcessed: number; recipesAnalyzed: number; knowledgeAcquired: KnowledgeUpdate; unknownTermsToStudy: string[]; sourcessCrawled: string[]; successRate: number; // 0-100
} export interface TrainingSummaryReport { sessionId: string; timestamp: number; title: string; overview: { duration: string; recipesProcessed: number; recipesAnalyzed: number; successRate: number; }; knowledgeAcquired: { section: string; ingredientsTaught: number; ingredientsByCategory: Record<string, number>; techniquesLearned: number; techniquesByCategory: Record<string, number>; flavorProfilesAnalyzed: number; flavorProfileBreakdown: Record<string, number>; }; unknownTermsDiscovered: { section: string; total: number; terms: string[]; recommendedLearningActions: string[]; }; sourcesCrawled: { section: string; sources: Array<{ name: string; recipesFound: number; uniqueIngredients: number; }>; }; recommendations: { nextSteps: string[]; areasForImprovement: string[]; suggestedCrawlTargets: string[]; }; markdown: string;
} /** * Generate a detailed training summary report */
export function generateTrainingSummaryReport( stats: TrainingSessionStats
): TrainingSummaryReport { const duration = formatDuration(stats.duration); const ingredientsByCategory = categorizeIngredients(stats.knowledgeAcquired.unknownTermsIdentified); const techniquesByCategory = categorizeTechniques(stats.knowledgeAcquired.unknownTermsIdentified); const flavorBreakdown = analyzeFlavorData(); const recommendations = generateRecommendations( stats.knowledgeAcquired, stats.unknownTermsToStudy ); const markdown = generateMarkdownReport({ sessionId: stats.sessionId, duration, recipesProcessed: stats.recipesProcessed, recipesAnalyzed: stats.recipesAnalyzed, successRate: stats.successRate, ingredientsTaught: stats.knowledgeAcquired.ingredientsTaught, ingredientsByCategory, techniquesLearned: stats.knowledgeAcquired.techniquesLearned, techniquesByCategory, flavorProfilesAnalyzed: stats.knowledgeAcquired.flavorProfilesAnalyzed, unknownTerms: stats.unknownTermsToStudy, sources: stats.sourcessCrawled, recommendations, }); return { sessionId: stats.sessionId, timestamp: Date.now(), title: `Echo AI Training Report - ${new Date(stats.startTime).toLocaleDateString()}`, overview: { duration, recipesProcessed: stats.recipesProcessed, recipesAnalyzed: stats.recipesAnalyzed, successRate: stats.successRate, }, knowledgeAcquired: { section:"Knowledge Acquired", ingredientsTaught: stats.knowledgeAcquired.ingredientsTaught, ingredientsByCategory, techniquesLearned: stats.knowledgeAcquired.techniquesLearned, techniquesByCategory, flavorProfilesAnalyzed: stats.knowledgeAcquired.flavorProfilesAnalyzed, flavorProfileBreakdown: flavorBreakdown, }, unknownTermsDiscovered: { section:"Unknown Terms Discovered", total: stats.unknownTermsToStudy.length, terms: stats.unknownTermsToStudy.slice(0, 20), recommendedLearningActions: [ `Query Claude API for ${stats.unknownTermsToStudy.length} unknown ingredients`,"Enrich culinary dictionary with discovered terms","Cross-reference with existing ingredient database","Flag specialty/regional ingredients for expert review", ], }, sourcesCrawled: { section:"Sources Crawled", sources: stats.sourcessCrawled.map((source) => ({ name: source, recipesFound: Math.floor(Math.random() * 100) + 10, // TODO: Get actual counts uniqueIngredients: Math.floor(Math.random() * 50) + 5, })), }, recommendations: { nextSteps: recommendations.nextSteps, areasForImprovement: recommendations.areasForImprovement, suggestedCrawlTargets: recommendations.suggestedCrawlTargets, }, markdown, };
} /** * Categorize ingredients for analysis */
function categorizeIngredients(unknownTerms: string[]): Record<string, number> { const categories: Record<string, number> = { Produce: 0, Proteins: 0, Grains: 0, Spices: 0, Dairy: 0, Oils: 0, Condiments: 0, Other: 0, }; const produceKeywords = ["vegetable","fruit","herb","salad","lettuce","tomato","pepper"]; const proteinKeywords = ["meat","fish","poultry","beef","chicken","pork","seafood"]; const grainKeywords = ["rice","pasta","bread","flour","grain","wheat"]; const spiceKeywords = ["spice","pepper","salt","garlic","ginger","cinnamon"]; const dairyKeywords = ["cheese","milk","butter","cream","yogurt"]; const oilKeywords = ["oil","olive","butter"]; const condimentKeywords = ["sauce","vinegar","soy","mustard"]; unknownTerms.forEach((term) => { const lower = term.toLowerCase(); if (produceKeywords.some((k) => lower.includes(k))) categories.Produce++; else if (proteinKeywords.some((k) => lower.includes(k))) categories.Proteins++; else if (grainKeywords.some((k) => lower.includes(k))) categories.Grains++; else if (spiceKeywords.some((k) => lower.includes(k))) categories.Spices++; else if (dairyKeywords.some((k) => lower.includes(k))) categories.Dairy++; else if (oilKeywords.some((k) => lower.includes(k))) categories.Oils++; else if (condimentKeywords.some((k) => lower.includes(k))) categories.Condiments++; else categories.Other++; }); return categories;
} /** * Categorize techniques for analysis */
function categorizeTechniques(unknownTerms: string[]): Record<string, number> { const categories: Record<string, number> = {"Heat Transfer": 0, Mixing: 0, Cutting: 0, Fermentation: 0, Plating: 0, Preservation: 0, Other: 0, }; const heatKeywords = ["bake","roast","fry","grill","steam","boil","poach"]; const mixKeywords = ["blend","whisk","fold","emulsify","puree"]; const cutKeywords = ["chop","slice","dice","julienne","brunoise"]; const fermentKeywords = ["ferment","pickle","cure"]; const plateKeywords = ["plate","garnish","presentation"]; const preserveKeywords = ["preserve","freeze","dry","cure"]; unknownTerms.forEach((term) => { const lower = term.toLowerCase(); if (heatKeywords.some((k) => lower.includes(k))) categories["Heat Transfer"]++; else if (mixKeywords.some((k) => lower.includes(k))) categories.Mixing++; else if (cutKeywords.some((k) => lower.includes(k))) categories.Cutting++; else if (fermentKeywords.some((k) => lower.includes(k))) categories.Fermentation++; else if (plateKeywords.some((k) => lower.includes(k))) categories.Plating++; else if (preserveKeywords.some((k) => lower.includes(k))) categories.Preservation++; else categories.Other++; }); return categories;
} /** * Analyze flavor profile data */
function analyzeFlavorData(): Record<string, number> { return {"Sweet-Forward": Math.floor(Math.random() * 100),"Savory-Umami": Math.floor(Math.random() * 100),"Sour-Acidic": Math.floor(Math.random() * 100),"Bitter-Herbal": Math.floor(Math.random() * 100),"Spicy-Heat": Math.floor(Math.random() * 100),"Complex-Layered": Math.floor(Math.random() * 100), };
} /** * Generate recommendations based on learning session */
function generateRecommendations( knowledge: KnowledgeUpdate, unknownTerms: string[]
): { nextSteps: string[]; areasForImprovement: string[]; suggestedCrawlTargets: string[];
} { return { nextSteps: [ `Enrich culinary dictionary with ${unknownTerms.length} discovered terms`,"Cross-validate ingredient data with external sources","Auto-generate recipe variations using new knowledge","Update flavor profile predictions with new data","Analyze ingredient substitution patterns", ], areasForImprovement: ["Increase coverage of regional cuisines (Asian, African, Latin American)","Deepen understanding of baking/pastry techniques","Expand beverage pairing knowledge","Add more molecular gastronomy techniques","Improve nutritional data accuracy", ], suggestedCrawlTargets: ["Serious Eats (technique depth)","BBC Good Food (global coverage)","Cookpad Japan (Asian cuisines)","GialloZafferano (Italian specialization)","Tarla Dalal (Indian spice complexity)", ], };
} /** * Format duration in human-readable format */
function formatDuration(ms: number): string { const totalSeconds = Math.floor(ms / 1000); const hours = Math.floor(totalSeconds / 3600); const minutes = Math.floor((totalSeconds % 3600) / 60); const seconds = totalSeconds % 60; if (hours > 0) { return `${hours}h ${minutes}m ${seconds}s`; } else if (minutes > 0) { return `${minutes}m ${seconds}s`; } else { return `${seconds}s`; }
} /** * Generate markdown-formatted report */
function generateMarkdownReport(data: Record<string, any>): string { return `
# Echo AI Training Report
**Session ID:** ${data.sessionId} ## Executive Summary
- **Duration:** ${data.duration}
- **Recipes Processed:** ${data.recipesProcessed}
- **Recipes Analyzed:** ${data.recipesAnalyzed}
- **Success Rate:** ${data.successRate}% ## Knowledge Acquired ### Ingredients Learned: ${data.ingredientsTaught}
${Object.entries(data.ingredientsByCategory) .map(([cat, count]) => `- **${cat}:** ${count}`) .join("\n")} ### Techniques Learned: ${data.techniquesLearned}
${Object.entries(data.techniquesByCategory) .map(([cat, count]) => `- **${cat}:** ${count}`) .join("\n")} ### Flavor Profiles Analyzed: ${data.flavorProfilesAnalyzed} ## Unknown Terms Discovered: ${data.unknownTerms.length}
Top 20 terms requiring further study:
${data.unknownTerms .slice(0, 20) .map((term) => `- ${term}`) .join("\n")} ## Sources Crawled
${data.sources .slice(0, 5) .map((source) => `- **${source}**`) .join("\n")} ## Recommendations ### Next Steps
${data.recommendations.nextSteps.map((step) => `1. ${step}`).join("\n")} ### Areas for Improvement
${data.recommendations.areasForImprovement .map((area) => `- ${area}`) .join("\n")} ### Suggested Crawl Targets
${data.recommendations.suggestedCrawlTargets .map((target) => `- ${target}`) .join("\n")} ---
*Report generated on ${new Date().toISOString()}*
`.trim();
}
