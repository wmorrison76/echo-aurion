/** * Express API routes for scheduling operations. * Includes CRUD, publish workflow, and audit logging. * Enhanced with Zod validation and RBAC. */
import { Router } from"express";
import { z } from"zod";
import { supabase } from"../../lib/db";
import type { Shift, PublishAudit } from"../../../shared/types/schedule";
import { emitShiftCreatedEvent } from"../../../../../server/lib/financial-event-emitter";
import { requireRole, requireDept, authenticateUser,
} from"../../middleware/authz";
import { v4 as uuidv4 } from"uuid"; // Zod schemas for validation
const ShiftSchema = z.object({ id: z.string().uuid().optional(), org_id: z.string().uuid(), outlet_id: z.string().uuid(), dept_id: z.string().uuid(), employee_id: z.string().uuid(), position_id: z.string().uuid(), starts_at: z.string().datetime(), ends_at: z.string().datetime(), break_min: z.number().int().min(0).max(480).optional().default(0), published: z.boolean().optional().default(false), tips_declared: z.number().min(0).optional().default(0),
}); const BulkShiftSchema = z.object({ shifts: z.array(ShiftSchema).min(1).max(500),
}); const PublishRequestSchema = z.object({ org_id: z.string().uuid(), outlet_id: z.string().uuid(), dept_id: z.string().uuid(), week_start: z.string().datetime(), notes: z.string().optional(),
}); const GetScheduleQuerySchema = z.object({ outlet_id: z.string().uuid(), dept_id: z.string().uuid(), week_start: z.string().datetime(), limit: z.coerce.number().int().min(1).max(1000).optional().default(1000), offset: z.coerce.number().int().min(0).optional().default(0),
}); const AuditQuerySchema = z.object({ outlet_id: z.string().uuid(), dept_id: z.string().uuid(), limit: z.coerce.number().int().min(1).max(500).optional().default(100), offset: z.coerce.number().int().min(0).optional().default(0),
}); // Simple UUID generator fallback
const generateId = () => { try { return uuidv4(); } catch { return"xxxxxxxx-xxxx-4xxx-yxxx-xxxxxxxxxxxx".replace( /[xy]/g, function (c) { const r = (Math.random() * 16) | 0; const v = c ==="x" ? r : (r & 0x3) | 0x8; return v.toString(16); }, ); }
}; const router = Router(); // Apply authentication middleware to all routes
router.use(authenticateUser); /** * GET /api/schedule * Fetch weekly schedule by outlet/department with pagination * Requires: user to be in same department */
router.get("/", requireDept("dept_id"), async (req, res) => { try { const validation = GetScheduleQuerySchema.safeParse(req.query); if (!validation.success) { return res.status(422).json({ error:"Invalid request parameters", details: validation.error.format(), }); } const { outlet_id, dept_id, week_start, limit, offset } = validation.data; const weekDate = new Date(week_start); const weekStartDate = new Date(weekDate); weekStartDate.setDate(weekStartDate.getDate() - weekStartDate.getDay() + 1); // Monday const weekEndDate = new Date(weekStartDate); weekEndDate.setDate(weekEndDate.getDate() + 7); const { data, error, count } = await supabase .from("shifts") .select("*", { count:"exact" }) .eq("outlet_id", outlet_id) .eq("dept_id", dept_id) .gte("starts_at", weekStartDate.toISOString()) .lt("starts_at", weekEndDate.toISOString()) .order("starts_at", { ascending: true }) .range(offset, offset + limit - 1); if (error) throw error; res.json({ data: data || [], pagination: { limit, offset, total: count || 0, hasMore: offset + limit < (count || 0), }, }); } catch (err: any) { console.error("GET /schedule error:", err); res.status(500).json({ error:"Internal Server Error" }); }
}); /** * POST /api/schedule/bulk * Bulk upsert shifts * Requires: MANAGER or ADMIN role */
router.post("/bulk", requireRole("MANAGER","ADMIN"), async (req, res) => { try { const validation = BulkShiftSchema.safeParse(req.body); if (!validation.success) { return res.status(422).json({ error:"Invalid request body", details: validation.error.format(), }); } const { shifts } = validation.data; // Verify all shifts belong to same outlet (prevent bulk cross-outlet operations) const outletIds = new Set(shifts.map((s) => s.outlet_id)); if (outletIds.size > 1) { return res.status(400).json({ error:"All shifts must belong to the same outlet", }); } const upsertData = shifts.map((s) => ({ id: s.id || generateId(), org_id: s.org_id, outlet_id: s.outlet_id, dept_id: s.dept_id, employee_id: s.employee_id, position_id: s.position_id, starts_at: s.starts_at, ends_at: s.ends_at, break_min: s.break_min, published: s.published, tips_declared: s.tips_declared, created_by: (req as any).user?.id ||"system", })); const { error } = await supabase.from("shifts").upsert(upsertData); if (error) throw error; // Emit financial events and Stratus events for each shift (for real-time P&L updates and decision intelligence) for (const shift of upsertData) { try { const startTime = new Date(shift.starts_at).getTime(); const endTime = new Date(shift.ends_at).getTime(); const durationMinutes = (endTime - startTime) / 60000; const durationHours = durationMinutes / 60; // Default hourly rate of $25 (in production, fetch from employees table) const hourlyRate = 25; const laborCost = durationHours * hourlyRate; // Emit financial event (existing) emitShiftCreatedEvent( shift.outlet_id, shift.org_id, { shift_id: shift.id, employee_id: shift.employee_id, start_time: startTime, end_time: endTime, hourly_rate: hourlyRate, labor_cost: laborCost, }, (req as any).user?.id, ); // Emit Stratus event for decision intelligence try { const { emitLaborShiftPublished } = await import("../../../../../../server/lib/module-event-emitters.js"); await emitLaborShiftPublished({ tenant_id: shift.org_id, org_id: shift.org_id, outlet_id: shift.outlet_id, shift_id: shift.id, employee_id: shift.employee_id, start_time: shift.starts_at, end_time: shift.ends_at, hourly_rate: hourlyRate, labor_cost: laborCost, role: shift.position_id, published_at: new Date().toISOString(), }); } catch (stratusError) { console.warn("Failed to emit Stratus shift event:", stratusError); // Continue processing even if Stratus event emission fails } } catch (eventError) { console.warn("Failed to emit shift event:", eventError); // Continue processing even if event emission fails } } res.status(201).json({ status:"ok", count: upsertData.length, ids: upsertData.map((s) => s.id), }); } catch (err: any) { console.error("POST /schedule/bulk error:", err); res.status(500).json({ error:"Internal Server Error" }); }
}); /** * POST /api/schedule/publish * Publish week (creates audit record and marks shifts as published) * Requires: MANAGER or ADMIN role */
router.post("/publish", requireRole("MANAGER","ADMIN"), async (req, res) => { try { const validation = PublishRequestSchema.safeParse(req.body); if (!validation.success) { return res.status(422).json({ error:"Invalid request body", details: validation.error.format(), }); } const { org_id, outlet_id, dept_id, week_start, notes } = validation.data; const auditId = generateId(); const publishedBy = (req as any).user?.id ||"system"; // Create audit record const { error: auditError } = await supabase.from("publish_audits").insert({ id: auditId, org_id, outlet_id, dept_id, week_start: new Date(week_start).toISOString().split("T")[0], published_by: publishedBy, notes, }); if (auditError) throw auditError; // Mark shifts as published for this week const weekDate = new Date(week_start); const weekStartDate = new Date(weekDate); weekStartDate.setDate(weekStartDate.getDate() - weekStartDate.getDay() + 1); const weekEndDate = new Date(weekStartDate); weekEndDate.setDate(weekEndDate.getDate() + 7); const { error: updateError, count, data: updatedShifts } = await supabase .from("shifts") .update({ published: true }) .eq("outlet_id", outlet_id) .eq("dept_id", dept_id) .gte("starts_at", weekStartDate.toISOString()) .lt("starts_at", weekEndDate.toISOString()) .select(); if (updateError) throw updateError; // Emit Stratus events for all published shifts try { const { emitLaborShiftPublished } = await import("../../../../../../server/lib/module-event-emitters.js"); if (updatedShifts && updatedShifts.length > 0) { for (const shift of updatedShifts) { const startTime = new Date(shift.starts_at).getTime(); const endTime = new Date(shift.ends_at).getTime(); const durationHours = (endTime - startTime) / (1000 * 60 * 60); const hourlyRate = 25; // Default, should fetch from employee data const laborCost = durationHours * hourlyRate; await emitLaborShiftPublished({ tenant_id: org_id, org_id: org_id, outlet_id: outlet_id, shift_id: shift.id, employee_id: shift.employee_id, start_time: shift.starts_at, end_time: shift.ends_at, hourly_rate: hourlyRate, labor_cost: laborCost, role: shift.position_id, published_at: new Date().toISOString(), }); } } } catch (stratusError) { console.warn("Failed to emit Stratus publish events:", stratusError); // Don't fail publish if Stratus events fail } res.status(200).json({ published: true, auditId, shiftsUpdated: count || 0, }); } catch (err: any) { console.error("POST /schedule/publish error:", err); res.status(500).json({ error:"Internal Server Error" }); }
}); /** * GET /api/schedule/audits * Retrieve publish audit trail with pagination * Requires: user to be in same department */
router.get("/audits", requireDept("dept_id"), async (req, res) => { try { const validation = AuditQuerySchema.safeParse(req.query); if (!validation.success) { return res.status(422).json({ error:"Invalid request parameters", details: validation.error.format(), }); } const { outlet_id, dept_id, limit, offset } = validation.data; const { data, error, count } = await supabase .from("publish_audits") .select("*", { count:"exact" }) .eq("outlet_id", outlet_id) .eq("dept_id", dept_id) .order("published_at", { ascending: false }) .range(offset, offset + limit - 1); if (error) throw error; res.json({ data: data || [], pagination: { limit, offset, total: count || 0, hasMore: offset + limit < (count || 0), }, }); } catch (err: any) { console.error("GET /schedule/audits error:", err); res.status(500).json({ error:"Internal Server Error" }); }
}); export default router;
