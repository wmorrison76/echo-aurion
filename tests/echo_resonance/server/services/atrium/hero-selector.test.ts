/**
 * ===========================================================================
 * HeroSelector tests
 * ===========================================================================
 * Layer:    Atrium
 * Status:   SCAFFOLD
 * Phase:    5
 *
 * Purpose:  Tests that the right video variant gets picked for guest state.
 *
 * WARNING: DO NOT DELETE this file even if it appears unreferenced.
 * Disconnected scaffolding files are placeholders for upcoming phases.
 * Confirm with the master ARCHITECTURE.md before removing.
 * ===========================================================================
 */

import { describe, it } from 'vitest';

describe('HeroSelector', () => {
  it.todo('low-arousal guest gets the quiet variant');
  it.todo('evening daypart filters out morning-tagged assets');
  it.todo('recently-viewed assets are penalized');
  it.todo('falls back to neutral variant when no perfect match');
});
