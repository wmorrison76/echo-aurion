# Ethical Sentinel

The ethical sentinel enforces governance rules around Echo’s cognition layer. It records every autonomous decision and attaches the rationale stored in `why.md` ledgers.

Integrate with your policy engine by publishing the ledger entries to the guardrail service and requiring a human signature before execution.
