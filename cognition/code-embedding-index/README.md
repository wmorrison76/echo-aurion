# Code Embedding Index

Use `buildEmbeddingIndex` to gather code documents suitable for embedding and searching. The function walks the repository, filters out large or unwanted files, and returns a consistent checksum for incremental updates.
